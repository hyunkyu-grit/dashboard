# 인계 — sauron-v2, 2026-08-14 (레인 6 · 내비 패널 + 커서 리드아웃)

앞 인계는 `HANDOFF.md`(레인 1까지). 이 문서는 그 뒤 세션이고, 아래는 전부 실측이다 —
**재검증하지 말 것.** 오너가 확인한 것까지 반영돼 있다.

---

## 0. 오너 상시 규칙 (깨지 말 것)

- **코드 고칠 때 항상 v1 을 읽으며 계약을 대사한다.** v1 = `Desktop\Assistant\Projects_AS\braveworld`.
  나중에 맞춰보는 게 아니라 **쓰면서** 맞춘다. 레인 5 첫 판이 이걸 안 해서 세 군데를
  어겼고, 셋 다 그럴듯한 숫자를 냈다.
- **v1 은 이 트리에서 절대 수정하지 않는다.** 읽기 전용이다.
- **커밋·푸시는 오너 몫.** 에이전트는 커밋하지 않는다.
- 오너가 정해야 하는 것은 **선택지로** 제시한다.
- 이 리포는 **pnpm**. `npm install` 금지.
- 포트 주인: `:3000/:8000` 동결 krw-fi-pms · `:3100/:8100` braveworld(v1) · `:3200/:8200` v2.
- 검증용 서버는 끝나면 내린다. **지금은 오너 지시로 `:3200` 을 켜 둔 상태다**("웹 열어봐").

---

## 1. 지금 상태

| | |
|---|---|
| HEAD | `127b878` — 이 리포는 **git 리포가 맞다**(환경 배너가 false 라고 말하지만 틀렸다) |
| 미커밋 | **66개 경로**. v2 작업 전체가 아직 워킹트리에만 있다 |
| 가드 | **357/357** (`pnpm vitest run`) |
| 빌드 | `pnpm build` 통과 |
| 프런트 | `:3200` — **`next start`(구운 빌드)**, `next dev` 아님 |
| 백엔드 | `:8200` 살아 있음 |

---

## 2. 이번 세션의 함정 — 여기서 시간을 다 썼다

### 2.1 `:3200` 은 구운 빌드다 — 소스를 고쳐도 새로고침으로 안 바뀐다

CSS 를 고치고 새로고침해도 `z-index` 가 계속 `auto` 로 나와서 한참 헤맸다. 원인은
`:3200` 에 `next dev` 가 아니라 **`next start`** 가 떠 있었다는 것. 굽기 시점의
번들을 그대로 서빙하므로 소스 편집이 반영될 리가 없다.

**규칙: 이 트리에서 화면을 확인할 땐 「굽기 → 서버 재시작」이 한 세트다.**

```powershell
# 지금 뭐가 떠 있나
Get-NetTCPConnection -LocalPort 3200 -State Listen | ForEach-Object {
  (Get-CimInstance Win32_Process -Filter "ProcessId=$($_.OwningProcess)").CommandLine }
```

`pnpm build` 가 돌던 서버를 **죽일 때도 있고 안 죽일 때도 있다.** 안 죽이면 새 서버가
`EADDRINUSE` 로 죽는다 → 리스너 PID 를 직접 죽이고 다시 띄운다:

```powershell
Get-NetTCPConnection -LocalPort 3200 -State Listen |
  Select-Object -ExpandProperty OwningProcess -Unique |
  ForEach-Object { Stop-Process -Id $_ -Force -Confirm:$false }
```
```bash
# Start-Process 는 샌드박스에서 EPERM. Bash 툴 + run_in_background 로 띄운다.
cd /c/Users/infomax/Desktop/Assistant/Projects_AS/sauron-v2 && \
  node node_modules/next/dist/bin/next start --port 3200
```

### 2.2 창이 백그라운드면 **CDS 차트가 통째로 안 그려진다**

`document.hidden === true` 인 창에서:

- 차트 `<svg>` 의 **자식이 0개**다(선도 축도 없음).
- 그러면 CDS 의 `chartRect.width <= 0` 가지가 타서 `xScales` 맵이 **빈 채로 남고**
  (`CartesianChart.js:96`), `getXScale()` 이 `undefined` 가 된다.
- 그 결과 `ScrubberProvider.js::getDataIndexFromPosition` 의
  `if (!categoryScale || !categoryAxis) return 0` 이 타서 **스크러버 인덱스가 항상 0**이다.
- `Page.captureScreenshot` 도 30초 타임아웃 난다(렌더 프레임이 안 나온다).

**이 상태에서 잰 값은 전부 무효다.** 이 리포가 rAF·ResizeObserver 로 이미 세 번 겪은
종류의 인공산물이고, 이번엔 CDS 차트에서 같은 얼굴로 나왔다. 브라우저로 검증하기 전에
반드시 `document.hidden` 을 먼저 확인할 것.

### 2.3 백엔드 `:8200` 은 루트가 404 다

`Invoke-WebRequest http://127.0.0.1:8200/` 는 예외를 던져서 **죽은 것처럼 보인다.**
살아 있는지는 리스너로 본다: `Get-NetTCPConnection -LocalPort 8200 -State Listen`.

### 2.4 합성 마우스 이벤트를 믿지 말 것

메가 패널을 진단할 때 `dispatchEvent(new MouseEvent('mouseenter'))` 가 어떤 때는 열고
어떤 때는 안 열어서 없는 버그를 쫓았다. 결론은 **진짜 hover(`computer` 툴)로 확인**하고,
합성 이벤트는 보조로만 쓴다.

---

## 3. 이번 세션에 한 일

### 3.1 메가 패널이 안 나오던 것 — 원인은 **덮개가 패널 위에 있었다**

`.sr-nav` 가 `position: relative; z-index: 20` 으로 쌓임 맥락을 만들고, 그 안에서

| | z-index | |
|---|---|---|
| `.sr-megascrim` | `19` | 19층 |
| `.sr-mega` | **없음 → `auto`** | 0층 |

19 가 0 을 덮었다. 33% 회색이 흰 시트에 얹히니 아래 화면과 밝기가 붙어 경계가 사라졌고,
`elementFromPoint(400,200)` 이 덮개를 돌려줬다 — **항목을 누를 수조차 없었다.** 덮개는
직전 턴에 내가 넣은 것이라, 정확히 그때부터 "패널이 안 나온다" 가 됐다.

- 고침: `src/theme/type.css` 의 `.sr-mega` 에 `z-index: 20`
- 가드: `guards/mega-stacking.test.ts` — 두 숫자가 다른 규칙에 떨어져 있어 한쪽만
  고치면 조용히 뒤집히는 자리다

### 3.2 커서 리드아웃 카드 신설 (오너 지시)

> "커서 가져다대도 패널 같은 건 안나와. 패널에서는 날짜, 레벨, 52주 최고·최저·평균,
> CD91 금리가 나와야 함"

**왜 없었나**: `Scrubber` 에 `accessibilityLabel` 만 주고 있었다. 그건 스크린리더용
문자열이라 화면에 아무것도 안 그린다. CDS 가 눈에 보이게 주는 건 스크러버 선 위의
**한 줄짜리** 라벨(`Scrubber.label`)뿐이라 여섯 줄이 안 들어간다. 그래서 v1 방식으로
갔다 — **카드는 SVG 밖의 HTML, 인덱스만 CDS 에서 받는다**(`onScrubberPositionChange`).

**새 파일 / 바뀐 파일**

```
src/ui/ReadoutCard.tsx          신설 — v1 `ui/ReadoutCard.tsx` 포팅
src/ui/PreviewPane.tsx          배선 + CHART_INSET + stats/d 수신
src/chart/curve.ts              IdleCurve 에 high/low/avg (range1y)
src/theme/type.css              .sr-plot(relative) · .sr-readout · .sr-readout-rows/-change
src/theme/direction.css         --sr-popover · --sr-popover-shadow (라이트/다크 쌍)
guards/readout-card.test.tsx    신설 9건
```

**줄 구성** — 날짜(제목) · 레벨 · 52주 최고 · 52주 최저 · 52주 평균 · CD 91일 · 당일 변화

- **52주 셋은 서버 값**(`/api/series` 의 `stats`, 최근 252관측). 구간을 1M 으로 좁혀도
  **안 바뀐다** — v1 과 같은 규칙이고, 차트가 보여주는 구간의 고저는 히어로 밑
  「이 구간」 열이 따로 말한다. 응답에 원래 있던 필드를 프런트가 버리고 있었다.
- **당일 변화도 서버 값**(`points[].d`). 브라우저는 시계열을 차분하지 않는다(§16).
- **CD 91일은 `refs.cd` 가 있을 때만** 나온다 — 없는 선의 값을 읽지 않는다.
- 아이들 커브에도 **같은 카드**를 달았다. 만기가 날짜 자리에 오고 나머지 다섯 줄은 같다
  (v1 `ui/CurveView.tsx` 와 동일한 구조).

**v1 대사 결과 (중요)**: v1 의 종목 차트 카드에는 **CD91 이 없다.** v1 은 `CD 91일`·
`기준금리` 를 백테스트의 구성 금리 패널(`ui/LinkedLegsChart.tsx`)에서만 찍고,
`ui/readouts.ts` 의 키 목록에 그 둘이 아예 없다. 오너 지시로 v2 에 추가하면서 **라벨은
v1 것을 그대로** 가져왔다(같은 값에 표면마다 다른 이름을 붙이지 않기 위해).

**내용 실측 대사** — 표와 일치 확인:

```
5Y   레벨 2.4700 · 52주 4.1850 / 2.4325 / 3.3567 · CD 91일 2.5100 · 당일 변화 −7.0
3M   레벨 2.9300 · 52주 2.9500 / 2.5000 / 2.7466        (커브 노드)
```

### 3.3 차트 여백

`CHART_INSET = { top: 16, right: 12, bottom: 8, left: 8 }` (전: `right: 8, bottom: 0`).
오른쪽 y 눈금이 SVG 오른쪽 끝에 **여백 0**으로 붙어 있었다 — 라벨 오른쪽 2529px,
SVG 오른쪽 2529px. 잘리진 않았지만 자릿수나 폰트가 조금만 바뀌면 잘릴 자리였다.
두 차트가 **같은 상수**를 쓴다.

### 3.4 통계 3열이 잘리던 것 — 차트 높이를 **상수로 뺐기 때문**

> 오너: "밑에 「이 구간」과 같은 정보들이 잘려서 안 보인다는거에요"

실측(2026-08-14, 카드 911px):

```
        전                후
히어로  128              128
차트    732   ← 상수     643   ← 잰 값
범례     22               22
통계    115  (88px 잘림)  115  (전부 보임)
```

`page.tsx` 가 `height={카드높이 − PREVIEW_CHROME_H(176)}` 로 차트 높이를 정하고 있었다.
차트 아닌 것의 실제 합은 **265**(128+22+115)라 상수가 89px 모자랐고, 그만큼 차트가
커져 통계 3열이 카드 밖으로 88px 밀려났다. `.sr-card` 가 `overflow: hidden` 이라
**잘린 쪽은 아무 흔적도 안 남긴다** — 화면에서는 그 정보가 그냥 없었다.

**상수를 265 로 올리는 것이 답이 아니다.** 셋 다 고정 높이가 아니다: 범례는 기준선이
있을 때만 서고(bp·ratio 차트엔 없다), 통계 3열은 폭이 좁으면 접히고, 히어로는 라벨이
길면 줄바꿈한다. 그래서 `PreviewPane` 에 **`fill`** 을 붙여 남는 높이를 스스로 재게
했다(표가 이미 쓰는 `useFillHeight` 와 같은 방법).

- `flexBasis={0}` 이 **되먹임을 끊는 핵심**이다. 없으면 상자의 기본 크기가 자기 내용
  (= 방금 잰 높이로 그린 차트)이 되어 잴 때마다 조금씩 자란다.
- 확대 창(`height={460}`)과 오버뷰 3열(`chartOnly`, 고정 200 [v1 OWNER])은 이 모드를
  **안 쓴다.** 실측으로 영향 없음 확인: Main 3열 카드 983 / 그림 189 / 넘침 −23.
- 가드: `guards/preview-fill.test.ts`

---

## 4. 미해결 — 다음 세션의 첫 일

### ① 【해결】 리드아웃 카드

오너 확인: "이제 패널은 나오고요". 인덱스가 0 으로 굳어 보였던 것은 §2.2 의
**백그라운드 창 인공산물**이 맞았다. 코드 변경 없음.

### ② Calendar 탭 — **보류** [OWNER 2026-08-14: "일단 신경쓰지 않는 걸로, 추후의 문제"]

착수하지 않았다. 조사만 했으므로 다음에 다시 파지 않도록 결론만 남긴다.

**무엇을 붙이는 것인가**: 오너가 이미 만들어 둔 **수급 분석 캘린더** =
`Desktop\Codex\rawData` (git 아님, `:3300`/`:8300`). braveworld 의 정책회의
캘린더(`ui/calendar.ts`)가 **아니다** — 그건 다른 물건이고, 이번 지시의 대상이 아니었다.

**오너가 이 이식을 이미 계획해 뒀다**:
- `PRODUCT.md` — "같은 화면이 곧 Sauron(braveworld) 연구실 탭으로 이식된다"
- `README.md`(08-10) — "스택은 braveworld와 같다 … **연구실 탭 이식을 준비하는 얼라인**"

즉 목적지만 바뀐다(v1 Lab 탭 → v2 Calendar 탭).

**그 앱의 모양**
- API 셋: `/api/calendar?months=&start=` · `/api/day/{iso}` · `POST /api/refresh`
- 화면: `frontend/src/App.tsx` **920줄** + `app.css` 608줄. 월/목록 두 모드, 레인
  필터 6종(발행예고·국고채입찰·공개시장운영·금통위·통안채계획·지준), 상세 2단,
  국고채 입찰 **강도**(52주 대비 응찰률·물량 백분위·가중평균 낙찰금리 Δbp),
  OMO 강도(공급/흡수), DART 원문 링크
- 서버는 `data/*.csv` 만 읽는다. 수집은 스케줄러 둘(`rawDataWatch` 평일 5분,
  `rawDataFullCollect` 평일 07:30)이 따로 돈다. 2026-08-14 기준 데이터 신선함
  (`ktb_auction.csv` 11:35 · `omo.csv` 10:40). 서버는 둘 다 내려가 있었다.

**CDS 판정 — 조건부 예스**
- **CDS `Calendar`(`@coinbase/cds-web/dates`)는 이 화면에 못 쓴다.** 날짜 하나 고르는
  피커라 셀에 섹터별 금액·건수·레인 배지를 넣을 자리가 없고, **커스텀 day 렌더러가
  없다**(`CalendarBaseProps` 전체 확인). 월 그리드는 직접 그려야 한다 — rawData 도
  자기 격자를 직접 그렸다.
  *(주의: CDS `Calendar` 는 v1 의 **정책회의** 캘린더에는 잘 맞는다. 두 물건을 섞지 말 것.)*
- 나머지는 전부 CDS 로 갈아탄다: 월/목록 `SegmentedTabs` · 레인 필터 `Chip` ·
  섹터 접기 `Collapsible` · 목록 모드 `Table` · 상세 2단은 **떠 있는 창**
  (`src/ui/window/`, 이미 확대·백테스트가 세든 기구) · 항목 `ListCell`.
  유리 툴바·신호등 같은 macOS 크롬은 버린다(v2 셸이 대신한다).
- 서버 문장(`src/gloss.py`)이 단일 출처라는 규칙은 v2 §16 과 같은 규칙이다. 그대로 산다.

**정하지 않은 것 둘** (착수할 때 오너에게 물을 것)
1. 데이터 경로 — ① `:8200` 이 프록시 ② 아침 굽기에 정적으로 ③ 둘 다
   (로컬은 라이브, Vercel 은 구운 것 — v2 가 이미 쓰는 `API_BASE`/`staticPaths` 패턴)
2. 첫 판 범위 — 월 그리드+그날 목록까지만인지, 강도·상세 2단까지 한 번에인지

### ③ 레인 6 나머지

Strategy·Lab 은 아직 "아직 만들지 않았어요" 상태. 그 외 미착수: 커맨드 바, 하단 스트립,
체인지로그, 인트로 커튼, 에러 바운더리, 모션 시스템, 틴트 범례, 표로 보기 매트릭스.

---

## 5. 검증 명령

```powershell
Set-Location C:\Users\infomax\Desktop\Assistant\Projects_AS\sauron-v2
pnpm vitest run          # 357/357
pnpm build

# 굽고 나면 반드시 서버 교체 (§2.1)
Get-NetTCPConnection -LocalPort 3200 -State Listen |
  Select-Object -ExpandProperty OwningProcess -Unique |
  ForEach-Object { Stop-Process -Id $_ -Force -Confirm:$false }
```
```bash
cd /c/Users/infomax/Desktop/Assistant/Projects_AS/sauron-v2 && \
  node node_modules/next/dist/bin/next start --port 3200
```

브라우저로 검증할 땐 **탭 ID 를 재사용하지 말고** `tabs_context_mcp` 로 새로 받는다.
그리고 재는 것보다 먼저:

```js
document.hidden   // true 면 이 세션의 모든 측정이 무효다 (§2.2)
```
