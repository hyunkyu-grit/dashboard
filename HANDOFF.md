# 인계 — sauron-v2 (2026-08-13 Coinbase 패스 + 2026-08-14 레인 1)

이 세션에서 확정된 사실과 미해결 상태. **재검증하지 말 것** — 아래는 전부 실측이다.

앞 인계(디자인 패스)의 「다음 할 일」 6개는 전부 완료되었거나 이 문서로 대체되었다.

> **2026-08-14 추가 — 레인 1(표 완성) 다섯 항목 전부 완료.** 세타 열 · 커브 배너 ·
> 호가/보간 · hover 미리보기 · 포워드 시작점 필터. 프론트 가드 **159/159**, `pnpm
> build` 통과, 백엔드 `pytest` 355 통과 / 2 실패(**둘 다 사전 존재** — §8.6 참조).
> **여전히 커밋 0건이다.** 자세한 것은 §8.

---

## 0. 세션 재시작 직후 할 일

### CDS MCP — 재시작 불필요

`.mcp.json` 은 프로젝트 스코프라 다른 디렉터리에서 Claude Code 를 켜면 안 잡힌다.
**그래도 재시작할 필요 없다.** `cds-mcp-server` 는 stdio 로 JSON-RPC 를 주고받는 평범한
Node 프로세스라 직접 띄우면 된다. 스크래치패드에 두 개 있었고, 없으면 다시 만들면 된다:

```
node cdsq.mjs --list                                   # 도구 목록
node cdsq.mjs list-cds-routes '{"platform":"web"}'     # 전체 카탈로그
node cdsfetch.mjs web/components/LineChart.txt ...     # 문서를 파일로 (컨텍스트 절약)
```

서버 실경로(재설치하면 pnpm 해시가 바뀐다):

```
node_modules/.pnpm/@coinbase+cds-mcp-server@9.15.0_@types+node@22.20.1/
  node_modules/@coinbase/cds-mcp-server/esm/server.js
```

- 패키지는 **처음부터 설치돼 있다**(`cds-web → cds-common → cds-mcp-server` 전이 의존성).
- `cds-mcp-setup` 은 대화형 마법사라 비대화형 셸에서 반드시 죽는다. 자동화 불가.
- **이 리포는 pnpm 이다. `npm install` 금지.**

### 앱 띄우기

```
pnpm build && npx next start --port 3200
```

**포트가 중요하다.** 백엔드 CORS 가 `:3200`/`:3100` 만 허용한다
(`backend/app/main.py:197`). 다른 포트로 띄우면 "FAILED TO FETCH" 로 죽는데, 데이터가
없는 게 아니라 오리진이 막힌 것이다.

다른 포트를 써야 하면 읽기 전용 CORS 심을 스크래치패드에 띄우고 페이지의
`window.fetch` 를 그쪽으로 돌린 뒤 화면의 "다시 시도" 를 누르면 된다(이 세션이 :8477 로
그렇게 했다).

포트 임자: `:3000`/`:8000` 동결 krw-fi-pms · `:3100`/`:8100` braveworld ·
`:3200`/`:8200` v2. 검증용 서버는 다른 포트에 띄우고 **끝나면 반드시 내릴 것.**

---

## 1. 이 세션이 한 일

오너 지시 넷을 처리했다.

1. **방향색은 토스, 나머지는 Coinbase, 디자인도 Coinbase 처럼** — `/price/bitcoin` 을
   해부해서 차트·히어로·기간선택·스탯 3열을 이식
2. **필터 항목 제거 + 상단 탭을 `coinbase_tab.png` 처럼** — Main/Backtest/Simulation/
   Strategy/Lab 메가메뉴
3. **한글 깨짐 파악** — 폰트가 아니라 흰 글자 위 흰 배경이었다
4. **CDS 컴포넌트만 쓰기 + v1 기능 차이 조사**

---

## 2. 오너 결정 (확정)

| 결정 | 값 | 비고 |
|---|---|---|
| 행 높이 | **60px**, 이름 두 줄 | CDS `condensed` 자연값과 정확히 일치 |
| 굵기 | **CDS(=Coinbase) 그대로 400/600** | `legal`(13px)만 500 — 아래 참조 |
| 방향색 | **토스 적/청** | `#de2b39` / `#2171eb` |
| 필터 칩 | **제거** | 조건자는 `table/screener.ts` 에 남겨둠 |
| 상단 내비 | **5섹션 메가메뉴** | Coinbase 상단 바 구조 |
| Backtest 분류 | **스왑·국채현물·국채선물·크레딧** | `bss` 는 스왑에 넣음(제 판단, 한 줄로 이동 가능) |
| 숫자 셀 굵기 | **400** [2026-08-14] | = CDS 기본. 토스 재측정(600)에도 Coinbase 스케일 유지. **닫힌 결정** |
| 세타 정렬 키 | **없음** [2026-08-14] | 52주 열과 같다 — 헤더에 컨트롤 없음 |
| 꼬리 우선순위 | **52주 → 세타 → 위치** [2026-08-14] | 세타가 제일 먼저 사라지던 것 뒤집음 |

`legal` 만 500 인 이유: **Coinbase 는 본문을 13px 로 두지 않는다**(최소 본문 14/400).
v2 는 60px 행이라 둘째 줄이 13px 로 내려가고, 그 크기에서 한글은 400 이면 획이 뭉개진다.
Coinbase 스케일과의 불일치가 아니라, 그 스케일이 쓰지 않는 크기에서 한글을 그릴 때
필요한 보정이다.

---

## 3. 확정된 실측

### 3.1 Coinbase 차트 조리법 (`/price/bitcoin` 해부)

**그 페이지는 CDS 로 그려져 있다.** 축 라벨이 `var(--color-fgMuted)` 를 푼다. 그래서
흉내내지 말고 **같은 컴포넌트를 부르면 된다** — `LineChart` + `Scrubber`,
`showArea` + `areaType="dotted"`.

영역 채움의 실제 구조:

```
<mask> <path d={area} fill="url(#pattern)"/> </mask>   pattern = 4×4, circle r=1 (흰색)
<path d={area} fill="url(#gradient)" mask="url(#mask)"/>  gradient = 색상, α 0→1
<path d={line} stroke={색상} stroke-width="2" linecap/linejoin="round"/>
```

측정값: 히어로 숫자 **40px/400**(= CDS `display3`), 섹션 제목 20/600(`title3`),
본문 16/400(`body`), 강조 16/600(`headline`), **표 헤더 16/700 muted**(CDS 에 700
토큰 없음), 자산표 행 **76px**, 이름칸 두 줄 16/600 위 14/400 muted,
행마다 64×32 스파크라인, y축 오른쪽·격자 없음, 기간 알약 40px 완전 라운드,
**활성 알약이 차트의 부호색을 먹는다**.

### 3.2 외부 제품 재측정 (앞 세션 인계값 중 셋이 틀렸다)

`TreeWalker` 로 보이는 텍스트 노드 전부를 세었다. 표본 Coinbase 306 / 토스 1,007 /
Robinhood 365.

- **Coinbase 는 700 을 쓴다** — 표 헤더 16px/700. "400·600 2종"은 본문에 대해서만 맞다.
- **토스의 표 본문은 500 이 아니라 600 이다** — 실시간 차트의 1차 값이 전부 13px/600
  이고, 500 은 12px 보조 숫자 자리다. **"한국 관례 = 500 본문" 이라는 근거가 없어졌다.**
- **Robinhood 의 500 은 표제 전용**(32px/500, 24px/500). 본문은 13/400·13/700.
- 토스 행 높이는 인계값 "~36px" 이 아니라 **40px**(그리고 **한 줄** 행이라 두 줄인
  우리 60px 과 직접 비교할 값이 아니다).
- 방향 관례 확인: 토스 상승 `rgb(222,43,57)` / 하락 `rgb(34,114,235)`, Coinbase 는
  녹/적. **CDS 의 `fgPositive`(초록)를 쓰면 한국 독자에게 하락이 초록으로 보인다.**

### 3.3 방향색 — 파랑만 명도 한 칸 내렸다

```
#de2b39  worst 4.66  통과
#2272eb  worst 4.49  ← 기준 4.5 에 0.01 미달
```

색상(hue) 고정하고 명도만 −0.002 → **`#2171eb`**(4.54). 8비트로 초록 채널 114→113,
육안 동일. 다크 쌍도 같은 두 색에서 밝기만 올려 유도(`#e86c76` 5.31 / `#5794f0` 5.35).

### 3.4 면 3단 — 스킴마다 토큰이 뒤집힌다

**라이트에서 `bgElevation1`·`bgElevation2` 는 `bg` 와 똑같은 흰색**이다(L=1.000 셋 다).
CDS 의 라이트 elevation 은 색이 아니라 그림자다.

```
             라이트                      다크
--sr-page    bgAlternate  238,240,243   bg            10,11,13
--sr-card    bg           255,255,255   bgElevation1  20,21,25
--sr-control bgAlternate  238,240,243   bgSecondary   40,43,49
```

**부호 있는 숫자는 카드 위에만.** `bgAlternate` 위에서 상승 4.08 / 하락 3.98 로 미달.

### 3.5 표 폭 산술 (ch = 8.791, Pretendard SR 14px/500)

```
label 159 + 현재 106 + 변화 3개 212 + 52주 230 + 위치 track 77 = 784px 필요
```

표 캡이 760 이라 **24px 차이로 위치 track 이 조용히 떨어지고 있었다.** 860 으로 올림.

`WIDEST.label` 은 아직 낡았다 — 가장 긴 식별자를 9글자 `1s1.5s10s`(62px)로 선언하는데
실제 최대는 **`10년 국채선물 내재금리` 130.9px** 이다. px 쿠션이 가려주고 있고, 정직한
수리는 한글 라벨용 px 단위 `WIDEST` 항목을 따로 두는 것이다(미해결).

---

## 4. 이번 세션이 잡은 버그

### 4.1 「한글 깨짐」 = 흰 글자 위 흰 배경

폰트는 정상이었다(한글 폭 Pretendard 128.47 / 폴백 136.06 / Malgun 149.63 — 셋 다 달라서
실제로 Pretendard 로 그려짐). 진짜 원인:

```
활성 칩 안쪽 SPAN  color = rgb(255,255,255)   ← 흰색
활성 칩 배경        = --sr-up 12% 틴트         ← 거의 흰색
                                              → 대비 약 1.1:1
```

`<Chip inverted>` 가 전경을 흰색으로 두는데(진한 채움 전제) `.sr-pill[data-active]` 가
**배경만** 옅은 틴트로 덮어써서 짝이 깨졌다. **배경을 바꿀 때 전경도 같이 움직여야 한다.**

### 4.2 CSS 변수를 스코프 위에서 정의 — 이 리포의 세 번째 재발

**CDS 는 `--color-*` 를 `:root` 가 아니라 자기 ThemeProvider 래퍼에 인라인으로** 심는다.
커스텀 속성 값은 **선언된 자리에서** 치환되므로 `:root { --sr-card: var(--color-bg) }` 는
무효값이 된다.

```
:root      --color-bg = (없음)
카드 요소   --color-bg = rgb(255,255,255)   ← 여기엔 있다
카드 요소   --sr-card  = (빈 값)            ← :root 에서 이미 죽었다
```

**아무것도 깨져 보이지 않는다** — 무효값이면 배경이 투명으로 떨어지고 테두리·라운드가
남아 "흰 바탕 위 흰 카드"로 멀쩡해 보인다. DOM 에서 `backgroundColor` 를 읽어야 잡힌다.
같은 계열을 세 번 밟았다(폰트 패밀리, 테마 폰트 스택, 면 토큰). `[data-sr-scheme]` 에
선언해야 한다. `guards/surface-tokens.test.ts` 가 감시한다.

### 4.3 테마 토글이 없어서 다크가 도달 불가였다

`providers.tsx` 에 `toggleScheme` 이 처음부터 있었는데 아무도 호출하지 않았다. 다크
팔레트·다크 방향색·다크 면 토큰이 전부 앱에서 볼 수 없는 상태였다. 내비 우측에 붙였다.

---

## 5. 함정 (전부 이번에 실제로 밟음)

- **`pnpm build` 는 돌고 있는 `next dev` 를 깨뜨린다.** 같은 `.next` 를 쓴다.
  이 세션이 :3200 dev 서버를 그렇게 죽였다(`ChunkLoadError: Loading chunk 177 failed`).
  **dev 서버가 떠 있으면 build 를 돌리지 말 것.**
- **백그라운드 자동화 탭은 rAF 를 못 받는다.** CDS 차트는 framer-motion 이라 첫 페인트가
  프레임 구동이다. 그래서:
  - 애니메이션이 중간에 얼어 **레이아웃 버그처럼 보인다**(클립이 186/1024 에서 정지)
  - 애니메이션을 끄면 **자식 0개짜리 SVG** 가 나와 "렌더링 실패"와 구분이 안 된다
  - **rAF 를 기다리면 CDP 가 45초 타임아웃 난다**
  스크린샷이 페인트를 강제하므로, 찍고 나서 다시 읽으면 값이 정상으로 온다. 실제로
  클립이 110.3 → 1024px 로 끝까지 가는 것을 그렇게 확인했다.
- **CDS `Box` 는 `as="span"` 이어도 `display:flex`** — 인라인이 필요하면 명시할 것.
- **CDS `HStack` 은 flex 아이템이라 내용만큼만 넓다** — `<div>`(블록)를 바꾸면 폭이 줄고
  `margin-left:auto` 가 죽는다. `width:100%` 필요.
- **CDS `TableCell` 은 children 에 `null` 을 안 받는다** — `cond ? … : null` 이
  타입 에러다. 빈 `<span />` 을 쓸 것.
- **둥근 컨트롤은 곧은 밑줄을 못 받는다** — `border-radius:999px` 위 `inset 0 -2px` 는
  알약을 따라 휜다.
- **테마 토글과 같은 틱에서 대비를 재면 거짓 실패**가 나온다. `void document.body.offsetHeight`
  로 리플로우 강제 후 잴 것.
- **`data-sr-scheme` 를 손으로 바꿔도 CDS 팔레트는 안 바뀐다.** CDS 는 자기 provider 의
  `activeColorScheme` 로 움직인다. 다크 검증은 실제 토글로 할 것.
- **폰트 적용 여부는 이름 말고 canvas `measureText` 폭으로** 확인 —
  `document.fonts.check()` 는 폴백에도 `true` 를 준다.
- Git Bash 에서 Windows 경로를 `tar` 인자로 주지 말 것(`C:` 를 원격 호스트로 해석).

---

## 6. 미커밋 상태 — 아무것도 커밋 안 됨

```
 M guards/ch-context.test.ts        프로브가 숫자 셀을 재는지 검사 +2
 M src/app/page.tsx                 TopNav 장착·필터 제거·면 적용·표 캡 860
 M src/chart/CandidateB.tsx         (앞 세션 것, 미확인)
 M src/lib/format.ts                unitSuffix 추가
 M src/table/InstrumentTable.tsx    이름 두 줄·52주 4열·프로브 선택자·헤더 700
 M src/table/cells.ts               subText 추가, 앵커 칩 기각 기록
 M src/table/columns.ts             COL_PAD.label 80, WIDEST 낡음 기록
 M src/table/rowHeight.ts           ROW_H 60 + 실측 유도
 M src/theme/direction.css          토스 방향색 + 면 3단 토큰
 M src/theme/sauronTheme.ts         폰트 스택 버그 수정, legal 500
 M src/theme/type.css               내비·메가·면·52주·헤더 규칙
 M src/ui/PreviewPane.tsx           Coinbase 상세 화면으로 재작성
?? DESIGN.md                        측정 근거가 붙은 규칙 문서
?? HANDOFF.md                       이 문서
?? guards/font-face.test.ts         폰트 스택 어긋남 (2회 발생)
?? guards/surface-tokens.test.ts    면 토큰 스코프 (3회 발생)
?? guards/chart-attribution.test.ts
?? src/ui/nav.ts                    섹션/카테고리 모델
?? src/ui/TopNav.tsx                상단 내비 + 메가 패널
?? public/fonts/                    Pretendard 자체호스팅
?? design/                          목업(desk.html) — 순수 HTML, 참조용
?? .mcp.json
```

**테스트 108/108, `pnpm build` 통과. 푸시도 커밋도 오너의 일이다.**

---

## 7. 다음 할 일 — v1 기능 파리티

v1 인벤토리를 받아 대조했다. **"V1 동일 수준" = v1 프론트 약 1만 8천 줄.** 한 턴에
넣으면 지금 세운 것(가드 108건·면 제약·열 폭 모델·방향색 기준)을 못 지킨 채 붙여넣기가
된다. 의존성 순서로 나눈다.

### 레인 1 — 표 완성 ✅ 2026-08-14 완료 (자세한 것은 §8)

- ~~세타 열~~ · ~~커브 배너~~ · ~~호가/보간~~ · ~~hover 프리뷰~~ · ~~포워드 시작점 필터~~

### 레인 2 — 차트 심화 (`PreviewPane` 안에서 끝남)

- ~~**CD 91일 + 기준금리 기준선**~~ ✅ 2026-08-14 (§8.11, §8.12) — **보조 %축 좌측 포함**
- **차트 타입** 선/주봉/월봉 (전역 리더 설정, localStorage)
- ~~**아이들 커브**~~ ✅ 2026-08-14 (§8.13) — 14노드 3M…10Y · 전일 · 노드 리드아웃
- 줌/팬 (CDS `Scrubber` 위에 얹을지 판단 필요)

### 레인 3 — 플로팅 창 기구 ✅ 2026-08-14 (§8.14)

드래그(헤더만)·세션 위치 기억·뷰포트 클램프·Esc 한 겹·하단 탭 서랍. **`src/ui/window/`
여섯 파일**이고 레인 4·5 는 `FloatingWindow` 에 내용만 넣으면 된다.

### 레인 4 — 백테스트 ✅ 2026-08-14 (§8.15)

~~12행 북~~ · ~~진입레벨 미리보기~~ · ~~3분해~~ · ~~자세히(레그별 DV01)~~ ·
~~세션 메모리~~ · ~~링크 복원~~ · ~~일별 대사 ReconStack~~(§8.17 — 시뮬과 공용).

### 레인 5 — 시뮬레이션 ✅ 2026-08-14 (§8.16 · §8.19)

**형태를 v1 실물 기준으로 다시 세웠다** [OWNER 2026-08-14]. 설정 = 전체 페이지
(좌측 카드 열 + 우측 커브 미리보기), 결과 = 떠 있는 창. ~~포지션 패널~~ ·
~~네 케이스~~ · ~~국고 앵커~~ · ~~커브·시계열 미리보기~~ · ~~케이스 비교~~ ·
~~성분 누적 경로~~ · ~~워터폴~~ · ~~일별 대사~~ · ~~기준금리 이벤트~~.
~~경로 설계~~ · ~~다리별 par 덮어쓰기~~ (§8.20). **레인 5 완료.**

### 레인 6 — 나머지

Main 3열 오버뷰(435) · Lab 「라고 할 때 살걸」(109) · 커맨드바(`/`, Cmd+K) ·
하단 스트립(10Y·3s10s·1Yx1Y) · 체인지로그 · 인트로 커튼 · 영역별 에러 바운더리 ·
모션 시스템 + reduced-motion · 억/만 표기(`fmtKrw`) · 틴트 범례 · 표로 보기(21×8 매트릭스)

### 그 밖의 미해결

- **숫자 셀 굵기 500 vs 600** — 토스 재측정에서 근거가 뒤집혔다(§3.2). 지금 `label2` 는
  400(CDS 기본)이고 토스는 같은 자리에 600 을 쓴다. **오너 결정 대기**, 바꾸면
  `sauronTheme.fontWeight` 한 줄.
- **`WIDEST.label` 낡음**(§3.5) — px 쿠션이 가려주고 있다.
- **차트 enter 애니메이션** — `animate={false}` 로 껐다. 포그라운드에서 한 번 볼 것.
- ~~`DESIGN.md` `[미검증·전달]` 4건~~ — **전부 정리됨 2026-08-14.** ΔE 는 직접 계산해
  27.3 을 폐기(적/청 protan 50.9, 그리고 **CDS 녹/적은 protan 16.8 로 사실상 구분
  불가**) · `--faint` 는 2.50 이 아니라 **2.73:1**(결론 동일, 그리고 그건 목업 토큰이지
  v2 것이 아니다 — v2 의 `fgMuted` 는 6.21 / 5.76 로 통과) · 폰트 레지스트리 재확인
  (391개 중 Pretendard 없음) · **목업 대비 200건은 폐기**: 그 목업은 CDS 0% 짜리
  참조물이고 v2 는 그 뒤 CDS 로 다시 세워졌다. 지금 와서 세는 것은 없어진 기준과
  대조하는 일이다.

---

## 8. 레인 1 — 2026-08-14

다섯 항목 전부 들어갔고, **브라우저에서 실제로 확인했다**(prod 빌드 :3200 + 백엔드
:8200, 1920 화면). 검증 중에 나온 것이 기능보다 값나간다.

### 8.1 가장 값나가는 발견 — `ch` 가 표 안에 세 벌이었다

`<colgroup>` 이 열을 px 로 고정한 뒤에도 52주 셀 **안쪽** 하위 격자는 `calc(6ch+24px)`
였고, 그 `ch` 는 격자 컨테이너 자신의 폰트에서 풀린다. 실측:

```
.sr-range      (격자 컨테이너)  16px/600   ch = 10.86   ← 트랙을 정하던 값
.sr-range > *  (실제 숫자)      13px/600   ch =  8.34
td.sr-num span (사다리 프로브)   14px/400   ch =  8.60   ← 사다리가 쓰던 값
```

**트랙이 표 안 어떤 글자에도 속하지 않는 advance 로 그려지고 있었다**(사다리보다 26%
넓게). 남는 `1fr` 필러가 흡수하는 동안은 멀쩡해 보였고, 세타가 다섯 번째 트랙으로
들어오자 셀이 열을 61px 넘겨 표에 가로 스크롤바가 생겼다. → `rangeTemplate(slider,
theta, chPx)` 이 측정값으로 px 트랙을 만든다(DESIGN §5.5).

같은 계열로 둘 더:

- **사다리가 `<table>` 이 아니라 호스트 div 를 재고 있었다** — 호스트 926 / 테이블 909,
  17px 은 스크롤 컨테이너 테두리 + 세로 스크롤바다. 관측을 `<table>` 로 옮겼다.
- **CDS `TableCell` 안쪽 인셋 16px** — 52주 셀만 쿠션이 없어서(고정 하위 트랙의 합)
  이 인셋을 사다리가 반드시 빼야 한다. `CELL_INSET`.

### 8.2 `maxWidth` 는 표 폭을 정하고 있지 않았다

카드 둘이 `width:100%` 로 서 있으면 flex 가 남는 폭을 **반씩** 나눈다. 1920 에서 표는
캡과 무관하게 928px 였고, **860→940 캡 인상은 화면에서 아무 일도 하지 않았다.**
(앞 세션이 760→860 으로 바꾼 게 효과가 있었던 건 그때는 860 < 928 이라 캡이 물렸기
때문이다.) 이제 표 `flexBasis 980`, 미리보기 `flexGrow 1 / minWidth 380` →
실측 표 980 · 미리보기 876 · 가로 넘침 0.

### 8.3 세타는 v1 에서 **포워드 포트**했다

v2 백엔드 복사본(`f5de1fa7`)이 뜬 뒤 v1 이 같은 날 오후에 넣은 기능이라 여기 없었다.
`app/theta.py` 와 `tests/test_theta.py` 는 **바이트 동일 복사**, `payloads.py` 는
v1 의 두 훅을 그대로 적용해 **v1 과 바이트 동일**이 됐다. 자세한 것은 `BACKEND.md`
의 「Forward-ports FROM braveworld」.

**`SCHEMA_VERSION` 7 → 8 은 v2 로컬이고 반드시 필요하다.** 같은 원천 데이터가 다른
페이로드를 낳으므로, 안 올리면 v7 캐시가 세타 없는 요약을 계속 내주고 화면은 아무
에러 없이 em dash 만 그린다. **v1 은 이 범프를 안 했다**(braveworld 는 아직 7) —
v1 의 잠재 결함이고, 이 트리에서 고칠 수 없어 기록만 한다.

### 8.4 앞 인계의 한 줄이 틀렸다 (정정)

> "`rows.ts` 의 `quoted: s.quoted ?? undefined` 가 API 의 `null`(보간)과 필드없음을 뭉갠다"

`??` 는 `false` 를 뭉개지 않는다. 백엔드는 아웃라이트에만 true/false 를 주고
(`derive.py:355`) 그 외 kind 에만 null 을 준다. **구분은 내내 데이터에 있었고 화면만
말을 안 하고 있었다.** 덤으로 포워드의 `live`(`forwards.py:70`)를 실어서 포워드도
고시/보간을 말한다 — 주요 여섯과 독립인 사실이라 구분선이 대신 말해주지 못한다.

### 8.5 가드 하나가 **빈 표를 재고 있었다**

`virtualization.test.tsx` 의 "준 것보다 적게 그린다" 는 0 에도 참이고, jsdom 에서 이
표는 실제로 0행을 그린다(virtual-core 가 `outerSize === 0` 에서 범위를 안 낸다 +
CDS 스타일시트가 없어 스크롤 조상을 못 찾는다). 이 리포에서 제일 비싼 회귀
(43,080 노드)를 막던 가드가 빈 표를 재고 있었다. 인라인 `overflow-y` 래퍼 +
`offsetWidth/Height` 스텁으로 실제로 행이 서게 고쳤고, `rendered > 0` 를 넣었다.
같은 스텁을 `hover-preview` / `theta-column` 가드가 쓴다.

### 8.6 백엔드 테스트 2건 실패 — **사전 존재, 이 세션과 무관**

```
FAILED tests/test_build_static.py::test_the_horizon_stays_well_ahead_of_the_ladder_being_needed
FAILED tests/test_build_static.py::test_the_manifest_publishes_its_coverage
FileNotFoundError: ...\sauron-v2\frontend\public\api\manifest.json
```

정적 굽기 경로가 `frontend/` 를 가정하는데 v2 는 프론트가 리포 루트다(`src/`,
`public/`). **v2 에 `frontend/` 디렉터리는 존재한 적이 없으므로** 이 두 테스트는 복사
이래 한 번도 통과한 적이 없다. 정적 트리를 구울 때 손봐야 한다.

### 8.7 이번에 밟은 함정

- `pnpm build` 는 `next start` 도 깨뜨린다(같은 `.next`). 확인할 때마다
  **빌드 → 리스너 kill → `npx next start --port 3200`** 순서.
- 소스 문자열을 보는 가드는 **주석을 먼저 걷어야 한다.** 규칙을 적어둔 문장에 규칙이
  걸려 두 번 빨갛게 됐다(`SCREENERS`, `toFixed`).
- JSX 에서 `return (` 바로 뒤에 `{/* */}` 를 놓으면 형제 둘이 되어 파서가 죽는다.
- alpha `Select` 의 목록 폭은 컨트롤 폭을 따라간다 — 168 에서 `1Y3M` 이 두 줄로
  접혔다. 200 으로.
- 라벨과 값이 같은 단어를 지면 "시작 1Y 시작" 이 된다. 단위는 라벨이 진다.

### 8.8 미커밋 파일 (레인 1이 추가·수정한 것)

```
 M backend/app/cache.py            SCHEMA_VERSION 8
 M backend/app/payloads.py         세타 배선 (v1 과 바이트 동일)
 M src/app/page.tsx                배너·hover·시작점 필터·flex basis 980
 M src/lib/api.ts                  Theta / ThetaBasis / SeriesSummary.theta
 M src/table/InstrumentTable.tsx   onHover·세타 셀·chPx 서브그리드·table 관측
 M src/table/cells.ts              보간 표기 + 틀린 주석 정정
 M src/table/columns.ts            THETA_*·CELL_INSET·rangeTemplate(chPx)
 M src/table/rows.ts               quoted 3상태·forward live·theta
 M src/table/universeRows.ts       하드코딩 quoted 를 안 옮긴다
 M src/theme/type.css              .sr-range 필러 패딩 0
 M guards/table-contract.test.ts   숨김 수 +1 (세타)
 M guards/virtualization.test.tsx  빈 표를 재던 가드 수리
?? backend/app/theta.py            v1 포워드 포트 (바이트 동일)
?? backend/tests/test_theta.py     v1 포워드 포트 (15 통과)
?? src/lib/krw.ts                  억/만 표기 (v1 krw.ts 부분 포트)
?? src/table/theta.ts              세타 열의 글자
?? src/table/forwardStarts.ts      시작점 목록·해석 (DOM 없이 검증되게)
?? src/ui/CurveBanner.tsx          커브 배너
?? src/ui/StartFilter.tsx          포워드 시작점 Select
?? guards/quoted-state.test.ts · curve-banner · hover-preview · forward-starts · theta-column
```

**프론트 159/159 · `pnpm build` 통과 · 백엔드 355 통과(2 사전 실패). 커밋은 오너의 일.**

### 8.10 오너 피드백 반영 (2026-08-14 오후)

세 건. 앞의 둘은 결정이고 셋째가 작업이다.

1. **숫자 셀 굵기 = 400 확정.** 코드는 이미 그 상태라 변경 0줄. `DESIGN.md §1.1` 의
   제목과 §미해결 항목을 결론으로 바꿨다. **닫힌 결정 — 다시 묻지 말 것.**
2. **세타 정렬 키 없음 확정.** 52주 열과 같은 이유(헤더에 컨트롤 없음).
3. **「세타 열이 가려진다」 — 사다리 우선순위가 원인.** 세타가 마지막 rung 이라
   **제일 먼저** 떨어졌고, 실측 임계는 뷰포트 **1390px** 이었다. 큰 화면에서도 창을
   반만 줄이면 사라진다. 고친 것:

   - 꼬리 순서를 **52주 세 숫자 → 세타 → 위치** 로. 위치가 자리를 내준다(옆 세
     숫자의 그림이라 사실이 행에 남는다). 표시 순서는 그대로.
   - `THETA_PAD` 30 → **20**. 선언 최대값 `−1억 2,345만원` 이 실제 95.2px(+셀 패딩
     12 = 107.2 필요)인데 124.6 을 예약하고 있었다. 그 17px 이 곧 마지막 17px 이다.
   - `withThetaData` **삭제** → 적용 여부가 `visibleColumns(…, hasTheta)` 의 입력이
     됐다. 사후 패스로 두면 위치가 **그리지도 않는 세타의 폭**을 예약한 뒤 떨어진다
     (포워드·민평 탭에서 실제로 그럴 뻔했다).
   - **떨어진 열은 좁혀도 안 돌아온다**(엄격한 prefix). 위치가 더 좁아서 "세타는
     못 들어가는데 위치는 들어가는" 폭이 있지만, 창을 줄이는데 열이 되돌아오면
     고장으로 읽힌다.

   실측(폭 = 뷰포트, 아웃라이트):

   ```
   1440+  위치 + 세타          1366  세타 (위치 1개 숨음)
   1280   52주 세 숫자 (2 숨음)  1100  52주 통째 (3 숨음)
   포워드 1280 → 위치 유지 (세타 없는 탭이 세타 폭을 안 낸다)
   ```

   **아직 1280 에서는 세타가 떨어진다.** 다음 레버는 §8.9 의 라벨 열 정직한 수리다.

### 8.11 오후 2차 — 남은 사항 수행 (2026-08-14)

#### 폭: 라벨 열은 **낭비가 아니었다**(제 앞선 진단 정정)

"스왑 탭이 안 쓰는 47px" 은 **이름만** 센 값이었다. 실측하면 라벨 셀의 최대는
이름이 아니라 **부제**다:

```
탭          이름 max                부제 max                 셀이 필요한 것
아웃라이트   1.5Y        27.7   1년 평균 3.4458% · 보간  133.5   133.5  ← 전체 최대
포워드      9Mx9M       49.7   1년 평균 3.4350% · 보간  133.3   133.3
버터플라이   6M/1.5Y/10Y 83.2   1년 평균 -33.9bp          94.3    94.3
국채선물     10년 국채선물 내재금리 130.9  1년 평균 3.2507%  99.5   130.9
```

157.4px 열에서 셀 인셋 32 를 빼면 125.4 … 가 아니라, 부제는 왼쪽 인셋 16 만 지므로
141.4 가 남고 133.5 가 들어간다. **여유는 8px 뿐이었다.** 즉 `COL_PAD.label = 80` 은
제 몫을 하고 있었고, 다만 `WIDEST.label` 이 **엉뚱한 것을 선언**하고 있을 뿐이다
(이름을 선언하고 실제로는 부제를 재고 있다). 폭 예산은 여기서 안 나온다.

#### 대신 나온 곳 — 날짜 헤더 [OWNER 2026-08-14]

오너 지시("앞에 연도는 없애줘도 될 듯")가 마침 병목이었다. **CDS `TableCell` 은
좌우 16px 씩, 합 32px 을 먹는다**(실측). 그래서 104px 짜리 현재 열이 글자에 주는
폭은 72px 인데 `2026-08-13` 은 13px/600 에서 **78.2px** — 그래서 두 줄로 접히고
헤더 행이 다른 열보다 높아져 있었다. `08-13` 은 ~40px 이라 한 줄로 들어가고, 열이
**104 → 73.6** 으로 줄었다. 연도는 헤더 툴팁과 상단 신선도 칩이 진다.

`RANGE_PAD` 도 실측으로 24 → 20 (`52주 고점` 이 53.5px, 트랙 안쪽 여유 59.6). 합쳐서
**~42px** 이 사다리로 돌아갔고, 그 결과:

```
1440+  위치 + 세타       1366  위치 + 세타      ← 전에는 1366 에서 위치가 떨어졌다
1280   세타 (위치 숨음)   1200  52주 세 숫자     1100  52주 없음
```

**세타가 1280 까지 산다.** 넘침은 전 구간 0.

#### 그리고 관측 대상을 두 번 잘못 잡았다 (실측 기록)

```
host div  926   17px 과대 — 스크롤 컨테이너 테두리 + 세로 스크롤바
<table>   909   넓은 폭에서는 맞고 **좁히면 틀린다**: table-layout:fixed 는 열 합
                밑으로 안 줄어들고 넘쳐버린다 → 관측값이 얼어붙어 사다리가 열을
                안 떨군다(1280 에서 40px, 1100 에서 220px 넘침)
scroller  909   표를 실제로 가두는 상자. 이게 정답.
```

열 폭에 의존하는 값으로 열을 고를 수는 없다. 피드백 루프다.

#### C 항목

- **정적 굽기 2건 → 해결.** `OUT_ROOT` 를 `public/` 로(V2-LOCAL 4/5), 그리고 구운
  트리가 없으면 **스킵**한다(5/5) — 이 둘은 빌더가 아니라 "커밋된 트리가 상해지지
  않았나" 를 보는 테스트라, 트리가 없으면 볼 것도 없다. 60 통과 / 2 스킵.
- **`test_static_agreement` 가 깨어났다.** 포트가 v1 것(:8100)이라 늘 스킵되고
  있었다 — v2 의 정적 트리를 **v1 백엔드**와 대조할 뻔한 배선이다. :8200 으로
  고치니 20건이 실패했고, 원인은 전부 "트리가 없다" 였다. 같은 스킵 규칙 적용.
- **`policy.py::CALENDAR_JSON`** 도 `frontend/src/data` → `src/data` (v2 엔 아직
  그 파일이 없고, 그건 정상이다 — 테스트 보조).
- **차트 enter 애니메이션 → 닫음.** hover 가 결론을 냈다: pane 이 포인터를 따라
  다시 마운트되므로 리빌 애니메이션은 폴리시가 아니라 스트로브가 된다.
- **`[미검증·전달]` 4건 → 전부 정리.** ΔE 는 직접 계산(적/청 protan **50.9**,
  인계값 27.3 폐기 / **CDS 녹적은 protan 16.8** = 사실상 구분 불가) · `--faint`
  는 2.50 이 아니라 **2.73:1**(그리고 그건 목업 토큰, v2 의 `fgMuted` 는 6.21 /
  5.76 로 통과) · 폰트 레지스트리 재확인(391개 중 Pretendard 없음) · 목업 대비
  200건은 **폐기**(CDS 0% 참조물과 대조하는 일).

#### 레인 2 첫 항목 — CD 91일 + 기준금리 기준선 ✅

`src/chart/references.ts` + `PreviewPane`. 규칙은 DESIGN §8.5.

**함정 재확인:** 자동화 탭이 백그라운드면 축 라벨이 안 그려진 것처럼 보인다.
DOM 에는 x 25개 · y 4개가 멀쩡히 있었다. 스크린샷으로 페인트를 강제한 뒤 DOM 을
읽을 것.

### 8.12 보조 %축 — 왼쪽 [OWNER 2026-08-14] · 그리고 제가 틀렸던 것

**"CDS 는 보조축이 없다" 는 제 판단이 틀렸다.** 근거로 삼은 `LineChart.d.ts:71` 의
단수 `yAxis` 는 그 **래퍼**의 편의였다. 한 겹 아래 `CartesianChart` 는 `yAxis` 를
**배열**로 받고 시리즈가 `yAxisId` 로 축을 고른다(`CartesianChart.d.ts:37`,
`Series.yAxisId`). `LineChart` 가 하는 일은 축 설정을 쪼개 `<XAxis>`/`<YAxis>`/
`<Line>` 을 대신 그려주는 것뿐이라, 자식들을 직접 쓰면 잃는 게 없다. **래퍼의 타입을
라이브러리의 한계로 읽지 말 것.**

`PreviewPane` 은 이제 `CartesianChart` 를 직접 부른다. 실측:

```
스프레드   좌 2.5000…2.9000 %   우 0.0…35.0 bp
변동성     좌 2.5000…2.9000 %   우 0.00…3.50 ratio
아웃라이트  좌 (없음)            우 2.5000…4.0000 %    ← 같은 단위는 축 하나
```

**함정 — 축에 이름을 붙이면 시리즈도 그 이름을 가리켜야 한다.** x 축 설정에만
`id: 'date'` 를 주고 시리즈에 `xAxisId` 를 안 달았더니 CDS 가
`xAxisId ?? DEFAULT_AXIS_ID` 로 조회하다 빗나갔고 `Line` 이
`if (!xScale || !yScale || !path) return` 으로 **아무 말 없이 사라졌다**(`Line.js:113`).
축과 눈금은 멀쩡히 그려져서 "선만 없는 차트" 가 됐고 **콘솔에는 한 줄도 안 남는다.**
찾는 데 든 것: `data-sr-probe` 로 시리즈/축 개수를 DOM 에 찍어보고 나서야 데이터가
아니라 조회가 문제라는 게 보였다. x 축은 하나뿐이니 이름을 주지 않는다 —
`guards/reference-lines.test.ts` 가 그 규칙을 지킨다.

### 8.13 아이들 커브 — 2026-08-14

빈 pane 이 이제 **IRS 파 커브**를 그린다(모든 탭에서 같은 커브 — v1 pass M 의 규칙).
규칙과 근거는 DESIGN §8.6. 실측으로 나온 것 둘:

- **`displayTenors` 는 커브가 아니었다.** 8개(6M…10Y)뿐이라 짧은 끝(3M)과 보간
  노드(4Y·6Y…9Y)가 통째로 빠진다. 아웃라이트 전부를 `sortKey` 로 세우고 1D 만 빼니
  **14 노드** — v1 이 "14 equal-spaced nodes" 라 적어둔 그 집합과 정확히 일치했다.
- **같은 축 함정에 새 코드가 다시 걸렸다.** 커브 시리즈에 `yAxisId` 를 안 달아
  선이 또 사라졌다(§8.12 와 같은 원인). 규칙: **축에 이름이 있으면 시리즈가 그
  이름을 말한다.** 이번엔 원인을 아는 상태라 1분 만에 잡혔다.

### 8.14 레인 3 — 떠 있는 창 (2026-08-14)

오너 지시로 레인 2 나머지(차트 타입·줌/팬)를 건너뛰고 바로 왔다.

`src/ui/window/` — `geometry.ts`(클램프·기본 자리·세션 기억) · `useFloatingWindow.ts`
(끌기, 이벤트 시점 스냅샷 + 포인터 캡처) · `escapeStack.ts`(Esc 한 겹) ·
`layers.ts`(z) · `FloatingWindow.tsx`(껍데기) · `WindowDrawer.tsx`(하단 탭 서랍).
규칙과 근거는 DESIGN §8.7. v1 의 `floatingWindow.ts`/`useFloatingWindow.ts` 를 값과
근거째 옮겼다.

**첫 세입자 = 확대 창**(`?w=`). 기구만 만들고 세입자가 없으면 브라우저 검증이
불가능하고, 그러면 레인 4 가 올라앉는 날 처음 시험받는다. pane 을 두 조각으로 나눠
쓴다(본문 = 차트, 서랍 = 통계) — `chartOnly`/`statsOnly` 두 플래그이고, 컴포넌트를
쪼개지 않은 이유는 히어로·구간·기준선·통계가 전부 같은 `view` 하나에서 나오기
때문이다(쪼개면 그 계산이 두 벌이 되고 두 벌은 언젠가 다른 구간을 말한다).

브라우저 실측: 끌기 → 좌하단으로 밀면 `left 0 / top 811 = 855 − 44` 로 클램프,
Esc 로 닫히고 `?w=` 도 사라짐, 다시 열면 **끌어놓은 자리(0, 811)로 복귀**, 서랍은
접힘이 기본이고 열면 통계 세 묶음.

#### 이번에 밟은 것 셋

- **Windows 파일명 충돌.** `floatingWindow.ts` 와 `FloatingWindow.tsx` 는 대소문자만
  다르다 → 대소문자 무시 파일시스템에서 import 가 **기하 모듈로 갔고** "no exported
  member 'FloatingWindow'" 가 났다. 기하를 `geometry.ts` 로 개명. **한 폴더에서
  대소문자만 다른 이름을 쓰지 말 것.**
- **헤더 1px.** `border-bottom` 이 높이 밖에 있어 렌더 45 vs 상수 44 → 아래로 끝까지
  끈 창의 손잡이 마지막 픽셀이 화면 밖. `box-sizing: border-box` 로 고침. (뷰포트가
  클램프 이후에 1px 바뀌는 경우는 남아 있다 — 리사이즈 재클램프는 v1 에도 없다.)
- **색 가드가 그림자를 잡았다.** `box-shadow` 의 `rgb(0 0 0 / 25%)` 도 색 리터럴이라
  `direction.css` 밖에서 금지. CDS 의 `--shadow-elevation2` 로 바꿨다 — 규칙이 제
  일을 한 것이고, 우회가 아니라 토큰이 답이었다.

### 8.15 레인 4 — 백테스트 (2026-08-14)

`src/backtest/` — `book.ts`(순수: 북 모양·방향 이름·URL 왕복·세션 기억) +
`BacktestWindow.tsx`(레인 3 창 안의 화면). 규칙과 근거는 DESIGN §8.8.

**브라우저 실측**(둘 다 라이브 백엔드):

```
10Y 페이 100억 2026-03-09 →  총 +3억 1,114만원
                             평가 +3억 5,355 · 롤다운 −738 · 캐리 −3,503
                             (API 직접 호출값 311,140,166 과 일치)

3s10s 스티프너 250억 2026-01-05 + 10Y 리시브 100억 2026-03-09
                          →  총 −6,128만원
                             평가 −3억 9,971 · 롤다운 +2억 9,148 · 캐리 +4,695
                             다리: 10Y 페이 250억 DV01 2,130만/bp
                                   3Y 리시브 744억 873만 DV01 2,130만/bp  ← 진입 시 중립
```

**링크가 곧 북**: 위 두 줄짜리 `?bt=` 를 붙여넣으면 그 북이 그대로 열린다(실측).

#### 실측으로 걸린 것 — 1만원

세 항목을 각자 `fmtKrw` 로 반올림했더니 **평가 −39,971 + 롤다운 +29,148 + 캐리
+4,696 = −6,127 인데 헤드라인은 −6,128** 이었다. 이 리포가 이미 한 번 출하하고
지운 거짓말(캐리&롤 블록의 −3.2 대 −3.1)과 같은 것이다. v1 의 `splitKrw` 를
포팅해서 **합계·평가·롤다운만 반올림하고 캐리는 그 차로** 낸다 — 지금은 화면에서
정확히 닫힌다(가드가 본다).

레인 1 에서 `krw.ts` 를 옮길 때 "`splitKrw` 는 백테스트가 없으니 안 가져온다" 고
적어뒀는데, 그 함수가 존재하는 이유가 바로 이 화면이었다.

#### 남은 것 — 일별 대사

서버는 이미 보내고 있다(`recon`: 115행 × 테너 15열, 시작 KRD·실제 Δbp·추정·
평가/롤다운/캐리). 서랍 탭이 그 자리에 있고 왜 비었는지 말한다. 표 자체가 자기
규칙을 지고 있어서(팬텀 버킷 이중 계상, carryover 앵커, KRD×Δbp=추정이 행 안에서
닫히는지) 절반만 만드는 것보다 통째로 다음에 하는 편이 낫다.

### 8.16 레인 5 — 시뮬레이션 (2026-08-14)

`src/sim/` — `scenario.ts`(순수: 줄 모양·충격 커브·페이로드) + `api.ts`(세 엔드포인트)
+ `SimulationWindow.tsx`(레인 3 창 안의 화면). 규칙과 근거는 DESIGN §8.9.

**이 레인의 가장 값나가는 것은 기능이 아니라 대사다.** 오너 지시로 v1 을 읽으면서
맞춰봤고, 첫 판이 **세 군데** 어긋나 있었다. 셋 다 예외를 안 던졌다 — 화면은 그려졌고
숫자도 그럴듯했다.

| # | 첫 판 | v1 / 계약 | 왜 조용한가 |
|---|---|---|---|
| ① | 다리를 브라우저가 만들었다(만기·par·픽싱) | `POST /api/instruments/expand` | 아웃라이트 한 다리는 우연히 맞는다. 스프레드의 DV01 중립 가중은 커브가 있어야 나오고 브라우저는 커브가 없다(§16) |
| ② | par 커브를 화면 요약에서 만들어 실었다 | `irsCurves: []` — 백엔드가 그날 스냅샷에서 읽는다 | 시장 데이터 출처가 둘이 된다. 갈린 날에도 숫자는 멀쩡하다 |
| ③ | `shockMode: "parallel"` | 언제나 `"matrix"` | parallel 은 swapCurve 를 **버리고** 전 만기에 같은 bp 를 쓴다 — 오버나이트까지 민다는 뜻이고, 원화에서 그건 금통위가 했다는 주장이다 |

③ 의 실측 차이(3Y 페이 100억 · 90일 · +50bp): **100,058,141 vs 99,869,511 = 18.9만원**.
3Y 한 다리라 이 정도이고, 짧은 다리나 스프레드에서는 답을 바꾼다.

부호도 대사에서 잡혔다. 첫 판은 프론트가 `페이 → −1` 로 뒤집었는데, **뒤집는 곳은
백엔드 한 곳**이다(`instruments.expand`). 실측: `direction:+1` 로 3Y 를 전개하면 다리가
`direction:-1`(고정 지급)로 돌아온다. 줄의 부호는 모니터·백테스트와 같은 "호가값을 롱".

**브라우저 실측**(라이브 백엔드, 180일 · 3Y +30bp · 스프레드 0):

```
3Y 페이 100억         →  총 +1,367만원   평가 +7,143 · 롤다운 −1,304 · 캐리 −4,472
1s2s 스티프너 100억 ×2 →  총 +1억 2,501만원
                          평가 +6,781 · 롤다운 +3,553 · 캐리 +2,167
                          다리: 2Y 페이 100억 3.7275% · 1Y 리시브 196억 1,905만 3.45%
                                (반반이 아니다 — DV01 중립이라 실행 전에 보여준다)
```

#### 실측으로 걸린 것 — 창이 목록을 자르고 있었다 (레인 4에도 있던 결함)

CDS `Select` 의 드롭다운은 포털이 아니라 **창을 기준 삼아** 절대 배치된다
(offsetParent = `.sr-window`, floating-ui). 그 창에 `overflow: hidden` 이 있었다.
결과: **백테스트 종목 99개가 70px 잘려** 마지막 항목들을 고를 방법이 없었다.
잘린 자리가 창 밖이라 화면에서는 "목록이 짧구나" 로 읽힌다.

수리는 창 층에서 한 번 — `.sr-window` 는 안 자르고, 아래 모서리는 `:last-child` 가
지고, 채운 면(서랍 탭 바)은 서랍이 자기 안에서 자른다. 덤으로 드롭다운 폭도
`max-content` 로 바꿨다(`ui/window/popup.ts`): 이 제품의 목록은 토큰이 아니라 문장이라
137px 에서 "아웃라이트" 가 두 줄로 접히고 있었다.

#### 남은 것

- 4케이스 · 경로설계(waypoint) · 커브 프리뷰 · 워터폴.
- **다리별 기여는 표시 정밀도에서 헤드라인과 안 닫힌다**(위 실측에서 1만원).
  **[OWNER 2026-08-14] 그대로 둔다** — 각 줄은 그 다리에 대한 사실이고, 합을 맞추려고
  한 다리의 값을 1만원 비트는 것은 다른 종류의 거짓말이다. 닫혀야 하는 것은 헤드라인
  바로 밑의 3분해이고 그건 닫힌다.

### 8.17 일별 대사 ReconStack — 두 창 공용 (2026-08-14)

`src/ui/window/ReconStack.tsx` + `src/theme/tint.ts`. 규칙과 근거는 DESIGN §8.11.
**백테스트와 시뮬이 같은 컴포넌트**를 쓴다(트레이더 피드백 5 = "둘 다에 존재").
다른 것은 어댑터 둘과 기본 정렬뿐이다.

**브라우저 실측** — 대사 문장이 실제로 닫힌다(시뮬 3Y 페이 100억·180일, 두 번째 블록):

```
KRD 합계 −2,581,613  = 테너 합             ✓
손익 합계   713,195  = 테너 합             ✓
KRD × Δbp ≈ 손익     (−340 × 0.08 → 27 vs 표시 29 — Δbp 표시 반올림)
평가 710,738 + 캐리 −249,315 + 롤다운 −87,370 = 그날 손익 374,053   ✓ 정확
```

백테스트 471행(157일 × 3) · 시뮬 363행(121일 × 3). 범례는 가로 600 / 세로 200 을
굴려도 네 변이 그대로다(실측).

#### 실측으로 걸린 것 — 창이 화면 아래로 자랐다

조각마다 상한을 둬도 합은 안 지켜진다: 머리 44 + 몸통 70vh + 서랍(32 + 38vh) =
**108vh + 76px**. 백테스트에서 대사를 펴자 창이 1,014px 이 됐고 화면은 911 이었다 —
표의 아래 175px 이 화면 밖인데 클램프가 `top ≥ 0` 이라 끌어올릴 수도 없었다. v1 의
서랍 주석이 예고한 그 실패다.

수리는 DESIGN §8.12 — 상한을 **자기 자리에서** 재고(`calc(100vh − top − 24px)`),
닿으면 몸통이 주고, 서랍 → 스택까지 사슬의 모든 고리가 `min-height: 0` 인 열 flex 다.
그 사슬의 고리 하나(ReconStack 의 바깥 `div`)가 블록이라 처음엔 안 먹혔다 — 서랍
219px 안에서 스택이 273px 이었고, 스크롤러가 둘이 되면 스택의 고정 범례가 깨진다.

### 8.18 기준금리 이벤트 (2026-08-14)

[OWNER 2026-08-14 — 세 선택지 중 "v1 그대로: 금통위 이벤트 목록"]

날짜 + 기준금리 변동(bp) + CD 추가(bp), 최대 여섯. 날짜 후보는 **백엔드가 낸다** —
`policy.upcoming` 을 V2-LOCAL 로 신설(`SCHEMA_VERSION` 8 → 9). 브라우저에 회의 일정을
두지 않는 이유는 이 모듈이 이미 겪은 것이다: `MPC_DATES` 자체가 `calendar.json` 의
사본이고 테스트가 둘을 대조하는데, 세 번째 사본은 그 대조에서 빠진 사본이 된다.
`<datalist>` 라 **고르는 것을 돕되 막지는 않는다** — 금통위가 아닌 날의 인하를 그려
보는 것도 정당한 질문이다.

**브라우저 실측**(3Y 페이 100억 · 180일 · 3Y +30bp):

```
이벤트 없음                    →  +1,367만원   (짧은 끝 안 움직임)
08-27 −25bp                    →    +763만원
08-27 −25bp · 10-22 −25bp      →    +160만원
캐리·롤다운은 셋 다 동일 (−1,304 / −4,472)  ← 동결 커브 몫이라 맞다
```

대사표에서 계단이 **정확히 그 날**에 선다: 08-27 의 Δbp 가 1D·3M 둘 다 −25.00 이고
전후 영업일은 0 이다.

#### v1 주석이 반쪽만 맞았다 — CD 추가는 1D 도 민다

v1 은 `generateShockCurves` 의 `cdSpreadBp` 자리에 *"3M 노드에만 더하고 1D 노드는
건드리지 않는다"* 고 적어 뒀다. 그 함수 안에서는 참이다. **화면에 보이는 답은 다르다**:
실측으로 CD +10 을 준 날 대사표의 1D 와 3M 이 **둘 다 −15.00** 이었다. 엔진의 일별
경로는 τ ≤ 0.25 를 `fundingEvents` 의 누적 계단에서 읽고, 그 계단에는 기준금리 변동과
CD 추가가 **합쳐져** 실린다(v1 도 같은 페이로드를 보낸다 — 동작은 같고 주석만 달랐다).
v2 의 주석과 가드는 실측을 적는다.

### 8.19 형태를 v1 실물에서 다시 세움 (2026-08-14)

**오너 지적**: *"내가 분명히 기존 Swap-monitor 보면서 진행하라고 했지."*

저는 v1 의 **소스만** 읽고 그걸 "보면서 진행" 으로 대체했다. 소스는 계약을 알려주지만
**화면이 무엇인지는 알려주지 않는다.** swap-monitor 를 실제로 열어 보니 형태가 통째로
달랐다 — 자세한 대조표는 DESIGN §8.9.

가장 큰 것 하나: **설정과 결과의 자리가 뒤바뀌어 있었다.** v1 은 설정이 전체 페이지
(좌측 카드 열 + 우측 커브 미리보기)이고 결과가 떠 있는 창인데, 제 첫 판은 설정을 창에
넣고 결과를 그 아래 붙였다. 그리고 결과의 주인공(네 케이스 · 워터폴 · 성분 3선 · 커브
미리보기)을 전부 "남은 것" 으로 미뤄 뒀다 — 실물에서는 그게 화면의 절반이다.

**맞았던 것**: 숫자. v1 을 같은 조건으로 돌린 값과 v2 가 **전부 일치**한다(DESIGN §8.9
의 대조표). 페이로드 대사는 옳았고 **제품의 형태만 지어냈다.**

#### 이번에 실측으로 걸린 것 둘

- **`--color-chart1`…`4` 는 없는 토큰이다.** 케이스 선 색으로 썼는데 CDS 가 심는
  `--color-*` 43개에 chart 계열이 없다. 무효값 → 상속색 → **네 선이 전부 같은 회색**.
  이 리포가 폰트·면 토큰에서 세 번 밟은 "아무것도 안 깨져 보이는" 결함의 네 번째다.
- **시리즈 id 충돌.** 기준 커브와 Base 케이스가 둘 다 `base` 였고 CDS 가 하나만
  남겨서, 케이스 선이 조용히 기준 커브를 다시 그렸다(두 `<path>` 의 `d` 가 동일).

#### 파일

`src/sim/` — `scenario.ts`(순수: 케이스·앵커·충격커브·페이로드) · `api.ts` ·
`CurvePreview.tsx`(우측 pane) · `SimulationPage.tsx`(설정 열) · `ResultsWindow.tsx`
(결과 창). 구 `SimulationWindow.tsx` 는 삭제했다.

### 8.20 경로 설계 + 다리별 par 덮어쓰기 (2026-08-14)

v1 실물을 열어 규칙부터 확인했다. 경로 설계는 자유 추가가 아니라 **30일 간격의 고정
격자**(180일이면 D+30·60·90·120·150)이고, D+0 과 마감일은 핀이다. 다리별 par 는 이미
있던 par 표시 자리가 그대로 입력칸이 된다.

#### 가장 값나가는 발견 — 안 손댄 경유지를 실으면 답이 2.9% 달라진다

경로를 격자로 바꾸자 Bull/Bear/Crisis 값이 앞 실행과 미묘하게 달라졌다. 원인을
격리했다(스크래치패드 `path_probe.py`):

```
목표      두 점 직선        정확 격자      0.1bp 반올림 격자
−50    −178,024,749    −178,024,749      −175,347,974   (+1.50%)
+100    178,258,080     178,258,080       173,070,725   (−2.91%)
+250    521,291,310     521,291,310       508,814,693   (−2.39%)
```

**정확 격자는 두 점과 원 단위까지 같다** — 격자 자체는 무해하다. `lerpDefaultBp` 의
0.1bp 반올림만이 답을 바꾸고, 그 크기가 경로 편차(0.03bp)의 **90배**다. 엔진에 "이
경로가 직선인가" 를 1e-9 로 재는 판정이 있어서, 손대지도 않은 반올림 하나가 시나리오를
통째로 다른 갈래로 보낸다.

수리 = **안 손댄 날은 안 싣는다.** 화면은 직선 위 기본값을 보여주되(읽히라고) 페이로드에는
안 넣는다. 직선 위의 점을 빼는 것은 엔진의 선형 보간에서 정확히 등가다.
(v1 은 방문한 케이스만 격자를 쓰고 나머지 셋은 두 점 씨앗을 보낸다 — 그래서 이 함정이
케이스마다 달리 나타났다. v2 는 아예 없앴다.)

**실측 확인**: 아무것도 안 꺾으면 네 케이스가 v1 과 동일(+1,367만 / −1억 7,802만 /
+1억 7,826만 / +5억 2,129만). Base 의 D+30 만 25bp 로 꺾으면 **Base 만** +1,339만으로
바뀌고 나머지 셋은 그대로다 — 경유지가 케이스에 속한다는 것이 눈으로 확인된다.

#### 실측으로 걸린 것 둘 더

- **음수를 칠 수 없었다.** `onChange` 에서 바로 `Number()` 로 파싱해 상태에 넣으면
  `"-"` 는 NaN → 0, `"4."` 는 4 가 되어 되쓰인다. 부호를 갖는 칸(목표·경유지·스프레드·
  기준금리)이 이 화면의 절반이라 사실상 못 쓰는 상태였다. `NumField` 로 blur/Enter
  커밋.
- **CDS `TextCaption` 은 `text-transform: uppercase`** 다. 한글은 안 바뀌어서 오래
  안 보였는데 "금리를 par에서 옮겼어요" 가 "PAR에서" 로, "bp · D+180" 이 "BP · D+180"
  으로 나왔다. 문장·단위는 `TextLegal` 로 바꿨다.

### 8.21 전면 디자인 관찰 (2026-08-14)

2560×1140(오너 실기기 폭)에서 화면별로 실측했다. 규칙은 DESIGN §8.13~8.15.

**고친 것**

| | 전 | 후 |
|---|---|---|
| 서체 | 내비·대사표·날짜칸이 **Noto Sans KR**, CDS 만 Pretendard | 전부 Pretendard |
| Main/Backtest 세로 | 아래 **428px 빈 채** 표가 560 안에서 스크롤 | 표 949 · 차트 807, 남는 건 23~87 |
| 시뮬 미리보기 | 차트 420 고정, 카드 절반이 빈 칸 | 959 |
| 시뮬 설정 열 | 360px 고정(2560 의 14%) | `clamp(360, 24vw, 480)` |
| Main | Backtest 아웃라이트와 **같은 화면** | 3열 오버뷰 |
| 컨트롤 | deprecated `compact` | `size="s"` 명시 |

**실측으로 걸린 함정 셋**

1. **`minHeight: 100vh` 는 되먹임을 만든다.** flex 가 나눌 정해진 높이가 없으니,
   재서 내려준 높이가 내용을 키우고 그 내용이 더 큰 측정값을 만든다 — 오버뷰 카드가
   1,214 화면에서 **2,106px** 까지 자랐다. `height: 100vh` 로 고리가 끊긴다.
2. **`useRef` + `useLayoutEffect([ref])` 는 한 번도 안 쟀다.** 페이지가 데이터보다
   먼저 마운트되므로 그때 카드가 없고(`ref.current` null), 카드가 나중에 생겨도
   의존성이 안 바뀌어 효과가 다시 안 돈다. 콜백 ref 로 바꿨다.
3. **`ResizeObserver` 가 자동화 탭에서 한 번도 안 온다.** 관찰자 전달이 렌더
   생애주기에 묶여 있어서다 — rAF 에서 겪은 것과 같은 자리. 붙는 즉시 동기로 한 번
   재는 것으로 보완했다.

**확인했는데 문제 없던 것**: 다크 모드(활성 알약 = 흰 배경 + 어두운 글자) · 가로
폭(카드가 2560 을 꽉 채우고 여백 24) · 타입 스케일(12/13/14/16/20 × 400/500/600,
화면당 7종) · 컨트롤 정렬(Select 와 TextInput 의 보이는 상자 둘 다 39px).

**오너 결정** [2026-08-14]: 세로는 창에 맞춰 늘림 · Main 은 3열 오버뷰 · 설정 열은
넓은 화면에서 420~480 · 설정 패널 본문은 **13/500 유지**(표 화면의 14/400 과 다른
레지스터인 것은 의도).

### 8.22 상단 내비 정렬 · 메가 패널 (2026-08-14)

오너 지적 셋: 내비 글자 정렬 · 화면 모드 토글 · **"커서 대면 나오는 패널은 다
어디갔어"**. 지시대로 coinbase.com 과 tossinvest.com 을 다시 재고 왔다.

| | 전 | 후 | 근거 |
|---|---|---|---|
| 내비 글자 | 상자 중심 32인데 **글자 중심 24**(8px 위) | 32 == 32 | Coinbase 34==34, 토스 26==26 |
| 화면 모드 | 글리프 문자 `☾` 를 라벨 알약에 | CDS `sun`/`moon` 아이콘 버튼 36×36 | 토스 32×32 radius 8 |
| 메가 패널 | 덮개 없음 → 표 위에 뜬 낱글자 | `--sr-scrim` 으로 바 아래 전체를 덮음 | Coinbase `rgba(50,53,61,0.33)` |
| 패널 안쪽 | 2,560 패널에 안쪽 **631px** | `width: 100%` | 설명 카드가 오른쪽 끝으로 |

**패널은 없어진 게 아니었다** — 코드도 hover 도 살아 있었고, 덮개가 없어서 "패널로
안 보였을" 뿐이다. 실측으로 그걸 확인하고 나서야 원인이 색이 아니라 **덮개의 부재**
라는 것이 나왔다.

**안 바꾼 것**: 내비 굵기. 처음 한 개만 재고 "Coinbase 는 400" 이라 읽었는데 그건
텍스트 노드가 아니라 래퍼 `<a>` 였다. 여덟 개를 전수로 재니 **전부 16px/600** 이고
v2 와 같다. n=1 로 규칙을 바꾸지 않는다.

**밟은 함정**: 색 가드가 스크림을 잡았다(색 리터럴은 `direction.css` 에만) — 옳은
지적이라 `--sr-scrim` 토큰으로 옮겼다. 그리고 `zoom` 액션의 좌표는 CSS 픽셀이 아니라
**스크린샷 좌표**다(2560 화면에서 1568 로 축소되므로 0.6125 를 곱해야 한다).

### 8.9 레인 1 이후 남은 것

- **`WIDEST.label` 이 엉뚱한 것을 선언한다 (§5.2, §8.11).** 폭 낭비는 없었다 —
  여유는 8px 뿐이다. 문제는 상수가 **이름**의 최대를 선언하는데 열을 실제로 정하는
  것은 **부제**(133.5px)라는 점이다. 수리 = 라벨 셀의 px 실측 최대를 선언하는 것.
  폭 예산이 아니라 정직성 항목으로 남는다.
- **`Row.theta` 를 민평·선물에도?** 지금은 스왑만이다. 국채선물 캐리는 다른 계산이라
  백엔드 결정이 먼저다.
- 2560 화면(오너 실기기)에서 미리보기가 1500+ 를 받을 때의 그림.

---

## 9. 읽을 것

- **`DESIGN.md`** — 규칙과 그 근거. 모든 수치에 `[검증됨]` / `[미검증·전달]` 이 붙어 있다.
  **출처 표시를 지우지 말 것** — 앞 세션이 외부 실측을 검증 없이 물려받아 세 군데가
  틀렸던 적이 있다.
- `design/mockup/desk.html` — 2026-08-12 실데이터 목업. 순수 HTML(CDS 0%)이라 **그대로
  머지하면 안 된다.** 참조용.
- v1: `braveworld/docs/DESIGN.md`, `braveworld/docs/HANDOFF.md`, `braveworld/frontend/src/ui/tabs.ts`
