# 인계 — sauron-v2, 2026-08-19 (오너 지시 패스 4차, **완료**)

앞 인계는 `HANDOFF-2026-08-18c.md`. 이 문서는 **오너가 화면을 보고 던진 지시들**을
받아 처리한 세션의 인계다. 감사·정렬 같은 다른 레인이 같은 트리에서 돌고 있으므로,
**이 세션이 박아 둔 규칙과 충돌 가능성**을 §5 에 따로 모았다 — 거기부터 읽는 것이
가장 빠르다.

## 0. 지금 상태

| | |
|---|---|
| 게이트 | `npx vitest run` **575/575 그린**, `npx tsc --noEmit` 0, `npx eslint src guards` 0 error(기존 warning 4) |
| 커밋 | **전부 커밋됨** — 이 세션의 작업은 `49e370d`(RV·백테스트·현금채권·시뮬) / `6374aa2`(공용층·테마·CSS) / `a484613`(가드) 안에 있다. HEAD = `fa427e8` |
| 미커밋 잔여 | `data/irsdata.xlsx`(다른 세션의 시뮬 P0 워크북 교체), `.impeccable/live/`(도구 산출물 — gitignore 권장). **둘 다 이 세션 것이 아니다** |
| 확인 방법 | 전부 실행 중인 앱(:3200/:8200)에서 CDP 실측·스크린샷으로 검증했다. 라이트·다크 양쪽 |

---

## 1. 오너 지시와 그 귀결 (시간순)

### 1차 — 7건 일괄
1. **"호가" 표기 제거** — `table/cells.ts::subText` 에서 `· 호가` 은퇴. **`· 보간`은
   남는다**(예외만 표시한다는 규칙으로 뒤집은 것). `guards/quoted-state.test.ts` 는
   "호가를 찍지 않는다"로 기대가 반전됐다.
2. **백테스트 자산군 축소** — 국고(`govt`)·본드스왑(`bss`)·크레딧(`credit`)·
   국채선물(`futures`)이 `nav.ts::BACKTEST_CATEGORIES` 와 `page.tsx::GROUPS` 에서
   내려갔다. `GROUPS` 가 곧 **URL 게이트**라 딥링크(`?g=credit`)도 이제 안 열리고
   `DEFAULT_GROUP` 으로 떨어진다. `rows` 메모에서 `toRows(universe)` 도 끊었다
   (탭 없는 행이 커맨드바로 점프하는 유령 방지). **백엔드 `universe.py` 와
   `table/universeRows.ts` 는 그대로 살아 있다** — 되살리려면 그 두 배열 + `toRows`
   재연결이 전부다. `universe.absent` 각주 렌더도 함께 은퇴(내려간 자산군 얘기라).
   **현금채권·자산스왑은 유지**(오너 확인 — 민평이 SQL 라이브라서).
3. **컨트롤 값 13px 통일** → §5.1 로.
4. **RV 열머리 형태 원복** — 점선 밑줄·`cursor:help` 걷음. 설명은 CDS Tooltip 유지.
5. **사분면 어휘** — "싸고 버팀/비싸지만 방어적" 계열 → **"평소보다 넓음/좁음 ·
   여유 있음/부족"**(오너가 선택지에서 고름). aria-label·각주·`nav.ts` blurb 동반.
6. **이력 창 정돈** — 문장 나열 → 머리 Stat 칸 행(지금·버퍼·BEP·상대 RV·캐리+롤·
   Coverage 백분위) + 차트 2 + 바닥 성분 한 줄.
7. 폰트·얼라인 → §5.1 / §5.2.

### 2차 — "모든 창의 규칙으로 박아라"
§5.1 참조. 가드로 강제했다.

### 3차 — 어휘·호버 패널
- **승리 구간 → 1등 구간**, 그리고 오너 요청으로 **격자에 테두리 띠**를 추가
  (`sr-rv-win` / `-start` / `-end`). `winFrom..winTo` 에 드는 칸이 테두리로 묶인다.
- **커브 레인 → 상세 분석**, **스왑점 → 교체점**, **동일섹터/동일테너 → 같은
  섹터끼리/같은 만기끼리**.
- **껍질 ◆ 마커 은퇴** — 문장을 쉽게 고쳐 제안했으나 오너가 "없애도 될 듯". 서버
  계약 `inHull` 과 fixture 는 잔존, 표시만 내려갔다. `.sr-rv-hull` CSS 삭제.
- 호버 패널 둘 수리 → §5.3.

### 4차 — 잔여 3종
- 대사 표 고정 열 가림 표식 → §5.4.
- 다크 실측 + **테마 영속화**(보류 해제) → §5.5.
- 타이포 shorthand **래칫 가드** → §5.6.
- **조건 바 순서**(오너 선택) — 한 줄·다섯 상시 그대로, 순서만
  `민평 → IRS → 후보 → H → 조달`(언제 → 무엇으로).

---

## 2. 이 세션이 실측으로 밝힌 함정 (다른 레인이 다시 밟기 쉬움)

1. **CDS `Select` 의 `styles.controlValueNode` 는 값 폰트를 못 바꾼다.** 그 인라인
   스타일은 감싸는 DIV 에 앉고, 값은 안쪽 `<p>` 자신의 body 아토믹 클래스로 그려진다
   (실측: 16px 그대로). **`font="legal"` prop 이 정답.** `TextInput` 은 `fontSize="legal"`.
2. **CDS 컨트롤의 크롬(좌우 패딩+셰브론)은 82px 이다** — Box 248 안에서 텍스트
   가용폭 166px 실측. 칸 폭 산술은 `텍스트폭 + 82`.
3. **CDS `Tooltip` 은 앵커를 flex DIV 로 감싸고 그 DIV 가 부모 폭을 채운다.** flex
   안에서는 `text-align` 이 죽어 라벨이 왼쪽에 앉는다 — 랭킹 표 열머리 셋이 자기
   숫자와 어긋나 있던 원인. 해시 클래스라 못 짚으므로 `.sr-rv-th > div` 로 잡았다.
4. **`Tooltip` 은 합성 이벤트나 `el.focus()` 로 안 열린다.** 실제 키 입력(CDP Tab)
   으로만 재현된다 — 자동화로 검증할 때 이걸 모르면 "툴팁이 없다"고 오진한다.
5. **툴팁 콘텐츠는 조상의 `white-space: nowrap` 을 상속한다.** 표 열머리(nowrap)
   안에서 열리면 패널이 한 줄 높이로 서고 문장이 **패널 밖으로 흘러나간다**.
6. **그림자 색을 `var(--color-fg)` 로 조립하면 다크에서 흰 글로우로 뒤집힌다.**
   그림자는 방향이 있는 양이라 테마마다 값이 달라야 한다 → `--sr-edge-shadow`.
7. **`data/irsdata.xlsx` 를 갈아끼우면 데이터 핀이 깨진다**(앞 세션 기록). 이 세션은
   안 건드렸다.
8. **`localStorage['sr-funding']` 이 `base`(기준금리)면 Strategy 전체가 422 로 죽는다**
   — SQL `infomax.기준금리` 가 2026-03-21 에 멈춰 있다(영업일 108일). 브라우저에
   그 값이 남아 있으면 코드와 무관하게 화면이 실패한다. §6 참조.

---

## 3. 새 가드 (둘 다 소스 스캔)

| 파일 | 무엇을 막나 |
|---|---|
| `guards/control-value-font.test.ts` | 앱의 모든 `Select` 가 `font="legal"`, 모든 `TextInput` 이 `fontSize="legal"` 을 지는지. 하나라도 빠지면 파일명과 함께 실패. `.sr-date` 의 13px 도 같이 핀 |
| `guards/typography-ratchet.test.ts` | deprecated 타이포 shorthand(8종) 여는 태그 수의 **천장 251**. 늘면 실패, 20개 이상 줄면 상수를 내려 적으라고 실패 |

`guards/rv-analysis.test.tsx` 는 어휘 교체·◆ 은퇴·1등 구간 띠를 반영해 갱신됐다.

---

## 4. 화면에서 바뀐 것 (스크린샷 대신 문장으로)

- 표 서브라인: `1년 평균 3.0801%` (호가 표기 없음), 보간 노드만 `· 보간`
- Backtest 메가 패널: **스왑 / 현금채권** 둘만
- 백테스트 창: 폭 **1020**, 종목 160 · 방향 264 · 규모 88 · 날짜 128×2 · 레벨 96,
  값 글자 13px, 방향 문장 말줄임 0, 진입 레벨·✕ 접힘 0
- Strategy 조건 바: `민평 … IRS … 후보 … H … 조달` + 우측 끝 `설정`
- 랭킹 열머리: 밑줄 없음, 숫자와 오른쪽 끝 정렬
- 사분면 네 구역 라벨: 평소보다 넓음/좁음 × 여유 있음/부족
- 사분면 커서 패널: 폭 200, 8줄(z 는 세 줄로 분해)
- 상세 분석 창: 만기 옆 ◆ 없음, 1등 구간 칸에 테두리 띠, 각주가 그 띠를 설명
- 일별 대사: 좌(구분)·우(합계) 안쪽 경계에 헤어라인 + 그림자
- 다크 유지: 새로고침해도 고른 밝기가 남는다

---

## 5. ⚠ 다른 레인과 부딪칠 수 있는 것들

### 5.1 컨트롤 값 13px — **앱 전역 규칙, 가드로 강제**
`Select`→`font="legal"`, `TextInput`→`fontSize="legal"`. 적용처: 백테스트·현금채권
창, 시뮬레이션(종류/상품/명목/금리), RV(금통위 입력·섹터), Setting(조달), 포워드
시작점, 종목군 필터. **정렬/간격 레인이 이 prop 을 걷어내면 `control-value-font`
가드가 죽는다 — 그건 의도된 실패다.** 근거는 `ui/window/popup.ts` 주석.

### 5.2 백테스트 창 폭 1020 과 칸 폭
`FloatingWindow width={1020}`. 기본 980 으로 되돌리면 진입 레벨과 ✕가 둘째 줄로
접힌다(오너가 두 번 지적한 결함). 칸 폭 산술은 §2-2.

### 5.3 RV 호버 패널 둘
- 열머리 툴팁: `.sr-rv-tiptext`(`white-space: normal` + **`word-break: keep-all`**).
  keep-all 이 한국어를 단어 사이에서만 접는다 — 오너가 지적한 "단어 단위로 잘림"이
  기본 CJK 규칙이었다. 문구도 두 문장으로 줄였다.
- 사분면 리드아웃: `ReadoutCard` 에 **`width` prop 신설**(기본 148 유지),
  `RvScatter` 만 `RV_CARD_W = 200`. **left 클램프도 같은 상수를 쓴다** — 폭만
  키우면 카드가 그림 밖으로 나간다.

### 5.4 대사 표 고정 열 경계 — **border 금지**
`.sr-recon-edge-l` / `-r` 는 반드시 `box-shadow` 다. 이 표는 `table-fixed` 에
`ch` 로 sticky 오프셋을 적어 두었고, **트랙이 1px 만 밀려도 고정 열 사이로 밑이
샌다**(모듈 주석의 v1 실측). 간격 정규화 레인이 여기에 padding/border 를 넣으면
그 결함이 되살아난다.

### 5.5 테마
`providers.tsx` 가 `localStorage['sr-scheme']` 를 쓴다. **첫 렌더는 항상 light
고정** — 초기값을 저장소에서 읽으면 CDS `ThemeProvider` 가 인라인으로 뿌리는 팔레트
변수까지 hydration 이 갈라진다. 다크 사용자에게 한 프레임 밝은 화면이 스치는 것은
알고 남긴 비용이고, 없애려면 페인트 전 스크립트가 필요하다.
새 토큰 `--sr-edge-shadow`(라이트 검정 20% / 다크 55%)가 `direction.css` 에 있다.

### 5.6 타이포 shorthand
일괄 교체는 **여전히 CDS 메이저 승급 전 별도 레인**(CLAUDE.md). 지금은 래칫만
걸려 있다. 새 코드에서 `TextCaption`/`TextLegal` 등을 쓰면 가드가 막는다 —
`Text font="caption|legal|label1|label2|body"` 를 쓸 것.

---

## 6. 오너 결정 대기 / 다음 후보

1. **Setting 의 "기준금리" 선택지** — 고르면 Strategy 가 항상 422 로 죽는다(§2-8).
   에러 문구는 친절하나 화면이 반드시 실패하는 선택지를 계속 내준다. 선택지:
   (a) 비활성 + 이유 표기, (b) 고를 수는 있게 두되 경고 배지, (c) 현행 유지.
2. **백테스트 종목 목록의 1D·3M** — 콜·CD 가 스왑처럼 목록에 선다. 엔진이 실제로
   가격하는지 미확인. 안 한다면 "서버가 거부하는 것을 화면이 제안하지 않는다"는
   이 리포의 규칙 위반이다. **확인 필요.**
3. **남은 은어 사전** — Δy, H, Coverage, Score, BEP 등 축 라벨·조건 바 용어. 오늘
   방식(선택지 → 컨펌 → 일괄)이 잘 통했다.
4. `.impeccable/live/` gitignore 추가.

---

## 7. 파일 지도 (이 세션이 만진 것)

```
src/table/cells.ts              호가 은퇴
src/ui/nav.ts                   카테고리 축소 · blurb 어휘
src/app/page.tsx                GROUPS 축소 · toRows 절단 · absent 각주 은퇴
src/app/providers.tsx           테마 localStorage
src/theme/direction.css         --sr-edge-shadow (라이트/다크 쌍)
src/theme/type.css              .sr-rv-th>div · .sr-rv-tiptext · .sr-rv-win* ·
                                .sr-recon-edge-* · .sr-rv-hull 삭제
src/ui/window/popup.ts          13px 규칙의 근거 주석 (DROPDOWN_STYLES)
src/ui/window/ReconStack.tsx    고정 열 경계 클래스
src/ui/ReadoutCard.tsx          width prop
src/ui/{BondTypeFilter,StartFilter,SettingView}.tsx   font/fontSize legal
src/backtest/BacktestWindow.tsx 창 1020 · 칸 폭 · font legal
src/cashbond/CashBondWindow.tsx font/fontSize legal
src/sim/SimulationPage.tsx      font/fontSize legal
src/rv/RankingTable.tsx         툴팁 문구 · .sr-rv-tiptext
src/rv/RvScatter.tsx            사분면 어휘 · RV_CARD_W 200 · z 세 줄
src/rv/RvPage.tsx               조건 바 순서 · 상세 분석 · Stat 칸 · 어휘
src/rv/SectorLane.tsx           1등 구간 + 띠 · 교체점 · ◆ 은퇴
guards/quoted-state.test.ts     기대 반전
guards/rv-analysis.test.tsx     어휘 · ◆ 은퇴 · 띠
guards/control-value-font.test.ts   (신규)
guards/typography-ratchet.test.ts   (신규)
```
