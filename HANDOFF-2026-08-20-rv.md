# 인계 — sauron-v2 RV Strategy (트레이더 워크북 정렬, **완료·미푸시**)

*2026-08-20 착수 · **2026-08-21 갱신**(§8 판단거리 넷 결론, 커밋 셋 추가).*

앞 인계는 `HANDOFF-2026-08-19b.md`. 이 레인은 그것과 겹치지 않는다 — RV(Strategy)
탭 하나와 Setting 탭 문구 한 줄이 전부다.

## 0. 지금 상태

| | |
|---|---|
| 커밋 | 08-20 `fcbacf4` · `2d576ab` · `98cac29` · `1523232`(이 문서) — 08-21 `0717f95` · `3748c1b` · `804cb39` · `9690540`. 시작 HEAD `457f414`. **전부 미푸시** |
| 게이트 | 백엔드 `pytest tests/ --ignore=test_static_agreement.py --ignore=test_issuance.py` **671 그린**(654 였고 +17 은 동시 세션 것) · RV 만 **76 그린** · 프런트 `vitest` **904/906**(빨강 2 = 아래 `production-env`) · `tsc` 클린 |
| 알려진 빨강 | `production-env` 2건 — **"승인된 빨강"이 아니다, 아래 정정** · `test_issuance` 2건(**동시 세션 것**) · `test_static_agreement`(라이브 :8200 감지해 스스로 거절 — 설계된 안전장치) |

> **[정정 2026-08-21] `production-env` 2건의 정체.** 앞 판(과 이 문서 08-20 판)은
> 이 둘을 "BOM·오너 승인"으로 적었는데, 실제로 확인하니 **지금 `.next` 가 dev
> 빌드**여서다(`BUILD_ID` 가 없다 — `next dev` 산출물). dev 청크에는
> `localhost:8200`·`127.0.0.1:8200` 이 **정당하게** 들어 있고, 가드는 그걸 읽고
> 빨강을 낸다.
>
> **따라서 이 둘은 푸시 전에 무시해도 되는 빨강이 아니다.** `pnpm build`(프로덕션)
> 뒤에는 **반드시 초록**이어야 한다. 거기서 빨강이면 그게 **라이브 404 를
> 만들었던 그 버그**다(`3d4855f` — 보이지 않는 BOM 한 글자가 절대 URL 을
> 상대경로로 만들었다). 그래서 배포 게이트는 **순서가 `build` → `vitest`** 다.
| 🔴 미결 | **라이브(rateslab.vercel.app) RV 화면이 깨져 있다** — §5 |

---

## 1. 근거 문서 — 이게 이 레인의 전부다

`Downloads\Telegram Desktop\크레딧 채권 상대우위 비교.xlsx`
(시트 셋: 만기선택 · 금통위 · 노트)

**이 워크북이 rv1 Appendix A/B 의 원본이다.** `backend/tests/test_rv.py` 의
`SHEET`/`EXPECT` 8행이 그 워크북의 특은채 커브와 H/L/M/S열 그대로다. 즉 v2 RV 는
처음부터 이 파일에서 나왔고, 이번 레인은 **그 뒤로 어긋나 있던 자리를 되맞춘 것**이다.

[OWNER 2026-08-20] **"엑셀에서 밝힌 트레이더의 논리가 우선"** — 판단이 갈리면
워크북 쪽이다. 단, 워크북이 명백히 틀린 자리는 §4 에 적었다.

---

## 2. 무엇이 바뀌었나

### 2.1 `fcbacf4` — 숫자가 움직인 것

| 바뀐 것 | 전 → 후 |
|---|---|
| **BEP 분모** | 수치 DV01 → 워크북 K열 **par 폐형** + **달력 잔존만기**(0.25 플로어 없음). 9M 166.3 → **169.0bp** (워크북 169.005) |
| **크레딧 RV 축** | 스프레드축(국고 헤지 페어, 조달 소거) → **아웃라이트**. 조달이 캐리에 남는다 |
| **스프레드 앵커** | 전 섹터 국고 대비 → **`SPREAD_BASE` 로 갈림** (아래) |
| **만기 상한** | 랭킹만 10Y → **RV 섹션 전체 3Y**. 후보 94 → 64 |
| **H** | 레인 A 6M / 크레딧 3M 두 벌 → **한 벌 6M**(화면 컨트롤 3/6/12) |
| **사분면 축** | x=상대 RV(σ)·y=Coverage(σ) → **x=월환산 총수익(bp/월) · y=지난주 스프레드 백분위(%)** |

```
SPREAD_BASE
  앞단  통안 · 특은(산금) · 공사      → 국고 대비
  확산  은행 · 회사 · 카드 · 캐피탈   → 특은채 대비   ← 트레이더 피드백
```

지적의 형태: *"특은채와 카드채 스플이 둘 다 최근 레인지의 75~80% 면 항상 일드
높은 카드가 유리하다는 결론이 나온다."* 앵커를 옮기면 "특은 대비 붙었나
벌어졌나"가 들어온다. 실측(같은 날):

    Score ↔ 신용위험 프리미엄(국고 대비)   +0.79 → +0.60
    최광폭 섹터(캐피탈) 상위 10 점유        3개 → 1개
    상위 10 을 차지한 섹터 수               3개 → 7개

**딸려 온 것**: 횡단면(섹터 상대 z)의 모집단도 같은 앵커끼리로 갈랐다(`peers_of`).
국고 대비 33bp 와 특은 대비 6bp 를 한 평균에 넣으면 그 평균은 아무 양도 아니다.

**새 기능**: 재투자 3갈래(워크북 B11) · 비평행 커스텀 커브 2벌(케이스 C/C-2,
레인 A 에 경로 열) · H 컨트롤.

**은퇴**: `coverage` · `bepSpreadBp` · `vol3mBp`. `covPct` 는 **이름만 바뀌어
살아 있다**(`spreadVolPct`) — Score 절대축 입력이고 이력창에만 보인다.

### 2.2 `2d576ab` — 설정 패널 정돈 (동작 변화 없음)

컨트롤 시작점 53·58·73·92px 계단 → 전부 한 열(`SET_LABEL_W=88`). 5줄 평면 →
3그룹(보는 방식 / 계산 조건 / 시나리오) + Divider. 테너 캡션 2벌 → 1벌.
설명 문구 두 자리 → 레이블 열만큼 들여쓴 아래 줄 하나. `SetGroup`·`SetRow` 헬퍼.

### 2.3 `98cac29` — 감사 수정

- **★ 서버가 안 막던 입력 둘**: 금통위 ±9999bp · 경로 Δ ±9999bp 가 200 으로
  통과했다(조달 102% 짜리 화면이 아무 말 없이 그려졌다). `MPC_LIMIT_BP=100` ·
  `PATH_LIMIT_BP=200` 으로 **거절**한다(자르지 않는다).
- **★ Setting 탭이 화면에서 거짓말**: "Cash Bond 백테스트**에만** 쓰여요" →
  RV 도 쓴다. `"Cash Bond 백테스트와 Strategy가 같이 써요. IRS 백테스트는 안
  써요."` 로 고쳤다.
- `parse_meetings` 가 콜론 없는 입력에서 파이썬 에러를 뱉던 것 → 우리 말로 거절.
- 회의 당일 규약을 문서·테스트로 못박음(§4.1).

---

## 3. 되돌린 것 — 같은 실수를 반복하지 않기 위해

**Score 의 절대축 입력을 사분면 y축으로 바꿨다가 되돌렸다.** 지시는 사분면 축
이었고 점수 입력은 범위 밖이었다. 그때 "회귀"라고 보고한 근거(+0.255 → +0.790)는
**유니버스가 달랐던 교란**이었다(옛 판 10Y / 새 판 3Y). 같은 항목 집합에서 다시
재니 세 입력이 사실상 같다(~+0.79). 그 A/B 표는 `spread_vol_hist_series`
독스트링에 있다.

교훈: **비교하기 전에 두 세계의 유니버스가 같은지 먼저 맞출 것.**

---

## 4. 건드리면 안 되는 자리 셋

### 4.1 회의 당일 규약 — 워크북과 다른 것이 맞다

```
여기      start <  md <= sale
워크북    start <= md <  sale
```

워크북 B6 은 사람이 손으로 적는 기준금리라 회의 당일 아침엔 옛 값이고, 그래서
그날 회의를 세야 맞다. 여기 base 는 `funding.rate_on` 의 피드이고 **그 피드는
회의 당일에 이미 새 값을 싣는다**(2026-07-16 실측: 전일 2.60% → 당일 2.85%).
같은 날 회의를 또 더하면 **25bp 를 두 번 센다.**

`TestMeetingDayBoundary` 가 지킨다. 같은 규약이 `path_rate` 와 페이로드의
`meetings`(`d > start`)에도 있고 **셋은 같이 움직여야 한다.**

### 4.2 워크북 O열(재투자 조달)의 결함은 이식하지 않았다

워크북은 재투자 구간 조달의 base 를 **오늘 기준금리**로 놓아 매수 만기일까지
이미 지나간 인상을 빼먹는다(앵커 조건에서 **6.3bp**). `path_rate` 가 만기일
시점의 경로 레벨을 제대로 집는다. `test_the_workbook_defect_is_not_ported` 가
그 자리를 지킨다.

### 4.3 롤은 공용 가격 커널을 쓴다 — 워크북과 9M 에서 0.37bp 남는다

워크북의 닫힌 식을 여기 다시 적으면 이 리포의 가격 정의가 둘이 된다
(`cashbond.dv01_at` 이 해석해를 마다한 그 이유). 남는 차이는 앵커 허용오차가
잰다(carry ≤0.02 · roll ≤0.4 · BEP ≤1.6bp).

---

## 5. 🔴 미결 — 라이브가 깨져 있다

:8200 백엔드를 **세 번 재기동**해서 새 페이로드를 서빙 중인데, Vercel 의
`rateslab.vercel.app` 프런트는 **옛 필드**(`coverage`·`bepSpreadBp`·`covPct`)를
읽는 옛 번들이다. 그래서 라이브 RV 화면이 안 뜬다.

[OWNER 2026-08-20 결정] **동시 세션의 발행 캘린더 작업이 끝나면 프런트를 같이
배포**한다. 그때까지 깨진 채 둔다. 배포는 **프런트 재배포 한 번**이면 된다.

**2026-08-21 현재**: `origin/main` 이 아직 `3809d57` 이라 미푸시가 **열 건 넘게
쌓였고**(`git log --oneline origin/main..HEAD` 로 세라 — 숫자를 여기 박으면
바로 낡는다) 그중 절반이 동시 세션 레인(발행 캘린더·Lab 시나리오·모형 탭)이다. 프런트
재배포 = `main` 푸시이므로, 지금 밀면 그쪽 미완성분(`test_issuance` 2건 빨강
포함)이 같이 나간다. 그쪽 워킹트리도 열려 있다. **여전히 기다리는 것이 맞다.**
굳이 갈라야 하면 RV 커밋만 뗀 브랜치를 배포하는 길이 있으나 오너 결정이다.

---

## 6. 동시 세션 (내 것 아님)

다른 세션이 같은 트리에서 **발행 캘린더 · Lab 시나리오**를 만지고 있다
(커밋 `62ab4d6` 이 그쪽 것이다). 부딪친 자리:

- `src/theme/type.css` — **공유**. 내 몫은 `.sr-rv-pathdy` 한 블록뿐
- `.next` 를 **두 번 부쉈다**(빌드 레이스) → `rm -rf .next && npx next dev -p 3200`.
  08-21 에는 그 레이스가 **가드를 공허하게 통과시키기까지 했다**(§9 끝)
- `test_issuance` 2건 빨강은 그쪽 미완성분
- 08-21 에 그쪽이 더 늘린 것: `issuance_mp.py` · `test_issuance_mp.py` ·
  `test_register.py`(문체 가드 — Lab 페이로드만 덮는다, RV 는 범위 밖) ·
  `src/lab/issuance/api.ts`. **무충돌 구역 = `src/lab/` · `issuance*` ·
  `labscenario` · `type.css` 를 뺀 나머지**

---

## 7. 이 환경에서 밟은 함정

| | |
|---|---|
| **`git commit --only -- <paths>`** | **인덱스를 무시하고 워킹트리를 넣는다.** 헝크만 스테이징한 `type.css` 에 동시 세션 CSS 가 통째로 딸려 들어갔다. `--only` 없이 인덱스 그대로 커밋할 것 |
| **`BW_MYSQL_*`** | 유저 환경변수에 있고 **Bash 툴 셸에는 상속 안 된다.** PowerShell 에서 `[Environment]::GetEnvironmentVariable($n,'User')` 로 주입해야 route 테스트 5건이 산다 |
| **`test_static_agreement`** | 예약태스크가 띄운 :8200 을 감지해 **수집 단계에서 거절**한다(설계된 안전장치). `backend\serve.ps1 -Local` 로만 돈다 |
| **백엔드 재기동** | `Stop-ScheduledTask SauronV2Backend` → 리스너 kill → `Start-ScheduledTask`. **떠 있는 백엔드는 새 코드를 모른다** |
| **경로 인코더의 0** | 커스텀 커브에서 0 을 "안 넣음"으로 거르면 워크북 기본형("단기 0 · 장기 +20")이 노드 하나만 남아 **평탄 외삽 = 평행이동**이 된다. 한 벌이라도 0 이 아니면 여덟 테너를 전부 싣는다 |

---

## 8. 판단거리 넷 — **2026-08-21 전부 결론 남** [OWNER]

넷 다 실측으로 깔고 오너가 골랐다. 열려 있는 것은 이제 §5 하나뿐이다.

**x축 경계 = 후보 중앙값 → 유지.** 0 선은 이분면도 못 만든다 — 실측 as-of
08-19 에 후보가 **100% 양수**다(H=3/6/12 전부). 대가는 정의상 늘 반씩 가른다는
것이고, 화면 캡션이 이미 "가운데 선이 후보 중앙값"이라 말한다.

**y축 = 절대선 50 → 유지 + 국면 한 줄**(`3748c1b`). 오늘 42개 중 38개가 그
위라 「많이범·좁음」 칸이 **비어 있었다** — x 를 옮긴 그 이유가 y 에도 있다.
그런데 옮기면 안 된다: 252영업일에서 "50 위" 비율이 **2%~93%** 로 흔들리고
(35~65% 로 갈린 날은 21% 뿐), **그 진폭이 곧 국면이다**. 중앙값 선은 날마다
반씩 갈라 그 사실을 지운다. 두 축이 다른 규칙인 근거도 있다 — y(52주 백분위)는
이미 자기 이력으로 정규화돼 50 이 뜻을 갖고, x(bp/월)는 생 크기라 0 이 뜻이
없다. 대신 각주 한 줄이 개수로 말한다. **임계는 안 뒀다**(가드가 지킨다).

**Score ↔ 신용프리미엄 +0.60 → 안 바꾼다. 다만 진단이 틀린 자리를 가리키고
있었다**(`3748c1b` 가 `rv.py` 독스트링 교체). 이 문서 옛 판과 코드 주석은
"캐리가 신용위험을 따라가서"로 읽혔는데 **캐리는 애초에 점수에 안 들어간다.**
50:50 을 갈라 재면:

    corr(Score, 스프레드) = +0.610
      ├ 절대축 spreadVolPct = +0.358   ← 앵커 교체가 고친 쪽
      └ 상대축 relRv 랭크   = +0.674   ← 남은 상관은 여기 산다

새는 곳은 deviation 만 쓰기로 한 **상대축**이다. 국면 탓도 아니다 —
corr(수준, 자기이력 z)를 252일 재면 중앙값 **+0.59**, 음수인 날 **7%**. 기전은
베타 > 1(넓어지는 국면에서 넓은 섹터가 자기 σ 보다 더 넓어져 z 가 수준을 다
못 지운다). 섹터 평균 Spearman +0.750, 가장 좁은 둘이 바닥(통안 −2.1bp →
38.6 · 은행 6.3bp → 51.0). **버그가 아니라 크레딧을 순위 매기는 일의 값이다 —
고치려면 relRv 의 z 정의를 건드려야 하고 그건 별개 레인이다.**

**버퍼 200bp 초과 → 기전 규명, 열 풀이만 손봄**(`0717f95`). 모든 H 에서
200bp 넘는 행은 **잔존이 가장 짧은 버킷, 예외 없이**다(H=6M: 잔존 0.25Y 가
86~293bp, 2.5Y 가 33~46bp — 잔존에 단조 감소). `buffer = (carry+roll) ÷ 매도
시점 D_mod` 이니 당연하다. "293bp"는 안전하다는 뜻이 아니라 **거의 만기라 금리에
안 다친다**는 뜻이다. 잔존 열 추가는 [OWNER] 안 한다.

---

## 9. 08-21 에 고친 것 — 전부 "사실이 옮겨 갔는데 문장이 안 따라간" 자리

판단거리를 재려고 파일을 열었더니 나왔다. **이 레인의 진짜 위험은 이 종류다.**

| 자리 | 무엇이 틀렸나 |
|---|---|
| 랭킹표 「버퍼」 풀이 | "**3개월** 캐리와 롤" 상수 — H 는 컨트롤이고 기본 6개월. **화면이 거짓말** |
| 랭킹표 「지난주 백분위」 풀이 | "**과거 52주** 중" 상수 — 창은 52주/전체 토글. 같은 파일 나머지 여섯 자리는 이미 따라갔다 |
| `rv.py` 머리 | 절대축 점수 입력을 `pctLastWeek` 이라 적음 — 그건 **되돌린 판**, 실제는 `spreadVolPct` |
| `RvPage.tsx` 머리 ×3 | 사분면 축 "상대 RV × Coverage"(은퇴) · "H 는 **두 값**"(한 벌 됨) · "BEP 는 크레딧 **스프레드** 축, 금리 효과는 화면 밖 — **각주 의무**"(아웃라이트로 뒤집혔고 각주도 은퇴) |
| `RankingTable`·`ScoreHeat` 머리 | 은퇴한 Coverage · σ 병기 |

`RvPage.tsx` 셋째가 제일 위험하다 — 모형 경계를 **정반대로** 적어 두고 각주
의무까지 선언해서, 읽고 손대면 은퇴한 각주를 되살린다.

**왜 여태 안 잡혔나**: 열 머리 풀이는 CDS `Tooltip` 안이라 **열기 전에는 DOM 에
없다.** `container.textContent` 로 읽는 가드는 이 문장들을 영영 못 본다 — 풀이
전체가 검사 사각지대였다. 새 가드(`804cb39`)는 트리거 `.sr-rv-thhelp` 에
포커스를 줘 열고 **포털까지** 읽으며, H 는 3/6/12 를 창은 두 값을 다 돈다.
원래의 거짓말 둘을 되심어 잡히는 것을 확인했다. `<Tooltip>` 은 앱에서 RV 에만
쓰이므로 이 사각지대는 여기서 닫혔다.

**초록이 거짓말한 실측**(`9690540`): `production-env` 가드가 `.next` **디렉터리
유무**만 봐서, 동시 세션이 빌드 중간에 비운 순간(`polyfills.js` 하나만 남음)
건너뛰지도 않고 돌아 **아무것도 못 보고 통과**했다. 직전까지 3건 빨강이던
것이 초록이 됐고 "고쳐졌다"로 읽힐 뻔했다. 이제 앱 청크가 없으면 그렇다고
말한다. 판정기를 떼어 가짜 목록으로 음성 쪽도 검증했다(진짜 `.next` 를 비우면
동시 세션 빌드를 깨므로).
