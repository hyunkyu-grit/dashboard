# 인계 — sauron-v2, 2026-08-18 저녁 (rv2 **진행 중** — Strategy = RV Analysis)

앞 인계는 `HANDOFF-2026-08-18b.md`(이식 레인 A·B·C 완료, 프런트 533/백엔드 546
그린). 이 문서는 rv2 레인이 **중간에 멈춘 상태**의 스냅샷이다 — 오너 지시로
내일 마무리한다. 세션 프롬프트는 "SESSION PROMPT rv2"(통합판) + **트레이더
피드백(2026-08-18, 크레딧 RV 재설계)** 둘 다다.

---

## 0. 지금 상태 — ⚠ 게이트 안 돎 (중간 상태)

| | |
|---|---|
| 백엔드 엔진 | `app/rv.py` **작성 완료**(트레이더 피드백 반영판까지) |
| 라우트 | `/api/rv/analysis` + `/api/rv/history` 배선 완료 (`main.py`) |
| calendar.json | v1 → `src/data/calendar.json` md5 일치 복사, `test_policy` 10 통과 |
| `tests/test_rv.py` | **낡음** — 재설계 전 23/23 이었으나, `scatter` 키를 참조하는 라우트 테스트가 새 `credit` 페이로드와 안 맞는다. **지금 돌리면 빨갛다** |
| 프런트 | `src/rv/` 5파일 중 A·B 카드는 완성, **C 는 재설계 전 구판** — 컴파일 안 되는 상태(아래 §3) |
| 배선 | page.tsx·nav·WindowKey·CSS **미배선** |
| 서버 | `:8200` 은 rv 라우트 **이전** 코드로 떠 있다 — 브라우저 확인 전 재기동 필수 |

**게이트 베이스라인**(이식 레인 종료 시점): 프런트 533/533 · 백엔드 546p/24s/1x.
rv2 완료 시 이 위에 더해져야 한다.

---

## 1. 읽은 것 (재검증 금지)

- `braveworld/docs/diagnostics/REPORT_rv1.md`(25f293ac) + `rv1_probe.py` —
  앵커(Appendix B 8행: carry ≤0.01bp·roll ≤0.37bp·스왑점 13.0 vs 13.2·조달
  3.091% 역산 = 시작 08-14 + 인상 08-27/11-26), 껍질 규칙(고듀레이션 가장자리
  유지), C12(섹터 안 선형화 1위 불일치 0, 풀 경계만 ±1bp), PN-2(껍질 ≠ 창 안
  승자 — 6M 이 껍질엔 있고 창 밖), 1bp 격자가 껍질 멤버(KDB 10Y +5.2~5.7bp)를
  건너뛰는 실측.
- 트레이더 피드백(세션 중 수신): [원칙] ① 절대축·상대축 독립 먼저, 합성은 다음
  ② BEP 는 **크레딧 스프레드 축**(금리 효과는 화면 밖 — 각주 의무) ③ level 이
  아니라 **deviation 만** 점수화 ④ 버퍼는 "+12bp (1.5σ)" 병기. [참고 = 출발값]
  BEP H=3M·Coverage=버퍼÷3M 실현 σ·Relative RV = z 3성분 40/40/20·Score =
  0~100 50:50·UX 4구역(랭킹 표·Score 히트맵·클릭 상세 2차트·사분면 X=RelRV
  Y=Coverage, 서술형 라벨)·합성 화면엔 "투자판단이 아니라 RV 랭킹" 명구 의무.

## 2. 백엔드 — `app/rv.py` (작성 완료, 테스트 낡음)

- **재사용 경계 지킴**: price/dv01_at/periods_for(cashbond) ·
  curve_points/interp(creditmatrix) · rate_on(funding — v2 기본 콜금리 결정
  경유) · MPC_DATES(policy). 새 수식은 조달 레그(avg_funding)·껍질·백분위뿐.
- 레인 A: `candidate`(grid/grid 규약 — 앵커가 채택한 그 쌍)·`upper_hull`
  (가장자리 유지)·`breakpoints`(선형화 — 섹터 안)·`refine_breakpoint`(재가격
  이분법 — **풀 경계만**)·`winners_in_window`(표시 보조 — 결정 숫자는 스왑점).
  섹터 블록에 격자(tr, 10bp 걸음)·스왑점·필터율.
- 레인 B: `spread_series` + `rank_pct`(**랭크** 백분위 — 기존 pct 의 min-max
  위치와 딴 통계라 딴 이름) + z. 히트맵 셀 = pct52/pctAll/z/now/obs.
- **크레딧 RV**(`credit_block` — 모듈 독스트링 "크레딧 RV 의 계산 정의" 절이
  코드보다 먼저 쓴 문서다):
  - BEP Buffer = **국고 헤지 페어**: carry = (y_b−f)t − (y_k−f)t (조달이
    다리별 carry_net 을 경유한 뒤 **페어에서 소거** — 원칙② 의 산술 형태.
    [조정 보고] 그래서 금통위 Δbp 는 크레딧 RV 에 무영향, 레인 A 에는 영향),
    roll = 두 다리 price() 재가격 차, 분모 = 채권 다리 매도시점 D_mod.
    carryBp+rollBp = bufferBp 가 가산 항등식, bepSpread = now + buffer.
  - Coverage = buffer ÷ `vol_3m`(겹침 63영업일 변화 σ, 최소 26관측 아니면 None).
  - Relative RV = 0.4·z(자기 이력) + 0.4·z(같은 테너 **횡단면 평균 대비**)
    + 0.2·z(같은 섹터 **커브 평균 대비**) — 성분 z 를 따로 싣는다(zAbs/zSector/
    zCurve) + pct + cheapBp(창 평균 = Fair, 출발값).
  - Score = 랭크백분위(coverage)·랭크백분위(relRv) 50:50, 후보 <2 면 None.
  - 숏 게이트 `SHORTABLE = {1:IRS, 1.5:IRS, 2:IRS, 3:IRS·선물, 10:선물}` — 5년 없음.
  - `credit_history(m, bt, lab, window)` — 클릭 상세용: points(t,s,rel) +
    spread/rel 각 {now, mean, sd}(±σ 밴드 재료).
- 라우트: `/api/rv/analysis?h&basis&spreadBp&window&mpc`(잘못된 입력 422),
  `/api/rv/history?sector&tenor&window`. 페이로드에 **소스별 as-of**
  (creditMatrix vs irs — B-2), funding provenance, meetings(달력 남은 회의
  미리 채움 0), dys, sectors, pool{hull/swapPoints(refined)/winners},
  heat, **credit**{hMonths:3, weights, items, exclusions}.

## 3. 프런트 — `src/rv/` (A·B 완성, C 구판 → 재작업 필요)

- `api.ts` — **구판**(scatter 타입). credit 블록·history 타입으로 갱신 필요.
- `SectorLane.tsx` — 완성. 손 표(만기×Δy 틴트 격자 + 버퍼 + 승리 구간 열),
  껍질 ◆ 마크와 "창 안 승자" 칸 분리, 스왑점 목록 = 결정 숫자, hMonths prop.
- `TenorHeat.tsx` — 완성. 랭크 백분위 셀, 다이버징 = `tintFor(pct−50, 50)`.
- `RvScatter.tsx` — **구판**(x=백분위, y=버퍼). 재설계: X=RelRV(σ, 경계 0) ·
  Y=Coverage(σ, 경계 1σ [출발값]) · 서술형 사분면 라벨(싸고 버팀/싸지만 취약/
  비싸지만 방어적/비싸고 취약 — 명령형 금지) · 속빈 마커 유지 · 리드아웃
  "+12bp (1.5σ)" 형식.
- `RvPage.tsx` — **구판**. 조건 바(고정·불투명: 후보·H·조달·이력창·소스별
  as-of 강조·제외·금통위 Collapsible 8행 BpField)는 골격 유지하되: 가중
  40/40/20 노출·H 표기(A 6M / 크레딧 3M)·금리효과 각주(원칙②) 추가, 카드 C 를
  4구역(랭킹 표 → Score 히트맵 → 사분면 → 클릭 상세)으로 교체, DrillWindow 를
  `/api/rv/history` 2차트(±σ = ReferenceLine dataY 밴드)로 교체. **명구
  "투자판단이 아니라 RV 랭킹이에요" 를 랭킹 표·Score 히트맵에 의무 부착.**
- **미배선 4건**(이거 안 하면 tsc 부터 깨진다):
  ① `page.tsx`: `section === 'strategy'` → `<RvPage/>`, `NOT_BUILT.strategy`
  제거, nav blurb 를 RV 로. ② `ui/window/geometry.ts` `WindowKey` 에 `"rv"`.
  ③ `theme/type.css` 에 `.sr-rv-*` 블록(bar·scroll·table·th·td·left·hull·
  tick·scatter·dot — 컴포넌트가 이미 참조 중). ④ CDS `Collapsible` import
  경로 확인(`@coinbase/cds-web/collapsible` — cds-tablecell 가드 파일들 참조).

## 4. 판단·조정 기록 (보고에 실을 것)

1. **레인 B(백분위)와 크레딧 Score 히트맵 병존** — 피드백의 히트맵은 크레딧
   RV 구역의 것("스프레드 수준 히트맵 아님")으로 읽었다. 레인 B 는 오너 확정
   (백분위 = deviation)이라 남기고, Score 히트맵은 합성층으로 따로 선다 —
   원칙①(독립 먼저, 합성 다음)의 배치 그대로.
2. **조달 페어 소거** — carry_net = 캐리−조달을 다리별로 경유시키되 페어에서
   상쇄됨을 코드·독스트링에 명시(헤지 다리 조달이 달라지는 날의 이음매 유지).
3. H 는 레인 A 6M(오너 확정) / 크레딧 RV 3M(트레이더 출발값) — 딴 값이 맞다.
4. Fair = 창 평균, 커브 RV = 섹터 커브 평균 대비 편차, Coverage 분모 = 겹침
   63영업일 σ(min 26), Score = 랭크 백분위 50:50, 사분면 Y 경계 = 1σ — 전부
   [참고] 출발값으로 구현, 조건 바·독스트링에 정의 명시.
5. 트레이더 앵커(70+7+5→82)는 1차 산술로 구성 가능: carry_bp ≈ s·t/D
   (70×0.25/2.5 ≈ 7) — 합성 커브(KTB 평탄 2.60% + BD 2.5Y=+60bp/3Y=+70bp,
   H=3M)로 pytest 재현 예정이었다(§5-1).

## 5. 내일 할 일 (순서대로)

1. **`tests/test_rv.py` 갱신** — ① scatter 참조 라우트 테스트를 `credit`
   페이로드로 재작성(items 의 5년 숏 불가·seriesId CRD-·score/coverage 존재·
   weights 노출·exclusions 의 만기보유) ② 트레이더 앵커 신설: 합성
   CreditMatrix(위 §4-5 커브, 상수 시드 — funding 은 `fd._series_cache`
   시드), 단언 = carry_bp∈[6,8]·roll_bp∈[4.5,5.5]·**buffer = carry+roll
   (반올림 항등)**·**bepSpread = now + buffer (항등)**·bep ≈ 82±1
   ③ `/api/rv/history` 모양 테스트 ④ 기존 Appendix B 앵커·껍질·게이트
   테스트는 그대로 두면 된다(엔진 미변경).
2. 프런트 재작업 §3 (api.ts → RvScatter → RankingTable/ScoreHeat 신설 →
   RvPage 조립 → DrillWindow).
3. 배선 4건 (§3 끝).
4. 가드 신설 `guards/rv-analysis.test.tsx`: 명구 존재 · 서술형 사분면(명령형·
   "매수" 문구 금지) · 별/메달 금지 · 속빈 마커 렌더(shortable=false →
   fill=--sr-card) · "껍질"/"창 안 승자" 두 용어 공존 · 다이버징 틴트 소스 핀
   (`tintFor(pct−50, 50)`, Score 는 `tintFor(score−50, 50)`) · §16(프런트에
   z·백분위 재계산 없음 — 서버 값 통과 소스 핀).
5. 게이트: `pnpm vitest run`(533+α) · tsc · build(후 :3200 dev 재기동) ·
   backend pytest(546+α). **:8200 재기동 후** 브라우저 실측(조건 바 as-of
   갈라짐 문구 — 지금 실제로 민평 08-14 vs IRS 08-17 로 갈라져 있어 바로
   보인다), HANDOFF 이 문서에 완료 절 덧붙이고 커밋 없이 보고.

## 6. 함정 재확인 (이 레인에서 이미 밟았거나 상속)

- `cd` 겹침 금지 — **실행 명령은 절대 경로로 시작**(어제 v1 오염 사고).
- `:8200` 는 import 시점 데이터로 산다 — 라우트/데이터 확인 전 재기동.
- CDS Select 자동화는 클릭 좌표가 표류한다 — 열고 **키보드(Down/Return)**.
- TextCaption 은 uppercase — "bp"·"(Setting)" 든 문장은 TextLegal.
- CDS Table 금지(셀당 32px) — RV 의 표는 전부 손 표(.sr-rv-table).
- 색 리터럴 금지 — 사분면 스크림도 `color-mix(... var(--sr-up) 6%, transparent)`.

---

## 7. 완료 (2026-08-19 — rv2-final 세션)

§5 다섯 항목 전부 끝났고 게이트가 그린이다. **커밋 안 함** — 커밋은 오너 몫.

### 게이트 (2026-08-19 실행)

| | 결과 |
|---|---|
| 백엔드 `tests/test_rv.py` | **27/27** (앵커 재현: carry 6~8·roll 4.5~5.5·BEP 82±1 첫 실행 통과) |
| 백엔드 전체 pytest | **574 passed / 23 skipped / 1 xfailed** (베이스라인 546p 위에 +28) |
| 프런트 vitest | **550/550** (베이스라인 533 + rv-analysis 가드 9 외) |
| tsc / next build | 둘 다 클린 |
| 브라우저 실측 | :8200 재기동(serve.ps1) + :3200 dev 재기동 후 확인 — 아래 |

### 브라우저에서 실측한 것 (:3200/?g=strategy)

- 조건 바: 후보 94 · **H 두 값**(동일섹터 6개월·크레딧 3개월) · 가중 40/40/20 ·
  **민평 2026-08-14 vs IRS 2026-08-17 갈라짐 강조 + 문구**(예고대로 실데이터가
  갈라져 있어 바로 보였다) · 금리효과 각주 · 제외 7건 · 금통위 Collapsible.
- 레인 A 격자(껍질 ◆·승리 구간·스왑점 목록) · 레인 B 히트맵 · **C 4구역**:
  랭킹 표 74행(bp(σ) 병기·Score 다이버징 틴트·σ 미확정 공사채 30Y 는 순위 —
  로 맨 아래·명구) → Score 히트맵(명구) → 사분면(서술형 라벨 4·유망 스크림·
  속빈 마커·"σ 미확정 1개" 문구) → 점 클릭 = DrillWindow **2차트 ±σ 밴드**
  (회사채 AAA 6M: 지금 44.5 · 창 평균 33.5 · σ 10.2bp 리드아웃 확인).
- `rw=all` 토글 URL 반영·콘솔 에러 0.

### 이 세션이 새로 밟은 함정 (다음 세션 상속)

- **CDS `Collapsible` 에 `heading` prop 없다** — controlled `collapsed` 컨테이너다.
  머리는 `Pressable` + chevron 을 손으로(시뮬 `SimulationPage.tsx` 의 관용구 그대로).
- **스크롤 flex 기둥의 카드는 `flexShrink={0}` 필수** — RvPage 루트가
  `overflow:auto` VStack 인데 자식 카드들이 넘치는 대신 **수축해서** 표가 중간에
  잘렸다(첫 스크린샷 실측). 카드·조건 바 전부에 flexShrink=0.
- funding 시드 테스트는 `fd._series_cache` 직접 시드 후 **반드시 `reset_cache()`
  복원** — 모듈 스코프 TestClient 라우트 테스트가 같은 캐시를 읽는다.

### 변경 파일 (이 세션)

    backend/tests/test_rv.py        scatter→credit 라우트 테스트 재작성 + TestTraderAnchor(합성 커브·조달 소거·σ None) + history 모양
    src/rv/api.ts                   RvCreditItem/RvCredit/RvHistoryPayload + fetchRvHistory (scatter 타입 은퇴)
    src/rv/RvScatter.tsx            재설계판 — X=RelRV σ(경계0)·Y=Coverage σ(경계1σ)·서술형 사분면·"+bp (σ)" 리드아웃
    src/rv/RankingTable.tsx         신설 (명구 의무·Score 틴트·행 클릭)
    src/rv/ScoreHeat.tsx            신설 (명구 의무·tintFor(score−50,50))
    src/rv/RvPage.tsx               조립 — 조건 바 확장·C 4구역·DrillWindow=/api/rv/history 2차트 ±σ·Pressable+CdsCollapsible
    src/app/page.tsx                strategy 분기 → <RvPage/> (ErrorBoundary "RV 분석")
    src/ui/nav.ts                   NOT_BUILT.strategy 제거·blurb "어디가 싸고 얼마나 버티는지 — RV 랭킹을 본다."
    src/ui/window/geometry.ts       WindowKey + "rv"
    src/theme/type.css              .sr-rv-* 블록 (bar sticky 불투명·table·scatter·quad·dot·row/cell)
    guards/rv-analysis.test.tsx     신설 — 명구·서술형·별/메달 금지·속빈 마커·껍질≠창 안 승자·틴트 소스 핀·§16 재계산 금지

---

## 8. 디자인 패스 (2026-08-19 — /impeccable critique 26/40 → 전건 수리)

크리틱 스냅샷 = `.impeccable/critique/2026-08-18T23-34-19Z__src-rv.md` (dual-agent,
첫 실행). 오너 답: **정직성부터 · 전부 다 · 2열로 접기**. 전부 반영했고 게이트
그린(프런트 558/558 · 백엔드 rv 27/27 · tsc · build). **커밋 안 함.**

- **P0** `/api/rv/history` 가 points 를 창으로 안 잘랐다 — 라벨 "52주" 아래 전체
  1,625점이 그려지고 ±σ 밴드만 창 통계였다(다른 모집단). `rv.py` 수리(52w=252점
  실측 확인) + pytest 핀.
- **P1** as-of 강조 `.sr-up` → `.sr-rv-asof-split`(600+밑줄) — 방향색은 바 배경
  (--sr-page) 위 4.1:1 미달 + 빨강 상승·경고 겸직. 가드가 회귀를 막는다.
- **P1** 랭킹 74행 → 카드 내부 스크롤(`.sr-rv-rank-scroll`, **뷰포트 연동 캡**
  `clamp(240px, 100vh-420px, 540px)` — 고정 540 이면 짧은 창에서 페이지를 내리게
  되고 열 머리가 조건 바 뒤로 사라진다, 실측) + `.sr-rv-th` sticky·불투명.
- **P2** 사분면 리드아웃 고정 높이(`.sr-rv-readout` 40px — hover 가 클릭 대상을
  밀던 실측) · `<tr role="button">` 제거 → 종목 칸 실제 버튼(`.sr-rv-linkbtn`)
  74개가 유일한 키보드 경로, 히트맵 칸·점은 tabIndex 없음/-1 포인터 지름길
  (탭 정지 226→~80), svg aria-label 이 비시각 요약("N개 중 M개가 싸고 버팀").
- **P3+관찰** `fmt.ts` 신설(sig 음수 0 수리·중복 정의 제거·bp 표기 toFixed(1)
  통일) · 드릴 창 aside TextCaption→TextLegal("BP"→"bp") · 드릴 창에 z 성분
  분해 상시 줄(툴팁 전용이던 것의 키보드 채널) · 동일테너 칸도 클릭→이력 창
  (Score 히트맵과 행동 통일) · 금통위 "전부 0으로" 리셋 + ±100bp 클램프 +
  캡션/입력 폭 76 정렬 · 에러/로딩 = 공용 ErrorState/LoadingState · 조건 바
  "갱신 중…" 표시 · x축 이름 가운데.
- **레이아웃**: 2열 두 줄 [A|B] / [랭킹|Score+사분면] [OWNER "2열로 접기"] —
  세로 스크롤은 100vh 원칙의 의도된 예외로 RvPage 모듈 주석에 문서화.
- **CDS 점검**: 사용 중인 것 전부 적절, 미채용 결정 둘을 문서화 — Tooltip(148개
  셀에 팝오버 기계 비용이라 접고 드릴 창이 키보드 채널), Table(측정 금지 유지).
  ErrorState/LoadingState 채용이 실질 교정.
- 가드 +8 (sticky·asof 회귀·표 의미론·tabIndex·fmt·svg 요약).
- 남긴 것(의도): 30Y 열(관측 1개라도 데이터 사실) · 명구 2회(트레이더 의무,
  가드가 지킴) · `.sr-strip` height 전환(RV 밖·문서화된 예외).
- **함정 재확인**: `next build` 는 떠 있는 dev 의 `.next` 를 부순다(:3200 이
  500 — 실측 재발). 빌드 후 dev 재기동 필수.

### 8.1 크리틱 도발 질문 3 — 오너 결정·반영 (2026-08-19 후속)

1. **랭크 Δ 열 추가 [OWNER]** — 서버가 전 영업일 랭크를 **전일까지의 이력만
   아는 세계에서 재계산**(`rv._credit_items(end_i)` — 미래 참조 금지) 해
   `rank`(동점은 seriesId 로 결정적)·`rankDelta`(양수 = 올라옴)를 싣는다.
   표는 서버 rank 순 정렬 + Δ 열(▲▼ 잉크 — 방향색과 딴 문법이라 색 없음,
   0 = '·', 어제 없던 항목 = 공란). 라이브 실측: 73개 전부 Δ 보유, 2위 ▲1·
   3위 ▼1·11위 ▼2.
2. **속빈 마커는 비싼 쪽에서만 [OWNER "싼 쪽은 채움"]** — 숏 수단 부재가 막는
   실행은 "비싸니 판다" 쪽뿐. `!shortable && relRv < 0` 만 속빈, 범례는
   "실행 가능 / 비싼데 숏 불가". 라이브 실측: 속빈 12개 전부 x=0 왼쪽.
3. **1행(A·B) 밀도는 그대로 [OWNER]** — 두 표의 행 수 차이는 데이터 사실.

게이트: rv pytest 27/27(rank 연속성·Δ 존재 핀 추가) · 프런트 559/559(Δ 렌더
+마커 부호 가드 추가) · tsc·build · :8200/:3200 재기동 후 실측. 커밋 없음.

### 8.2 재크리틱 26→35/40 + 잔가시 전건 수리 (2026-08-19 3차)

2차 크리틱 스냅샷 = `.impeccable/critique/2026-08-19T00-29-07Z__src-rv.md`
(35/40 — 1차 수리 전부 계측 검증: sticky 792/792·tr role 0·linkbtn 74·탭 정지
226→~80·속빈 12개 전부 왼쪽·전 셀 대비 합격 8.11/6.96:1). 오너 답: **잔가시
전부 다 + 카드 우측 빈공간 꽉 채우기(폰트 확대 허용)**, 구조 질문 둘(Score
히트맵 접기·랭크 급변 한 줄)은 보류. 반영(560/560·tsc·build 그린, 커밋 없음):

- **P1 다크 흰 스크롤바** — `direction.css` 에 `[data-sr-scheme]` 별
  `color-scheme: light/dark`(UA 크롬 힌트는 스킴 페어의 집에). 다크 실측:
  스크롤바 어두운 트랙 확인. 가드 핀.
- **P1 열 머리 가림** — 조건 바를 **한 줄 sticky**(Cond 행 + as-of 차단 문구만,
  107→**55px** 실측)로 줄이고 각주·제외·금통위는 **비-sticky 흐름**으로(위에선
  읽히고 스크롤하면 비켜남). 행 24까지 열 머리 생존 실측.
- **P2 랭킹 죽은 여백** — clamp 캡 은퇴 → **absolute 채움**(`.sr-rv-rank-fill`
  relative + `.sr-rv-rank-scroll` absolute inset 0). absolute 인 이유: 흐름에
  두면 표 내용(1,799px)이 2열 행 높이를 밀어올린다. 실측: 카드 891px 중 표
  769px(이전 517), 죽은 여백 0.
- **P2 이력 창 토글** — 동일테너 카드 머리 → 조건 바로(전역 조건은 전역 자리).
- **P3 Δ 발화** — `<Delta>` 컴포넌트 `role="img"` aria-label("2계단 올라옴"/
  "변동 없음"). 가드 핀.
- **[OWNER 꽉 채우기]** `.sr-rv-table { width: 100%; font-size: 14px }`(13→14,
  일별 대사 표와 같은 크기)·th 12→13px — 랭킹 표 584/1020px(57%)이던 폭이
  카드 전폭으로.
- CDS 재확인: 이번 수리에 CDS 대응물 없음(color-scheme·스크롤바·sticky 표 =
  브라우저/손 표 영역), SegmentedTabs·Collapsible·Button 은 CDS 그대로.
- 남은 관찰(보류·오너 몫): Score 히트맵 존폐, 랭크 급변 리드, 조건 바 요약
  계층화(Q3), 표 위 휠 트랩(내부 스크롤의 본성 — 채움 높이가 커져 완화됨).

### 8.3 오너 후속 결정 반영 (2026-08-19 4차, 560/560·미커밋)

1. **Score 히트맵 유지** [OWNER].
2. **Δ 열 → "순위변동" 개명** + **오늘의 한 줄** [OWNER 문구: "지금 XX가 가장
   매력적입니다"] — 조건 바 아래 흐름에 서버 랭크 1위 리드(누르면 이력 창,
   Score·버퍼(σ)·순위변동 병기). 명구 규율과의 긴장은 오너 지시 우선으로 기록.
3. **조건 각주 은퇴** [OWNER "설명 지우고 각 칸 꽉꽉"] — BEP 금리효과 각주(원칙
   ② 각주 의무였음 — **오너 지시로 화면에서 내림**, 정의는 rv.py 독스트링 잔존),
   제외 N건 문장(정보는 서버 exclusions + 표의 — 표기 잔존), 금통위 안 설명
   한 줄. 금통위 입력 컨트롤·전부 0으로·클램프는 유지.
4. **크레딧 리스크 미반영 문제 제기** [OWNER — "회사채 단기물이 가장 매력적으로
   보임, 타개책 제시"]: 산술 원인 = Coverage(버퍼÷σ ≈ s·t/D÷σ)가 **스프레드
   수준(=신용위험 프리미엄)을 점수로 통과**시킴 — 레인 B 가 금지한 병리(OFB
   독점)가 합성층으로 재침입한 형태(원칙 ③ deviation-only 의 우회로). 타개책
   4안 제시 → **오너 선택 = "Coverage 도 평소 대비로"** (§8.4 에서 구현).

### 8.4 Coverage 레벨 오염 수리 (2026-08-19 5차, 560/560·rv 30/30·미커밋)

**Score 의 절대축 입력 = covPct(Coverage 의 자기 이력 백분위)** [OWNER]:
- `rv.coverage_hist_series` — covProxy(t) = s(t)÷vol3m(t), vol3m(t) 는 시점별
  롤링 252자리 σ(누적합 O(n) — 74계열×1,600일×2회가 요청 경로). buffer ≈ k·s
  (캐리 1차) 근사에서 백분위는 k 불변 — **레벨 불변성이 핀**(스프레드 5배
  계열과 백분위 동일, pytest).
- `mid_rank_pct` 신설 — 동률 절반 셈(상수 계열이 ≤-셈으로 100%="늘 후하다"로
  읽히는 왜곡; rank_pct 와 **다른 통계 다른 이름**).
- Score = 0.5·covPct + 0.5·rank_pct(오늘 relRv 랭크). 표시축(coverage bp·σ
  병기·사분면 Y축)은 그대로 사실. 페이로드 +covPct, 드릴 성분 줄에 노출,
  랭킹 명구에 "스프레드가 넓다고 점수가 오르지 않아요" 명시.
- **라이브 검증**: 1위 회사채 6M(covPct 74)→**10위**, 새 1위 = 회사채 2Y
  (covPct 99·버퍼 +10.8bp — 크기가 아니라 평소 대비), **스프레드 27bp 산금채
  5Y 가 2위** = 수준이 아니라 deviation 이 점수라는 결정적 증거. 상위 8에
  2Y~7Y 혼재(단기 독식 해소).

### 8.5 오너 4건 후속 (2026-08-19 6차, 560/560·미커밋)

1. **사분면 hover 흔들림 근절** — 리드아웃을 아래 블록(min-height 예약)에서
   **플롯 위 오버레이**(`.sr-readout` 재사용 + `.sr-rv-hovercard`, absolute·
   pointer-events 없음)로. 범례는 항상 서 있는 정적 줄. 계측: hover on/off 에
   svg top 412→412 **불변**. (min-height 예약판은 랩 줄 수 차이로 재발했었다.)
2. **"그래도 회사채?" 데이터 검증** — 수리 후 회사채 상위는 레벨이 아니라
   **실제 편차**: 섹터 평균 zSector **+1.04**(다른 AAA 대비 평소보다 벌어짐 —
   나머지 AAA 섹터는 전부 음수), 2Y 실측 52주 평균 39.4→지금 57.4bp(+1.6σ)·
   섹터 상대 9.2→17.5bp(+2.2σ). 그것이 신용 악화(가치 함정)인지 수급(RV
   기회)인지는 시스템이 판정하지 않는다 — 명구의 선. **구조적 주의 기록**:
   자기 이력 계열이 covPct(50%)+zAbs(20%)로 합산 ~70% — 섹터 전체가 최근
   벌어지면 그 섹터가 상위권을 넓게 차지한다. 완화하려면 RV_WEIGHTS 의 zAbs
   하향(40→20)·섹터/커브 상향이 후보 — **오너 결정 대기**.
3. ~~빈 공간 전멸(히트맵 행 stretch)~~ — **오너 지시로 롤백**("이전으로
   돌려주고"): 카드 채움은 행 높이가 아니라 레이아웃의 일. 히트맵 밀도 원복.
4. **디자인 컨셉 = Main·Backtest 정렬(1차)** — 랭킹 표에 행 문법 이식:
   `.sr-rv-stack/.sr-rv-name/.sr-rv-sub`(지금 bp·숏 수단이 서브라인 —
   "1년 평균 · 호가" 자리), `.sr-rv-divided`(행 헤어라인), 열 9→7.

### 8.6 디자인 컨셉 = Main·Backtest **문법 그대로** (2026-08-19 7차, 560/560)

[OWNER — "진짜 그거랑 비슷하게 가야"] Main 카드의 실제 구현(`OverviewColumns
.tsx:88-93` — 머리 = TextLabel1 하나 · paddingX 2/pt 1.5/pb 0.5 · 설명 문장
없음 · 표는 카드 전폭)을 RV 카드 다섯 장에 그대로:

- 카드 머리: TextTitle3+캡션 문장 → **TextLabel1 단독**(캡션 다섯 개 삭제 —
  정보는 표 하단 각주가 이미 짐). 사분면 카드만 미리보기(PreviewPane) 문법으로
  **as-of 서브라인**("민평 2026-08-18 · 52주 창") 병기.
- **표 전폭**: 카드 padding 제거, 가장자리 인셋 16px 은 첫/끝 셀이 진다
  (`.sr-rv-table th/td:first/last-child`) — 틴트·헤어라인이 카드 가장자리까지
  가는 Main 의 InstrumentTable 룩. 표 밖 줄(스왑점·각주·범례·plot)은 각
  컴포넌트가 paddingX 2 로 인셋.
- 게이트 560/560·tsc·build·브라우저 실측(랭킹·히트맵 전폭, 사분면 서브라인,
  hover 오버레이 유지).

### 8.7 IA 재구성 — Backtest 그대로 (2026-08-19 8차, 560/560·미커밋)

[OWNER — "진짜 백테스트 가서 읽어보고 와라: 위계, 중요한 건 앞으로, 세부는
인터랙션 뒤. 스크롤 불편. 글자 크기 다름. 사분면 구림"]

- **페이지 스크롤 소멸** — 100vh 기둥 복귀: 조건 바 + 오늘의 한 줄 +
  **[랭킹 표(주인공, basis 980 — Backtest 표 카드의 그 산술) | 사분면
  (미리보기, 나머지 폭)]** 한 행이 남는 높이를 다 받는다. 실측: rootScrollH
  == clientH(스크롤 0)·가로 오버플로 0.
- **세부는 인터랙션 뒤** — 동일섹터·동일테너·Score 히트맵은 표 머리의
  **"커브 레인" 버튼**(Backtest 의 백테스트 버튼 자리)이 여는 FloatingWindow
  (`windowKey: "rvlanes"`, 1180px)로 이사. 클릭 = 이력 창(기존).
- **행↔미리보기 연동** — RankingTable `onHover` → 사분면 점 강조(링+이름표)
  + 하단 리드아웃 갱신(Backtest 의 행 hover → 차트 문법). 실측 확인.
- **활자 = Backtest 실측값** — 표 16px·이름 14/600·서브 13/500·th 16/700
  뮤트(`.sr-rv-table`/`.sr-rv-th`/`.sr-rv-name`).
- **사분면 재디자인** — viewBox 640×430, 바닥·왼쪽 축 프레임, 눈금·사분면
  라벨 13px(라벨 600), 점 r5·강조 r6.5+링+이름표(`.sr-rv-dotlabel`),
  리드아웃 = svg 아래 **고정 높이 44px** 블록(포인터 > 표 hover > 안내 —
  흔들림 3차 근절), 범례 한 줄 상시. **svg 는 flex 로 남는 높이만**
  (`.sr-rv-scatter` height:100%·flex:1 — 폭 기준 확장이 리드아웃을 카드
  밖으로 밀던 실측).
- 오버레이(.sr-rv-hovercard)는 pane 문법으로 대체(CSS 클래스는 잔존·미사용).

### 8.8 컨트롤 위계 + 토스체 (2026-08-19 9차, 560/560·미커밋)

[OWNER — "메인 보고 와서: 커브 레인·52주/전체 폰트가 클 필요 없다 + 설명은
토스식 쉬운 문장으로"]

- **컨트롤 pill = Main 기간 셀렉터 실측값**(14/600·높이 32) — `.sr-rv-pillbtn`
  신설. 52주/전체(SegmentedTabs 16/36 은퇴 → aria-pressed pill 둘)와 커브 레인
  (CDS Button s 36 은퇴 → 상시 채움 pill). 계측 확정 3개 전부 14/600/32.
- **문장 토스체** — 랭킹 각주 "평소보다 얼마나 벌어졌는지로 줄을 세워요.
  ▲▼는 어제와 비교한 순위예요"(명구는 의무라 유지·가드 그대로), Score 히트맵
  "100에 가까울수록 평소보다 벌어져 있고 버틸 힘도 있다는 뜻", 동일테너
  "숫자가 높을수록 지금 스프레드가 평소보다 넓은 쪽", 동일섹터 "칸은 금리가
  그만큼 움직였을 때 N개월 들고 있으면 얻는 수익 · 스왑점을 지나면 1등이
  바뀌어요"(창 밖 후보 문구 삭제), 사분면 안내 "줄이나 점에 커서를 올려
  보세요 / 오른쪽 위가 싸고 튼튼한 자리예요", x축 "오른쪽일수록 평소보다
  넓어요", 드릴 "가는 선은 평균과 ±1σ예요", 조건 바 "후보 94개"(격자 설명
  삭제).

### 8.9 히어로 위계 + 드릴 리드아웃 + CDS (2026-08-19 10차, 560/560·미커밋)

[OWNER — "이력에도 Main/Backtest 처럼 커서 패널 · 폰트 위계 제대로 · 조건/한
줄/금통위 눈에 안 띔 · CDS 능동 활용"]

- **히어로 = Main 의 그 문법**(PreviewPane: 이름은 작게, **값은 크게**) —
  이 화면의 값은 오늘의 1위 종목이라 종목명이 **CDS TextDisplay3** 로 선다:
  뮤트 라벨 "지금 가장 매력적이에요" / **회사채 AAA 2Y**(클릭=이력 창) /
  메타(Score·버퍼·순위변동) 뮤트 TextBody. 상단 크기 사다리 완성: 히어로 ≫
  카드 제목 14/600 > 표 16 > 서브 13.
- **금통위를 조건 바 행에 편입** — 별도 층(접이 머리)이던 것을 pill
  ("금통위 전부 0"/"조정 중", 조정 중이면 data-on 채움)로 바 끝에. 펼침
  몸통(CdsCollapsible)만 바 아래. 상단이 [조건 한 줄 → 히어로 → 본 화면]
  세 층으로 정리 — 위계 없는 잔글씨 3층 문제 해소.
- **드릴 차트 커서 리드아웃** — CDS `Scrubber`(enableScrubbing +
  onScrubberPositionChange) + 공용 `ReadoutCard/ReadoutLevel`(PreviewPane
  배선 그대로, `.sr-plot` + hoverX 클램프): 날짜 title + 값/창 평균/σ 세 줄.
  실측: 카드 뜸·구슬 찍힘 확인.
- CDS 능동 활용 이번 회: TextDisplay3·Scrubber·CdsCollapsible + 공용
  ReadoutCard(CDS 문법 카드). Pressable 은퇴(금통위 머리가 pill 로).

### 8.10 오너 8건 일괄 (2026-08-19 11차, 561/561·rv 30/30·미커밋)

1. 문구: "숏 IRS/숏 없음" → **"헤지수단 IRS/헤지수단 X"**(서브라인·hover
   카드·범례 통일).
2. **사분면 = 실측 크기**(useMeasure — meet 비율 끼움은 남는 쪽을 여백으로
   버렸다) → 패널을 왜곡 없이 꽉 채움(`.sr-rv-plotfill` flex:1).
3. svg 이름표(점들과 겹침) 은퇴 → **커서 옆 작은 패널**(공용 ReadoutCard —
   종목/스프레드/상대 RV/버퍼/BEP/헤지수단). 하단 고정 리드아웃도 은퇴(카드가
   승계). mouseover 발화 검증.
4. **점 = 방향색**(흑백은 안 읽혔다) — 히트맵 틴트와 같은 문법: 오른쪽(평소
   보다 넓음) `--sr-up` 적 · 왼쪽 `--sr-down` 청, 속빈은 비싼 쪽 헤지 불가만
   (테두리 = 그 방향색). 가드 갱신.
5. **순위변동 ▲적/▼청**(sr-up/sr-down — 카드 위라 4.5:1 통과).
6. **랭킹 만기 상한 10Y**(`rv.RANK_MAX_YEARS`) [OWNER] — 20Y·30Y 는 후보에서
   빼고 exclusions 가 "10년 초과" 사유(라이브 74→67·제외 7). pytest 핀.
7. **이력 창·금통위·가중 → 우측 끝 "설정" 하나**(pill → CdsCollapsible 펼침).
   조건 바 상시 = 후보·H·조달·민평·IRS 다섯 + 비기본 상태 배지(전체 창·금통위
   조정 중)만 — **첫 화면은 필요한 정보만** [OWNER].

### 8.11 마감 라운드 (2026-08-19 12차 — 561/561·미커밋)

**오너 확정**: 가중은 트레이더 엑셀 설계 그대로(40/40/20·50:50 — 이중 계상
지적은 기록만) · 정렬/필터 보류.

**검증 3종**: 다크 재확인 ✓(색 점·▲▼·히어로 판독) · **창중창** ✓(커브 레인 창
안 셀 클릭 → 이력 창이 위에, Esc 는 맨 위만 — 스택 규칙 정상) · 데드 코드
청소 ✓(.sr-rv-hovercard/dotlabel/pane-readout CSS·중복 import).

**3차 크리틱 26→35→36/40** (`.impeccable/critique/2026-08-19T03-27-11Z`).
인페이지 디텍터 15→**2건**(전부 오탐 계열). P1~P3 마감 수리:
- pill 선택 채움이 라이트에서 bar 배경과 동일(238,240,243 실측) → **on =
  잉크 반전**, 커브 레인은 `data-fill`(--sr-control — Backtest secondary look).
- 랭킹 th 16→**13px**(DESIGN §5.3 "분리는 굵기, 크기는 아님" — 앞서 적었던
  "Backtest th 16px 실측"은 딴 th 를 잡은 혐의로 주석 정정).
- 사분면 y 도메인 **3σ 캡**(10σ outlier 가 축을 늘려 1σ≈30px — 초과점은 상단
  가장자리 + 리드아웃 실값).
- **exclusions 렌더**(계약에만 있던 것) — 랭킹 각주 "랭킹에서 뺀 항목 n개 —
  만기 보유·10년 초과"(title 상세).
- 히어로/종목 버튼 hover 밑줄(클릭 어포던스).
- 주석 정정: RvPage "유일한 스크롤 페이지"(낡음)·RankingTable "74행".

**잔여(보류·오너 몫)**: 정렬/필터 · 커브 레인 세 표 열 격자 상이. **커밋 = 오너.**

### 8.16 백테스트 기준선 + 시뮬 사전 검증 (2026-08-19 17차, 571/571·미커밋)

1. **백테스트 차트에 CD 91일·기준금리** [OWNER "CD금리는 같이 그려줘야지" +
   기존 동결 지시 2026-07-31 "CD와 기준금리는 항상 같이"]: `LinkedCharts` 종목
   차트에 미리보기 pane 과 같은 낱말로 얹음 — `alignByDate`/`policyByDate`
   날짜 정렬·`referenceMode`(%-종목은 같은 축, bp-종목은 왼쪽 %축)·기준금리
   stepAfter·--sr-ref-cd/policy 색·범례는 그려진 것만·리드아웃 카드에 CD 행.
   CD 는 `PreviewPane.loadCd` **모듈 캐시를 export 해 공유**(두 번 안 받음),
   policy 는 prop(BacktestWindow·CashBondWindow — 현금채권 창도 같은 배선이라
   자동 동반). 실측: 10Y 1년 북에서 파랑 종목선+호박 CD+보라 기준금리 계단+
   범례 확인.
2. **시뮬 실행 전 검증 확대**(`scenario.ts::preRunErrors` — 크리틱 #5 잔여):
   존재 이유 = "조용히 빠지는 것들". ① rowError 줄(전개에서 무언 skip 되던
   것) → "포지션 N번 줄: {이유}" ② 다리 전개 실패 줄(페이로드에서 무언 탈락)
   → 서버 이유 그대로 ③ 날짜 미정/구간 밖 이벤트(`inWindow` 가 무언 폐기) →
   케이스 이름 달고 ④ days 정수·≥1 ⑤ legs 0 의 무언 return → 문구. 실측:
   기간 0 → "실행하지 못했어요 — 기간(일)은 1 이상의 정수여야 해요."(요청
   미발송). 가드 `simulation.test.ts` +5(케이스 라벨은 영문 "Bear").
   게이트 571/571·tsc·build·dev 재기동.

### 8.15 전체 크리틱(30/40) 수리 라운드 — P0 백엔드 + P1 둘 + 얼라인 (2026-08-19 16차, 562/562 프런트·미커밋)

**[OWNER 결정 4]**: P0 지금 조사 / 핀 = 옅은 회색 배경 / **커맨드바 은퇴**(단축키
불요) / 정돈 = "가능하면 한 줄, 아니면 가로세로 얼라인 — 모든 컴포넌트".

1. **P0 시뮬 실행 실패 — 뿌리 셋 전부 수리**:
   - 뿌리: v2 `data/irsdata.xlsx` 가 **08-13 탄생 스냅샷**(마지막 날짜 08-13)
     — 페이지 asOf(08-18, SQL 벽)와 5일 갈라져 기본 실행이 항상 실패.
     v1(braveworld, 아침 굽기가 매일 갱신·오늘 07:52)에서 복사(읽기, v1 무변).
     **항구 파이프라인(아침 태스크에 복사 한 줄)은 오너 몫.**
   - 가면: `NonBusinessDayError` 시그니처 (date, reason) 인데 `irsdata.py`
     3곳이 메시지 1개로 호출 — **깨끗한 에러가 되려다 생성자에서 TypeError**
     (backend.log 4회 실측). 3곳 수리.
   - 표면: 스트리밍 200 은 헤더가 나간 뒤라 4xx 불가 → **엔진 예외를
     `{"detail": …}` 페이로드로 스트림에 실음**(simulate.py) + 프런트
     readOrThrow 가 detail 모양이면 그 문장으로 throw(sim/api.ts).
   - `test_simulate_errors.py` 3 신설(생성자 회귀·콜사이트 시그니처 전수·
     스트림 detail). 백엔드 재기동 후 **브라우저 실행 성공 실측**(네 케이스
     결과 창, Base +988만원).
2. **P1 핀 행**: `aria-selected`(role=table 행에서 무효 — a11y 크리틱 동시
   해소) → **`aria-current`** 로, CSS `tr[aria-current] > td` = --sr-control
   채움(RV data-on 과 같은 낱말). 틴트 셀 인라인 배경이 위에서 이김(정보 위계).
3. **P1 점프 미스크롤**: 가상화 스크롤러 주인(InstrumentTable)이
   `scrollToIndex(align:'auto')` — 선택 변경/행 등장 순간만, 정렬·필터로는
   재스크롤 없음. 실측: 하단 띠 10Y → scrollTop 340·행 가시.
4. **P1 메가메뉴 스크림 트랩**: 스크림 자체가 nav 자식이라 바깥 클릭이 안
   닿았음 → 스크림 onClick=닫기(덮는 막이 곧 닫기 표면). 실측 닫힘.
5. **커맨드바 은퇴** [OWNER]: CommandBar.tsx·가드 15개·CSS(sr-cmd*·키캡
   sr-navslash)·TopNav "/" 키캡·이벤트 전부 삭제. 이동은 하단 띠 앵커가 승계.
6. **얼라인 [OWNER "가로세로 얼라인 다 맞추기"]** — 실측 후 수리:
   - 상단 우측 유틸리티 줄 28/36 혼재 → **전부 32**(.sr-clog-trigger 28→32·
     .sr-naviconbtn 36→32 — 주석의 토스 실측 32 와 코드가 어긋나 있던 것).
   - 제목 줄 우측 창 트리거 CDS Button size s(36) → **32px pill 문법**
     ("백테스트"·"표로 보기" — RV "설정"·"커브 레인"과 동일). Button 임포트 은퇴.
   - 실측 정돈 확인: 카드 인셋 24·간격 16·오버플로 0 전 화면(시뮬 좌기둥
     31px 골은 스크롤바 15px + gap 16 = 구조적, 유지).
7. 기타: 확대 창 제목 중복(chartOnly 는 이름을 창 제목에 승계·빈 span 으로
   자리 유지)·정렬 헤더 접근 이름 PUA → aria-label 사람 말.
8. 가드 `critique-repairs.test.ts` 8 신설. 프런트 562/562(커맨드바 가드 은퇴
   +신규 8)·tsc·build 클린·dev 재기동·브라우저 전건 실측.

9. **워크북 교체의 여파 — 데이터 핀 3건 파손·수리**(전체 pytest 3 failed →
   580 상당 그린): `test_irsdata_source` 값 핀 2건은 테스트 자기 주석의 절차
   ("refresh → update the number") 그대로 갱신(on_rate 0.02561→0.02556·픽싱
   시작 01-18→01-19); `test_backtest` DV01 갭 단조성은 short 진입일이 절대
   날짜라 파일이 자랄 때마다 지평이 자라던 것 — **`ds.dates[-6]` 상대 핀**으로
   견고화(급등 5거래일이 붙자 short 갭이 mid 를 추월했던 실측).

**남긴 것**: 시뮬 입력 사전 검증 확대·타임아웃 안내(크리틱 #5, harden 소관) ·
워크북 아침 복사 파이프라인(오너) · 그룹 전환 단축키(오너 기각) · 다크 미영속
(보류 확정).

### 8.14 전체 앱 검토 3종 + 활성 컨트롤 문법 통일 (2026-08-19 15차, 568/568·미커밋)

**검토 3종(전부 검토 전용, [OWNER "일단 검토 관점에서만 스킬로서 사용"])**:

1. **impeccable 전체 앱 크리틱** — 35/40, `.impeccable/critique/2026-08-19T04-30-49Z__src-app.md`.
   P1 테마 리로드 미영속·P1? 표 행 보조기술 노출 등은 **[OWNER "전체적인
   디자인 얼라인에 한정"]** 으로 보류. 도발 질문 3(백테스트 명칭·Setting 방·
   Score 색) = **전부 현상 유지** [OWNER].
2. **ui-ux-pro-max** — 유효 매치는 "Active State 한 가지 문법" 일반 원칙뿐
   (활성 컨트롤 혼재 재확인). 나머지 검색 오프토픽, 미채택.
3. **cds-code 감사**(src/rv + 컨트롤 문법) — 발견만, 수리는 별도 판단:
   - **타이포 shorthand 전부 deprecated** — `TextCaption`(190)·`TextLegal`
     (158)·`TextLabel2`(102)·`TextLabel1`(61)·`TextBody`(53)·`TextTitle3`(15)·
     `TextDisplay3`(13)·`TextHeadline`(10) = 602건, d.ts "Use `Text` with
     `font=…` — will be removed in a future major". 시각 무변·기계적 마이그
     레이션이라 **얼라인 아님 → 미수리, 다음 메이저 승급 전 일괄이 정답**.
   - `@coinbase/cds-web` 9.15.0 → 최신 9.17.1 (마이너 2 갭, 승급 = 오너).
   - `RvPage.tsx` Box `style={{marginLeft:'auto'}}` 1건(style-props 룰) —
     경미, 미수리.
   - 색 리터럴·dangerouslySet·잘못된 import 경로 = 0.

**수리(얼라인 한정) — 활성 컨트롤 문법 통일**: 세 방언(네비 회색 알약 /
SegmentedTabs 검정 알약 / **PeriodSelector 코인베이스 블루 wash**) 중 파랑만
어긋남 — 이 앱에서 파랑은 하락 방향의 낱말이라 상승 차트 밑 파란 활성 칩은
"1Y 하락"으로 읽힌다.

1. **고장 발견**: `.sr-spans[data-dir] [aria-selected]` color 룰(부호 합의
   의도)이 **한 번도 작동 안 했다** — 탭 안쪽 span 의 CDS `fgPrimary` atomic
   class 가 자기 color 를 갖고 이김(실측: 버튼은 빨강, 글자는 파랑). wash 는
   인라인 `var(--color-bgPrimaryWash)` 라 스타일시트로 못 이김.
2. **수리 = CDS 변수 재정의**(둘 다 var 를 읽으므로 스코프에서 변수만 바꿈):
   - `.sr-spans` — 기본(flat) `--color-fgPrimary: var(--color-fg)` +
     `--color-bgPrimaryWash: var(--color-bgSecondary)`(네비 회색); up/down 은
     `var(--sr-up/down)` + `color-mix(… 10%, transparent)` 틴트 — **부호 합의
     의도가 처음으로 실제 렌더**(표 틴트 셀과 같은 낱말).
   - `.sr-tabs-neutral` 신설 — Lab 풀/발행체/테너/구도 4무리(부호 없는
     필터·카메라)는 중립 잉크(Surface3D 래퍼 Box 4곳 부착).
3. 실측: Main 활성 1Y = 빨간 글자+빨강 10% 알약(라이트·다크 모두 스킴 변수
   자동 추종, 다크 = 232,108,118), Lab = 회색 알약+잉크 = 네비와 동일. 파랑
   활성 소멸. build→dev 재기동 후 유지 확인.
4. 가드 신설 `guards/active-control-grammar.test.ts`(5) — 변수 3벌 소스핀·
   고장 룰 재출현 금지·Lab 래퍼 수=PeriodSelector 수·Main sr-spans 스코프.
   전체 568/568.

**결과 사전**: 활성 = ①검정 알약(SegmentedTabs·RV pill, 강조) ②회색 알약
(네비·drawer-tab·Lab 셀렉터, 중립) ③방향 틴트(미리보기 기간 — 데이터 부호).
파랑은 다시 하락 전용.

### 8.13 헤지수단 은퇴 + 열 머리 설명 툴팁 (2026-08-19 14차, 562/562·미커밋)

1. **헤지수단 명시 전부 은퇴** [OWNER] — 서브라인("지금 bp"만)·리드아웃 카드
   행·범례, 그리고 **속빈 마커도**(표기가 사라지면 마커는 설명 불가한
   수수께끼) — 점은 잉크 단일. shortable/shortVia 는 서버 계약에 잔존.
   실측: 화면 전체 '헤지수단' 0건.
2. **버퍼·BEP·상대 RV 열 머리 = CDS Tooltip** [OWNER — "커서 대면 친절하고
   간결하게"] — 토스체 설명("3개월 들고 있을 때 캐리와 롤로 버는 폭이에요…"
   류), hover·키보드 포커스 둘 다 열림(CDS 기본), 점선 밑줄+cursor:help 가
   표식(`.sr-rv-thhelp`). 열 3개뿐이라 팝오버 비용 문제없음 — 셀 148개에 안
   달던 그 판단의 반대편. 포털이라 스크롤 컨테이너에 안 잘림(실측).
3. 가드: 속빈 테스트 → 잉크 단일+'헤지수단' 부재 핀, ThHelp 3종 핀.
   **CDS Tooltip 은 ThemeProvider 요구** — 가드 렌더를 sauronTheme 로 감쌈
   (`rtl()` 헬퍼, 새 함정 기록).

### 8.12 최종 라운드 — 오너 4결정 반영 (2026-08-19 13차, 561/561·미커밋)

1. **양방향 연동** [OWNER "추가"] — 점 hover → 표의 그 행 강조(data-on, hover
   와 같은 채움) + `scrollIntoView(nearest)` 로 보이게(RankingTable
   highlightId+rowRefs). 실측: 점 hover 로 39위 행 강조·스크롤 확인.
2. **히어로/미리보기 구성 유지** [OWNER] — 크리틱 질문 ①③ 종결.
3. **사분면 점 = 잉크 중립 복귀** [OWNER "사분면만 중립으로" — 방향색 점이
   옆 모니터의 빨강=주의 사전과 충돌]. 방향은 위치(x=0)와 스크림이 말한다.
   순위변동 ▲적/▼청·히트맵 틴트는 유지. 범례는 속빈 하나로 축소. 가드 갱신.
4. **성분 분해 native title 은퇴** [OWNER] — 사분면 리드아웃 카드가 승계:
   rows 에 z 절대·섹터·커브 / 캐리+롤 추가, **표 행 hover·키보드 포커스**
   (linkbtn onFocus)에도 그 점 위치에 카드가 뜬다. 실측: 점/행/포커스 세 경로
   전부 카드+성분 확인.
