# 인계 — sauron-v2, 2026-08-19 (간격·얼라인 레인, **완료**)

앞 인계는 `HANDOFF-2026-08-19.md`(오너 지시 패스 4차). 그 문서의 §5 "다른 레인과
부딪칠 수 있는 것들" 이 가리키는 **그 다른 레인이 이 문서다.** 시작할 때 그 절을
먼저 읽었고, 거기 적힌 네 경고(13px prop 유지 · 창 폭 1020 · 대사 표 경계는
box-shadow · 타이포 래칫)를 전부 지켰다.

## 0. 지금 상태

| | |
|---|---|
| 게이트 | `pnpm vitest run` **599/599 그린**(exit 0) · `pnpm lint` exit 0(기존 warning 4) · `pnpm build` exit 0 |
| 커밋 | `009f957`(얼라인 수정) · `f88d1de`(가드 둘). 시작 HEAD `9d489cc` → 종료 HEAD `f88d1de` |
| 미커밋 잔여 | `data/irsdata.xlsx` · `.impeccable/live/` — 둘 다 이 레인 것이 아니다(앞 인계와 같은 판단) |
| 확인 방법 | 실행 중인 앱(:3200)에서 CDP `getBoundingClientRect`/`getComputedStyle`/`Range` 잉크 상자. **타이밍은 재지 않았다.** 라이트·다크 양쪽 |
| 진단 전문 | 리포 밖 `scratchpad/REPORT_align_a.md`(측정 표 전부 + 순위 매긴 결함 목록) |

---

## 1. 무엇이 잘못돼 있었나 — 뿌리 하나, 증상 셋

폼 행들은 `alignItems="flex-end"` 다. **바닥이 정렬되는 규칙에서는 블록 높이가 곧
라벨 높이**여서, 높이가 다른 구성원 하나가 자기 라벨을 데리고 위아래로 움직인다.
백테스트 창 실측 라벨 줄: 149 / 155 / 167 — **스프레드 18px.**

| 구성원 | 상자 | 왜 |
|---|---|---|
| Select 종목·방향, date 진입일·청산일 | 32 | `.sr-date` 는 CSS 로 32 를 적어 뒀고, Select 는 `font="legal"` 이 상자까지 32 로 줄였다 |
| **규모 TextInput** | **38** | `fontSize="legal"` 은 글자만 줄인다 — 상자는 CDS `size="s"` 기본값에 남았다 |
| **진입 레벨** | **38**(블록) | 값이 맨살 글자 20px 이라 블록이 짧고, 바닥 정렬에서 라벨만 12px 내려앉았다 |
| **✕** | **28** | `.sr-window-close` 에 height 선언이 없어 내재값(16 lh1 + 6/6) |

### 원인이 기록으로 특정된다

`HANDOFF.md:873` 은 2026-08-14 에 "컨트롤 정렬(Select 와 TextInput 의 보이는 상자
**둘 다 39px**)" 을 *확인했는데 문제 없던 것* 으로 적어 뒀다. 그 뒤 컨트롤 값 13px
패스(`HANDOFF-2026-08-19.md:64-66`)가 `Select`→`font="legal"`,
`TextInput`→`fontSize="legal"` 로 갈랐다. **폰트가 한쪽 상자만 줄였고, 그때 검증된
등고가 그때 비대칭으로 깨졌다.** 아무 테스트도 그걸 몰랐다 — `control-parity` 가
생긴 이유다.

---

## 2. 고친 것 (before → after, 같은 하네스)

| 지표 | before | after |
|---|---|---|
| 라벨 줄 스프레드 | **18px** | **0** |
| 컨트롤 중심선 스프레드 | **5px** | **0** |
| 컨트롤 상자 높이 집합 | {28, 32, 38} | **{32}** |
| ✕ 잉크 중심 이탈 | +1.5px | **−0.5px** |
| 전 표면(백테스트·현금채권·시뮬·RV·Setting) 이탈 행 | 3 | **0** |

1. `TextInput height={32}` — 백테스트·현금채권·시뮬 세 곳. 폰트는 안 건드렸다
   (`control-value-font` 가드가 `fontSize="legal"` 을 요구하고 그건 유지). `height`
   는 TextInput 이 `InputStack` 에서 받는 CDS prop 이다(설치된 dts 로 확인:
   `controls/TextInput.d.ts:13` ← `InputStack.d.ts:34`).
2. 진입 레벨 값 줄을 `HStack height={32} alignItems="center"` 에 담았다 — 컨트롤이
   아닌 값도 컨트롤과 같은 상자에.
3. `.sr-window-close` 에 `height: 32px` + `padding: 0 8px`. **좌우 8 은 그대로
   뒀다** — 머리에서 그 8 이 컨테이너 인셋 8 과 합쳐져 잉크가 가장자리 16 에 서고,
   그게 제목 잉크의 좌측 16 과 이루는 대칭이다(실측).

### 실측이 내 수정을 되돌린 사례 하나 — 기억할 것

Setting 의 조달 스프레드 칸을 32 로 내리자 **오히려 나빠졌다.** 그 칸의 이웃은
Select 가 아니라 **SegmentedTabs 이고 그건 36** 이다:

| 값 | 중심선 이탈 |
|---|---|
| CDS 기본값 38 | 1px |
| 32(앱 기본 등고) | **2px** ← 내 첫 수정 |
| **36** | **0** ← 채택 |

그래서 그 칸은 36 이고, 가드도 "무조건 32" 가 아니라 "**명시된 행 높이**를 지고 그
값이 {32,36} 안" 으로 썼다. **등고는 앱 전역 상수가 아니라 행의 성질이다.**

---

## 3. 새 가드 둘 (둘 다 `src/**` glob, 주석 선제거, 위반 심기 증명 포함)

| 파일 | 무엇을 막나 |
|---|---|
| `guards/control-parity.test.ts` (10) | `size="s"` CDS TextInput 이 명시 높이({32,36})를 지는지 + CSS 가 적는 다섯 컨트롤 높이(`.sr-date`/`.sr-rv-pillbtn`/`.sr-naviconbtn`/`.sr-clog-trigger`/`.sr-window-close`)가 전부 32 인지 |
| `guards/spacing-scale.test.ts` (12) | 계단 {0,2,4,6,8,12,16,24,32} 밖의 간격 값. CDS prop 은 스텝만, CSS px 는 계단 위이거나 **사유가 적힌 예외**만. **쓰이지 않는 예외를 잡는 테스트**도 있다 |

둘 다 "찾지 못해서 통과" 를 막는 자기검증을 넣었다(스코프가 비면 실패, 심은 위반은
잡히고, 주석 속 예시 JSX 와 `var()`/`calc()`/`%`/`auto` 는 안 잡힌다, 속성 안
화살표 함수가 여는 태그를 끊지 않는다).

---

## 4. 손대지 않은 것과 그 이유 — **되짚지 말 것**

| 항목 | 판정 | 근거 |
|---|---|---|
| 행 오른쪽 20.92px 여백 | **설명됨** | 창 폭 1020 이 접힘을 없애고 남은 잔여. `BacktestWindow.tsx:330-344` 의 산술 + `HANDOFF-2026-08-19.md:121` 의 경고("되돌리면 진입 레벨·✕ 가 둘째 줄로") |
| 창 머리 패딩 16/8 | **반박** | CSS 는 비대칭이나 **잉크는 16/16 대칭**. 16/16 으로 "고치면" ✕ 잉크가 24 로 밀려 깨진다 |
| 버튼 행 위 6 / 아래 16 | **BLOCKED** | 그룹 안/사이 경계이고, 산술적 뿌리는 `space['1']`=6 (아래) |
| **`space['1']` = 8→6** | **BLOCKED** [OWNER 2026-08-19: "그대로 둔다"] | `ROW_H = 60` 을 지탱한다(셀 패딩 6+6). 되돌리면 표 전 행이 움직이고 사다리·가상화까지 재검증. 그 대가로 계단이 `4 → 6 → 12` 로 등간이 아니고 **8px 은 prop 으로 도달할 수 없다**(CSS 는 8 을 20번 쓴다) — 두 갈래가 구조적으로 어긋나 있음을 `spacing-scale` 주석에 적어 뒀다 |
| 계단 밖 예외 다섯(10 ×7선택자 · 7 · 5 · 14 · 20) | **사유만 기록** [OWNER: "지금은 그대로"] | 10 은 일곱 표면이 쓰는 사실상의 계단이라 올리면 일곱 곳 동시 재측정. 7 은 랭킹 표 67행의 행 높이를 만드는 값(동시 레인이 방금 맞춘 표) |
| chPx / 사다리 임계 | **반박** | 프로브는 이미 글자를 그리는 노드(`td.sr-num span`, 14px·tabular)에서 잰다. `chPx = 8.60` 이 `columns.ts:211` 기록값과 일치. 남은 10.8% 는 **마침표가 숫자보다 좁은** 산술(마침표당 5.04px)이고 하드스톱이라 기록만 |
| 폭 1280/1440/1743 | **BLOCKED(측정)** [OWNER: "실기기에서 직접"] | `resize_window` 가 성공을 보고하는데 `innerWidth` 가 1920 에서 안 움직이고 `setDeviceMetricsOverride` 미노출. 완화: 창이 `position:fixed; width:1020` 이라 폭 불변(두 번 시도에서 여섯 지표 동일), 테마 전체 미디어 쿼리가 `prefers-reduced-motion` 하나뿐 |
| 타이포 | 손대지 않음 | `typography-ratchet` 천장 251 유지 — shorthand 를 늘리지도 줄이지도 않았다 |
| SUBPIXEL(<1px) | **0건** | 건너뛸 것이 없었다 |

---

## 5. 다음 레인이 알아야 할 함정

1. **`pnpm build` 가 떠 있는 dev 의 `.next` 를 부순다.** 이 세션에서 두 번 밟았다
   (게이트 직후 화면이 스타일 없는 맨 HTML 로 뜬다). 빌드 후에는 dev 재기동.
2. **`resize_window` 는 성공을 보고하면서 아무것도 안 바꿀 수 있다.** viewport 는
   반드시 `innerWidth` 로 되읽어 확인할 것.
3. **바닥 정렬 행에서 높이를 바꾸면 라벨이 따라 움직인다.** 컨트롤 하나의 높이는
   그 칸만의 일이 아니다.
4. **등고 값은 행이 정한다.** 이웃이 SegmentedTabs(36)인지 Select(32)인지 재고
   나서 적을 것. 앱 전역 상수로 밀면 이 세션처럼 1px 나빠진다.
5. **오른쪽 여백·패딩 비대칭은 잉크로 다시 재라.** CSS 대칭 감사만으로는 오탐이
   난다(머리 16/8 이 그 예).
