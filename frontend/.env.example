# Copy to .env.local. Every value here is the built-in default, so an empty
# .env.local behaves exactly like no file at all.
#
# Where the browser sends its API calls. NEXT_PUBLIC_ means it is inlined at
# BUILD time, not read at runtime — a different backend host needs a rebuild.
NEXT_PUBLIC_API_BASE=http://localhost:8100
