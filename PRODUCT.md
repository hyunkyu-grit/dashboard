# sauron-v2 — PRODUCT

- **register**: product (design SERVES the product — 트레이딩 데스크 도구)
- **what**: KRW IRS 모니터 v2 — braveworld(v1)를 Coinbase Design System 으로 다시
  세우는 리포. 표(종목군 9탭) + 미리보기 차트 + 백테스트/시뮬레이션 창 + Lab.
- **who**: 오너(채권 트레이더) 한 사람의 데스크. 인포맥스 터미널·알프스 메신저
  옆에서 하루 종일 떠 있는 화면 — 화려함이 아니라 **한눈에 읽힘**이 가치다.
- **scene**: 4모니터 트레이딩 데스크, 밝은 사무실, 장중. 라이트가 기본이고 다크는
  토글. 페이지는 스크롤하지 않는 100vh 기둥이다.
- **design authority**: `DESIGN.md` (실측 기반 규칙 — 취향이 아니라 측정을 적는다),
  토큰은 `src/theme/direction.css` 한 파일, 활자 규칙 400/600 두 단계.
- **frozen**: 방향색(상승 적 `#de2b39` / 하락 청 `#2171eb`, 토스 실측) · 모션 3시간
  1곡선 · "부호 있는 숫자는 카드 위에만"(대비 실측).

## Lab 「모형」 · 전략 면 — 고른 것과 안 고른 것 [세션 2, 2026-08-21]

- **잔차 처리는 「감쇠」다** [2026-08-24 뒤집혔다]. 여덟 분기 경로가 끝나도 준칙
  잔차 `u_{i,t}` 가 0 으로 안 떨어지고 AR(1) **ρ = 0.801** 로 잦아든다
  (`backend/bigfoot/solve/system.py::RESIDUAL_TAIL`). 화면은 그 이름을 **기저에서
  읽어서** 단다(`scenario_basis.json::residual_tail.treatment`).
  - **예전 「급단절」은 설계가 아니라 빈칸이었다.** 떨어뜨리는 코드가 있어서 0 이
    아니라, 못이 없는 분기의 잔차를 아무도 안 채워서 0 이었다. 같은 잔차의 역사
    자기상관이 0.801(Newey-West 표준오차 0.0745, 2000Q1–2026Q2 n=106)이라
    ρ=0 은 추정 밴드에서 10σ 밖이다.
  - **화면 숫자가 절반이 됐다.** 지속 −25×8 의 지평 이탈 +27.8bp → **+14.8bp**,
    12개월 3Y +11.69 → +8.11bp(준칙 몫 33% → 4.5%). 완화 쪽으로 벗어난 잔차가
    경로가 끝나는 순간 증발하지 않고 준칙을 붙잡는다.
  - **되돌릴 수 있다.** `solve(tail_rho=0)` 이 예전 급단절을 바이트 단위로
    복원한다. ρ 는 우리가 잰 값이지 논문이 박은 값이 아니고, 그 사실이 방법 면
    원장의 `residual-tail` 행에 「어떻게 틀릴 수 있나」로 적혀 있다.
  - **새 한계**: `policy_q8` 기저가 24분기 안에서 안 잦아든다(`|q24|/max` i_kr
    0.248→0.427). T 를 늘릴지는 아직 안 정했다.

- **진입·목표·손절을 안 적는다.** 방향·크기·수렴 지평까지가 이 물건이 아는 것의
  끝이다. 한 번 빗나가는 날 잃는 신뢰가 「모형이 뭘 보는가」를 정직하게 말해서 번
  것이라, 그걸 담보로 잡지 않는다.
- **10년은 트레이드 후보에 없다.** IRS 커브가 10Y 에서 끝나 1Y×10Y 포워드가 커브
  밖이고, 백엔드가 외삽을 거절한다. 「2s10s 스티프너」는 시장 대비 숫자를 못 단다.
- **노트 내보내기는 해요체다** — 데스크 노트 문체 관례가 이 리포에 없다.
  오너 답이 오면 `note.ts` 의 문장 조립 한 자리만 바꾼다.

### 오너 결정 셋 [2026-08-21]

- **노트 내보내기는 해요체다.** 앱과 같은 목소리로 둔다. 데스크용 합니다체 판은
  안 만든다 — 필요해지면 `note.ts` 한 자리다.
- **「함의」 줄은 3년 고정이다.** 자동 선택(=|모형 − 시장| 최대)은 동결 경로에서
  캐리가 제일 큰 1Y 를 매번 뽑아, 헤드라인이 «오늘 뭐가 제일 싼가» 가 아니라
  «어디 캐리가 제일 센가» 가 됐다. 가장 큰 기회는 아래 트레이드 줄이 말한다.
- **「함의」의 기준축은 «12개월 캐리 대비» 다.** 「오늘 리치/치퍼」(= 기저가 버린
  `cdavg(0)`)는 **부호가 반대**이고 — 지속 −25×8 의 3년이 오늘 −19.6bp / 12개월
  +11.7bp — 엔진 재굽기를 요구한다. 그 판이 필요해지면 별개 레인이다.
