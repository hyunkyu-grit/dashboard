# 테마 작업 핸드오프 프롬프트

> **이 문서는 지시문이지 현황판이 아닙니다.** 아래 본문은 2026-08-07 에 오너가
> 준 브리프 그대로이고, 그 안의 "현재 main = `a63684a`, 게이트 전부 초록" 같은
> 문장은 **그때의 사실**입니다. 지금 상태는 이 머리말만 보세요.
>
> ## 어디까지 됐나 (2026-08-07, `44647f4`)
>
> 끝난 것:
>
> - **좌측 사이드바** — 가로 탭 스트립을 대체했습니다. 폭 240, 행 32(킷
>   Medium; 이 제품 본문이 13px 이라 목업의 Large 40 대신). 그룹 둘 —
>   종목군 6 · 도구 2. 배지는 종목군만, 글리프는 다리 수 점.
>   → `ui/Sidebar.tsx`, 정의는 `ui/tabs.ts`
> - **툴바** — 높이 52, 격자 행이 아니라 뜬 레이어(`Z_TOOLBAR`), Liquid Glass.
> - **거터 80 → 24** (`ui/pageGutter.ts` + `sim/ui/layout.ts`).
> - **액센트가 실제로 칠해집니다** — 기본 버튼·사이드바 선택·포커스 링·
>   백테스트 실행·세그먼티드. 라벨은 전부 `--bw-on-accent`.
> - **자유도** — 시뮬레이션 상품 선택기 106개를 주요/전체 `<optgroup>` 으로,
>   목표 변동 bp 에 스테퍼.
> - **문서** — `DESIGN.md` §2·§3·거터, `sim/ui/HoverPanel.tsx` 갱신.
>
> 아직 안 된 것:
>
> 1. **유리 밑을 지나가는 것이 없습니다.** 사이드바는 항목 여덟 개라 스크롤이
>    안 생기고 본문 기둥은 위쪽을 바 높이만큼 비웁니다 — 유리가 합성하는 것이
>    움직이는 콘텐츠가 아니라 바탕면이라, 아래 본문이 경계하는 "장식이 된
>    유리" 가 지금 상태입니다. 표의 스크롤 컨테이너를 바 밑에서 시작시키려면
>    그 위의 고정 머리(스크리너 칩)를 어떻게 할지 먼저 정해야 합니다.
> 2. **시뮬레이션 날짜 두 칸**은 자유 입력 그대로입니다. "워크북에 있는 날만
>    유효" 를 강제하려면 유효일 집합을 내려주는 엔드포인트가 필요합니다.
> 3. **워드마크가 창 제목 자리**에 있습니다(아래 부록 5번). 목업도 같아서
>    남겨 뒀습니다 — 오너 판단.
> 4. `theme/kit.generated.css` 은퇴가 절반입니다. `theme/kit.css`(벤더링)가
>    들어왔지만 `.kit-*` 클래스 네 군데가 아직 옛 파일을 씁니다.
>
> 아래 본문에서 **이미 반영된 지시**: 폐기 확정 표 전부, 배경·머티리얼 토큰,
> 사이드바, 상품 선택기, bp 스테퍼, 가드 고쳐 쓰기.
> **아직 유효한 지시**: 판단 기준 두 가지(의미에 맞는 컨트롤 · 자유도를 낮추는
> 방향), 범위 제한(표면만), 함정 셋, 실행·게이트, "눈으로 봐야 합니다".

아래를 그대로 붙여넣으세요. 새 세션이든, 이미 돌고 있는 세션이든 됩니다.

---

`C:\Users\infomax\Desktop\Assistant\Projects_AS\braveworld` (Sauron, KRW IRS
모니터 · `:3100` 프론트 / `:8100` 백엔드 · Next 16 + Tailwind 4)의 **표면만**
목업에 맞춰 주세요.

```
기준 목업   C:\Users\infomax\Desktop\Codex\mockups\defense.html   ← 이쪽이 의도에 가장 가깝다 [OWNER]
참고        C:\Users\infomax\Desktop\Codex\mockups\sauron.html    ← Sauron 내용으로 같은 언어를 쓴 예
킷          C:\Users\infomax\Desktop\Codex\designcodex\           (완성, 자체 게이트 12개 통과)
붙이는 절차  designcodex/docs/USAGE.md
```

두 목업은 같은 시스템입니다. `:root` 토큰 공통 72개가 값까지 전부 같고
충돌이 0개입니다. 액센트도 둘 다 `#FF8D28` / `--accent-fg: #B85E00`.

## 판단 기준 [OWNER] — 이게 이 일의 척추입니다

유리와 사이드바는 수단이고, 실제로 보는 것은 **"이 자리에 맞는 컴포넌트를
쓰고 있는가"**입니다. 두 가지로 갈립니다.

**1. 의미에 맞는 컨트롤인가.** 킷은 컨트롤마다 뜻이 다릅니다. 이 리포는 이미
그 구분을 한 번 겪었고 `ui/BacktestWindow.tsx`에 적어 뒀습니다 —

> `<select>`는 킷에서 **Pop-up Button**이지 텍스트 필드가 아니다. 둘은 모양을
> 공유하지 않는다: 팝업은 셰브런을 달고 필드는 안 단다. 캡슐도 아니다 —
> Pop-up Buttons 3 Rg(120×24)는 라운드 **사각형**이고 4 Lg(120×28)가 첫
> 캡슐이다. 모든 컨트롤을 캡슐로 만든 패스는 28/36 아트보드만 보고 규칙이라
> 불렀다.

같은 눈으로 전체를 훑으세요. 세그먼티드여야 할 자리에 버튼이, 스테퍼여야 할
자리에 자유 입력이, 팝업 버튼이어야 할 자리에 텍스트 필드가 있는지.

**2. 자유도를 낮추는 방향인가.** 과하게 구성하지 말고, 사용자가 틀릴 수 있는
칸을 줄이세요. 지금 확실히 걸리는 자리:

- **시뮬레이션 상품 선택기 — 옵션 106개**(아웃라이트 13 · 스프레드 28 ·
  **버터플라이 56** · 포워드 9)를 네이티브 `<select>` 하나에 쏟아붓고 있습니다.
  56개를 스크롤해서 고르는 건 고르는 게 아닙니다. 이 리포에는 이미 큐레이션
  장치가 있습니다 — `backend/app/derive.py`의 **주요 세트**
  (`KEY_OUTRIGHTS` `KEY_SPREADS` `KEY_FLIES`)와 각 탭의 `주요`/`전체` 분리.
  같은 규율을 여기 적용하세요.
- **`ConfigureStage`의 자유 입력 8개**(날짜 `Input` 4 + `NumberField` 4).
  날짜는 워크북에 있는 날만 유효한데 자유 입력입니다. bp 목표는 스테퍼가
  맞는 자리일 수 있습니다.

자유도를 줄이는 것이 기능을 줄이는 것과 다릅니다. 할 수 있는 일은 그대로
두되, **틀린 값을 넣을 방법을 없애는** 쪽입니다.

## 범위 — 표면만. 화면 재설계 아님 [OWNER]

**정보 구조는 그대로 둡니다.** 지금 Sauron이 전달하는 정보와 그 배치는
정확합니다. 바꾸는 것은 그것이 **보이는 방식**뿐입니다.

가져올 것:

1. **배경·머티리얼** — 이게 가장 중요합니다.
   ```
   --glass-bar    light rgb(255 255 255 / .7)    dark rgb(26 26 26 / .5)
   --glass-side   light rgb(250 250 250 / .8)    dark rgb(12 12 12 / .85)
   --glass-menu   light rgb(230 230 230 / .55)   dark rgb(90 90 90 / .5)
   --glass-edge   light rgb(0 0 0 / .12)         dark rgb(255 255 255 / .14)
   --blur         40px
   --fill-1/-2    rgb(0 0 0 / .1) · rgb(0 0 0 / .08)   (다크는 흰색 같은 알파)
   --sep          light rgb(60 60 67 / .29)      dark rgb(255 255 255 / .2)
   ```
   `backdrop-filter` 적용처: `.toolbar` `.sidebar` `.popover`는
   `blur(40px) saturate(180%)`, `.menu` `.desk-card`는 `blur(40px)`,
   `.notif` 28px, `.alert` 40px.

2. **좌측 사이드바** — 가로 탭 스트립을 대체합니다.
   `--sidebar: 240px` · `--sb-header: 20px` · `--sb-item: 40px`.
   그룹은 `종목군`(전체·아웃라이트·스프레드·버터플라이·포워드·변동성)과
   `도구`(시뮬레이션·연구실). defense처럼 항목에 카운트 배지를 달지는
   **판단해서 제안**하세요 — 그 자리에 맞는 사실이 있는지 보고.

3. **인터랙션의 표현** — 행을 고르면 그 종목의 팝오버가 열리는 감각.
   **새 메커니즘을 만들지 마세요.** Sauron에는 이미 hover→미리보기,
   pin→팝업(`PreviewPane`, `EnlargedView`)이 있습니다. defense의
   `.popover`(유리 + `blur(40px) saturate(180%)`) 처리를 그 위에 입히는
   일입니다. 선택 토글·포커스 링·호버 밴드도 같은 결로.

가져오지 않을 것 [OWNER]:

- **주장 문장**(defense의 "수주잔고는 8분기 연속 늘었지만…") — 필요 없음
- **KPI 우상단 요약** — 필요 없음
- 화면 재설계 일반. 열 구성·탭 내용·표 구조는 지금 것이 맞습니다.

defense의 필터 바·스파크라인·배지·버블 차트는 **위 세 항목에 필요한
만큼만** 참고하세요. 그 자체가 목표가 아닙니다.

## 폐기 확정 [OWNER, 2026-08-07]

`DESIGN.md`·`CLAUDE.md`에 `[OWNER]`로 박혀 있지만 목업이 이깁니다.
조용히 어기지 말고 문서도 같이 갱신하세요.

| 폐기 | 박혀 있는 곳 |
| --- | --- |
| 액센트 은퇴(palette cut) · "빨강 하나 파랑 하나" | `globals.css` §9, `guards/palette.test.ts` |
| "이 셸은 머티리얼을 안 쓴다" | `sim/ui/HoverPanel.tsx` |
| list-first 가로 탭 스트립 | `DESIGN.md` §2 |

목업의 주황은 킷의 `System Colors / 2 Orange` 값 그대로입니다
(`designcodex/dist/kit.json` 대조 확인). **목업을 입히는 것 = designcodex를
붙이는 것**이라 따로 하지 마세요. 현재 `theme/kit.generated.css`(199줄)는
재생성 도구가 리포에 없는 고아이고 `designcodex/dist/kit.css`가 대신합니다.

**오너에게 물어야 할 것 하나**: 목업은 커브 선을 주황으로 칠하는데 이 제품의
빨강/파랑은 부호(상승/하락)를 뜻합니다. 액센트를 크롬에만 쓸지, 데이터에도
쓸지 — 후자면 §5 채널 예산을 다시 짜야 합니다. 임의로 정하지 마세요.

## 시뮬레이션 탭도 같은 규칙으로

`src/sim/**`도 같이 갑니다. `sauron.html`에는 시뮬레이션 화면이 없으므로
(사이드바 항목 하나뿐) 컨트롤은 `defense.html`에서 가져오세요 — select 36 ·
세그먼티드 50 · 스테퍼 15 · 슬라이더 17 · 입력칸 25 · 버튼 35가 이미 그
언어로 그려져 있습니다. defense에만 있는 토큰 8개가 그 몫입니다:
`--stepper-w` `--stepper-nf` `--switch` `--alert-w` `--alert-btn`
`--notif-w` `--notif-h` `--tb-seg4`.

**새 패턴을 만들지 말고 있는 것을 조합하세요.** 만들었다면 `[OWNER]`로
표시하고 이유를 남기세요. 여기도 화면 재설계가 아니라 표면 교체입니다.

주의: 시뮬레이션은 왼쪽 폼 `w-[520px]` + 오른쪽 미리보기 2단입니다.
사이드바 240px가 들어오면 가로 예산이 줄어드니 폼 폭을 다시 재세요.

## 함정 셋

- **캔버스는 `var()`를 못 읽습니다.** 차트 색은 `theme/bridge.ts`를 거쳐야
  하고, SVG에 요소마다 `var()`를 박으면 컴포지터가 멈춥니다.
- **Tailwind는 소스 텍스트를 읽습니다.** 클래스가 컴포넌트에 문자 그대로
  있어야 하고 런타임 조립은 규칙이 생성되지 않습니다. 토큰을 바꾼 뒤엔
  **빌드된 CSS를 확인**하세요. `globals.css`의 `@theme`에 이름이 없으면
  `text-ink-2`는 조용히 아무것도 안 만듭니다.
- **`theme/ramp.ts`가 `tokens.css`의 미러**입니다. `src/sim/theme/ramp.ts`에
  **두 번째 EDGE_OPACITY**가 있어 둘 다 고쳐야 `ramp-sync`가 통과합니다.

## 가드

`palette` · `band-hue-contrast` · `no-raw-hex`(`kit.generated.css`가
허용목록에 있음) · `tint-contrast`가 걸립니다. **지우지 말고 새 규칙으로
고쳐 쓰세요** — 실측 근거를 들고 있습니다. 유리 위에 얹히는 텍스트는 대비가
뒤에 무엇이 있느냐로 달라지니 **재서** 주석에 남기세요(`#f04452`가
3.71:1로 기각된 전례가 있습니다).

## 실행 / 게이트

```bash
cd backend  && python -m uvicorn app.main:app --port 8100
cd frontend && pnpm dev -p 3100          # 기본이 :3000이라 -p 필수

cd backend  && python -m pytest tests -q                     # 317 통과가 기준선
cd frontend && pnpm vitest run && pnpm lint && pnpm build    # 706 / 0 / 0
```

`pnpm lint`·`build`는 성공해도 stderr에 씁니다. **파이프에 물리지 말고 종료
코드를 직접 읽으세요.** 현재 `main` = `a63684a`, 게이트 전부 초록.

## 눈으로 봐야 합니다

**헤드리스 금지.** 직전에도 모든 게이트가 초록인 채 시뮬레이션 탭이 통째로
에러 경계에 잡혀 있었습니다(무한 렌더 루프 — 타입도 테스트도 못 잡음).
목업과 `localhost:3100`을 나란히 띄우고 라이트·다크 둘 다 확인하세요.
**유리는 정지 스크린샷으로 판단이 안 됩니다** — 뒤에 무엇이 지나가느냐로
보이는 게 달라지니 스크롤하면서 보세요. 목업 HTML은 `<meta charset>`이 없어
서버로 띄우면 한글이 깨집니다. 파일로 직접 여세요.

## 하지 말 것

- `designcodex/`의 생성물(`data/` `dist/` `Design.MD`)을 손으로 고치지 마세요.
- **기능·데이터·백엔드·정보구조는 건드리지 마세요.** 이번 일은 표면입니다.
- `git push origin`은 오너 권한입니다. 미러(`git push mirror main`)까지만.
- 완료 보고에는 커밋 해시와 `git show --stat`을 붙이세요.

---

# 부록 — 킷 원문(Design.MD)을 읽고 나온 정정

`Codex/designcodex/Design.MD` 5,471줄을 읽은 뒤 위 프롬프트에 더해야 할 것들.
이미 돌고 있는 세션에는 이 블록만 던지면 된다.

```
추가/정정: 킷 원문(C:\Users\infomax\Desktop\Codex\designcodex\Design.MD)을
읽고 확인한 것들입니다. 목업만 보고 가면 어긋나는 자리가 있습니다.

1. 유리는 기능 레이어 전용입니다 (§6.1).
   HIG 원문 "Don't use Liquid Glass in the content layer." 툴바·사이드바·
   팝오버·메뉴는 맞고 본문 카드는 아닙니다. "아껴 쓸 것 — 가장 중요한 기능
   요소로 제한"도 명시돼 있습니다.
   ⚠ 목업의 `.desk-card`가 blur(40px)를 씁니다. 그건 콘텐츠 레이어이고
   HIG 이탈로 보입니다. 목업을 그대로 베끼면 그 이탈도 같이 들어옵니다.
   본문 카드에는 `Materials` 5단계(Ultra Thin~Ultra Thick)를 쓰거나
   불투명하게 두세요.

2. 주황은 stroke에만. 글자에는 쓰지 마세요 (§6.2).
   킷 문서가 직접 경고합니다 — System Colors를 글자색으로 쓰면 흰 배경에서
   대비가 모자랍니다. 8 Blue #0088FF = 3.52:1, 1 Red #FF383C = 3.57:1로
   13px 본문 기준 4.5:1 미달. 주황 #FF8D28은 더 밝아 더 낮습니다.
   비텍스트 요소·큰 굵은 글씨는 3:1로 충분하므로 **커브 선(stroke)은 괜찮고
   숫자·라벨은 안 됩니다.** 이것이 "액센트를 데이터에 쓸까" 질문의 답입니다:
   선은 되고 글자는 안 됩니다. 부호(빨강/파랑)는 글자에 쓰이므로 그대로 둡니다.

3. increased contrast 지원을 잃지 마세요 (§6.2).
   HIG는 light·dark·increased contrast 세 벌을 요구하는데 **킷은 두 벌만**
   내보냅니다. braveworld는 이미 `prefers-contrast: more`를 지원합니다
   (tokens.css 하단). 킷을 그대로 받으면 지금 있는 것을 잃습니다. 유지하세요.

4. 사이드바 행 높이는 사용자 설정입니다 (§6.5).
   킷은 small(24)/medium(32)/large(40) 세 벌을 주고, 문서가 "하나만 골라
   박으면 사용자 설정을 무시하는 것"이라고 적어 뒀습니다. 목업은 40 하나로
   고정했습니다. 미결로 남아 있던 "행 높이 48 vs 킷 32"가 바로 이
   이야기입니다 — 32는 medium입니다. 어떻게 할지 정하고 근거를 남기세요.

5. 앱 이름을 창 제목으로 쓰지 마세요 (§6.4).
   HIG "Don't title windows with your app name." 지금 헤더가 `Sauron / KRW
   IRS` 워드마크이고 목업도 같습니다. 제목은 그 뷰가 무엇인지 말해야 합니다.
   툴바 규칙 몇 개 더: 세 영역(leading/center/trailing), 그룹 최대 3개,
   베젤 없음, 배경 대신 ScrollEdgeEffect, 주요 동작은 trailing에 하나만.

6. blur·코너 반경·blend mode는 킷 값이 아닙니다 (§7 정합성 메모 5).
   DTCG export 대상이 아니라 토큰에 존재하지 않습니다. 목업의 --blur: 40px과
   모든 반경은 제품이 정한 값이지 킷 권위가 없습니다. 바꿔도 되고, 바꾸면
   [OWNER]로 남기면 됩니다.

7. 서체는 Pretendard 그대로 둡니다.
   킷은 SF Pro 한 종류지만 이 제품은 한글이 본문입니다. 킷의 텍스트 스타일
   67개에서 가져올 것은 **크기·굵기·행간**이고 서체는 아닙니다.
   참고: 킷 기본 본문 13px(06 Body), 최소 10px, 그 아래는 HIG 이탈.

8. 창 신호등을 넣는다면 킷 값을 쓰세요 (§1.1).
   킷 #FF5C60/#FAC800/#35C759 는 실제 macOS #FF5F57/#FEBC2E/#28C840 와
   미묘하게 다릅니다. 문서가 "기억으로 쓰면 틀린다"고 적어 뒀습니다.
   Active/Inactive 두 벌뿐이고, 비활성 창 호버는 세 번째 벌이 아니라
   그 두 벌을 다시 쓰는 것입니다.

정정 — 앞서 "목업을 입히는 것 = designcodex를 붙이는 것"이라고 했는데
절반만 맞습니다. 목업은 킷의 **색 값**을 정확히 썼지만(주황 #FF8D28/#FF9230는
System Colors 2 Orange 그대로), 위 1번과 4번은 킷/HIG를 벗어납니다.
목업은 참조이지 판정 기준이 아닙니다. 어긋나면 킷 원문이 이깁니다.

참고: Design.MD는 킷 전량이 아닙니다 (§1.1). 심볼 안에 직접 칠해진 채움
2,458건(고유 139색, 그중 49색은 토큰에 없음)이 빠져 있고, 문서가 그 49색
상당수는 작업용 찌꺼기(#D8D8D8 217건 자리표시, #FF0000 30% 49건 레드라인)라
그대로 쓰지 말라고 경고합니다.
```
