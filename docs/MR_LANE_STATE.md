# Mean Reversion 레인 — 통합 밴드 워치 (2026-09-01)

이 문서는 **다음 세션이 붙여 넣을 프롬프트**다. 위쪽이 프롬프트, 아래쪽이
「오늘 알아낸 것」이다. 공용 기계(커밋 규약·게이트·차트 부품)는
`docs/CHART_LANE_STATE.md` 가 진다 — 여기는 **MR 전략 창**만 적는다.

---

# 프롬프트 (여기부터 복사)

## 0. 첫 3분

```bash
cd "C:\Users\infomax\Projects\apps\sauron-v2"
git log --oneline -6
git status --porcelain          # 남의 미커밋을 눈으로 센다
curl -s localhost:8200/api/health && curl -s -o /dev/null -w " dev=%{http_code}\n" localhost:3200/
```

백엔드는 **뜰 때 한 번만 SQL 을 읽는다.** `backend/app/` 을 고쳤으면
`Start-ScheduledTask SauronV2Backend`.

## 1. 원격이 **둘**이다 — 이것부터

```
origin      github.com/wwoo1116-cell/rateslab.git     ← 배포(Vercel). 이 레인의 집
dashboard   github.com/hyunkyu-grit/dashboard.git     ← 같이 밀어 온 사본
```

**2026-09-02 기준**(머리는 `git log --oneline -1` 로 본다 — 여기 해시를 박으면 하루에 여섯 번 낡는다) [OWNER "푸시하고
띄워보자"] — 이 레인 다섯(다리 레벨·pv01·대사 수리 셋·인계문·디자인 통일)이
두 원격에 다 나갔고 Vercel 새 빌드를 실측했다(마커 «거래 줄을 누르면 하루씩
대사가 서요» in page 청크, 12:46). 앞선 회차(08:45)에는 q1 레인 둘이 오너의
정렬 지시로 같이 나갔다.

**배포 순서도 지켜졌다**: 프런트 푸시 → 백엔드 재기동(§6 ⑥ — 새 필드
`principal`·`govt/irs/cd` 가 있어 구 백엔드면 화면이 조용히 빈다). 프로덕션
경로 실측: principal 되곱 1,000,000.00 · 다리 레벨 3,017/3,017 · 보드 fut
defn 「벤더 직독」 · 타임스탑×σ=0 라우트 200.

**밀기 전에는 늘 다시 재라** — 아래 규율이 그대로 산다. 이 리포는 푸시가 곧 Vercel 배포이므로, 미는 것은
「내 커밋을 배포한다」가 아니라 「남의 레인 둘도 같이 배포한다」다 — 오너 결정
이고, 그쪽 레인의 게이트 상태를 모른 채 밀지 마라. 「나갈 것」을 잴 때는
**밀 대상 원격의 참조**로 재라 — `git remote -v` 를 자르지 말고 전부 읽어라. 한 번
`origin/main`(딴 리포)으로 재고 `dashboard` 에 밀어서 4개인 줄 알았던 것이
29개 나간 적이 있다.

```bash
git push origin master:main && git push dashboard master:main
```

## 2. 커밋

`git commit --only -- <경로들>` 만 쓴다. 새 파일은 `git add -- <경로>` 로 하나씩.
bare `git commit` · `git add -A` · `git stash` 금지. 전후로
`git diff --cached --name-only` 와 `git show --stat HEAD` 를 보고에 남긴다.

**남의 레인 미커밋**(2026-08-27 기준): `CLAUDE.md` · `src/app/layout.tsx` ·
`src/theme/direction.css` · `src/terminal/` · `src/app/terminal/` ·
`src/theme/terminal.css` · `guards/terminal-keyboard.test.ts` · `mrs.json` ·
`backend/data/raw/bigfoot_*.csv`. **읽지도 고치지도 않는다.**

## 3. 게이트

```bash
npx tsc --noEmit -p tsconfig.json
npx eslint src guards
npx vitest run
cd backend && python -m pytest tests/ -q --ignore=tests/test_static_agreement.py
NEXT_PUBLIC_API_BASE=https://e110430.tailc7b701.ts.net/v2 npx next build
```

**⚠ `next build` 는 dev 서버의 `.next` 를 덮는다.** 돌릴 거면 dev 를 내리고,
끝나면 다시 띄운다(`CHART_LANE_STATE.md` §4 에 순서). 한 세션에 네 번 밟았다.

**⚠ 백엔드를 재기동하면 모형 아티팩트 7개가 드리프트한다** —
`backend/output/{assumptions,engine_status,scenario_basis}.json` ·
`src/lab/model/artifacts/*.json` · `src/lab/scenario/basis.json`. 남의 레인
것이라 **커밋하지 말고** `git checkout --` 로 되돌린다. 안 되돌리면
`scenario-parity`·`model-strategy-note` 가 빨갛다.

**이미 빨간 것(내 결함 아님):** `test_cashbond` 4건(하드코딩 날짜) ·
`test_issuance`·`test_issuance_mp` 2건(금통위 날짜) · `production-env` 2건
(`.next` 가 dev 산출물일 때만).

기준(2026-09-02 실측 · HEAD 검사 뒤): 프런트 **1,622 통과 · 1 skip · 101파일**
(신설 `mr-span` 11 · `mr-canon` 27 · `shared-controls` 11) · 백엔드 **987 통과 ·
7 skip · 1 xfail** · 캐시 **v14**. ⚠ `next build` 를 밟으면 `production-env` 둘도
초록이다 — 그 둘이 빨간 것은 `.next` 가 dev 산출물일 때뿐이다.
빨간 것은 `.next` 가 dev 산출물일 때뿐이다(이번에 확인). `next build` 뒤에는 위 「이미 빨간 것」이 전부 그린이다(실측: 그날
test_cashbond·test_issuance 도 통과했다 — 하드코딩 날짜가 아직 안 지났다).

## 4. 이 레인의 부품

```
backend/app/mrregime.py     레짐 필터·동적 비용. **미래를 안 보는 것이 전부**
                            (확장 창 백분위). 화면과 연구 스크립트가 같은 것을
                            쓴다 — 두 벌이면 수가 갈릴 때 판정할 자료가 없다.
backend/scripts/mr_live_wfo.py    전진분석 기계(사전등록이 파일 머리에)
backend/scripts/mr_live_report.py 표를 찍는다 — `--all` 로 전 만기
backend/app/mrbacktest.py   PMS 이식 엔진. **적합성 벡터로 잠겨 있다**
                            (tests/test_mrbacktest.py, KPI 15건). 규칙을 바꾸면
                            거기서 터진다 — 새 항은 **선택 인자**로 넣어라.
backend/app/mrcarry.py      캐리(2026-08-27 신설). 계열별 산술·단위 함정·항등검사
backend/app/mr.py           계열 목록·방향(DIR_LEGS)·거래 가능 방향(TRADABLE_DIRS)
backend/app/mrbook.py       **BSS 테너 통합**(2026-09-01). 합치기만 한다 — 규칙은
                            엔진 것이다. `watch`(보드 한 줄) + `aggregate`(장부)
backend/app/main.py         `_mr_leg` = 계열 하나의 준비+시뮬 **공용 자리**.
                            `/api/mr/strategy` 와 `/api/mr/book` 이 같이 쓴다 —
                            통합의 수가 낱개 아홉의 합과 갈릴 자리를 안 만든다
src/mr/StrategyWindow.tsx   전략 실험 창. 거래 표 · 일별 대사(창 바닥 서랍) ·
                            표시 구간 탭 (이웃 칸·진단은 2026-09-02 에 내렸다)
src/mr/BookWindow.tsx       통합 장부 창. 만기별 성적 · 모은 거래 · 동시 다리 수
src/mr/KnobBar.tsx          노브 **한 줄** + stale 판정 — **두 창이 같이 쓴다**
src/mr/parts.tsx            패널 상자 · 청산 사유의 우리말 · 짧은 날짜 ·
                            `headFont()`(표 머리 활자 기준)
src/mr/api.ts               타입
```

## 5. 지금 상태

- **머리는 §1 이 진다** — 이 절에 해시를 또 적지 않는다(한 문서가 머리를 세
  값으로 말한 적이 있다. 2026-09-02 검사).
- 캐리가 **기본 켜짐**(`carry=true`). `carry=false` 면 원본 PMS 수 그대로.
- 거래 줄을 누르면 **일별 대사**가 열린다. 2026-08-28 에 다시 세웠다 —
  열은 날짜·구분·z·Δ(bp)·감도·평가·캐리·비용·그날·누적이고 **감도 × Δ = 평가**
  가 한 줄 안에서 닫힌다(감도 = `hold` × 명목). 그 열이 아래 「단위」 결함을
  첫 줄에서 드러냈다.
- **진입 규칙이 노브다**(`entryMode`): 「이탈 즉시」(원본 PMS·기본) ·
  「밴드 복귀」. 규칙 전문은 `mrbacktest._entry_signal`.
- 차트 셋이 사건 지도를 같이 읽는다 — 진입 점(방향색)·청산(뮤트)·손절(하락색),
  진입 세로선, 펴 놓은 거래만 청산선까지 잉크.

- 청산 사유가 다섯이다: 손절 > 청산 > 역신호 > 타임스탑, 그리고 상태로서의
  미청산. **우선순위가 곧 이름**이고 화면 어휘는 `WHY_WORD` 한 곳이다.
- **BSS 테너 통합 밴드 워치가 생겼다**(2026-09-01, 아래 절). 보드 랭킹 **아래**에
  고정된 한 줄과 `/api/mr/book`, 그리고 통합 장부 창.
- **표시 구간 탭이 생겼다**(2026-09-02) [OWNER — "지난 한달, 지난 한분기, 지난
  1년칸을 신설"]. 전체·1년·1분기·1개월이 차트 넷과 거래 표의 **표시만** 날짜로
  자른다 — 백테스트는 늘 전체라 stale 을 안 세우고(warnZ 규율) 성과·진단은
  안 바뀐다. 누적은 구간 시작 0 재기준이라 마지막 점이 곧 구간 순손익이고,
  패널 머리가 «구간 순 · 걸친 거래(청산이 구간 안) · 전체 순» 을 병기한다.
  고르개는 캐논(`PeriodSelector` + `.sr-tabs-neutral`). 낱개 창만이다(오너 답).
- **거래 한 줄이 스스로 검산이 된다**(2026-09-02) [OWNER — "직접 대사가
  가능하므로"]. 거래 표에 일련번호(전체 실행 기준·구간을 잘라도 불변)·진입/청산
  레벨·Δ(bp), 일별 대사에 레벨 열(합계는 진입→청산). 명목(₩/bp)·**액면(억)**은
  모든 줄에 같은 수라 머리·조건 카드·각주에 선다 — 액면은 서버 신설 필드
  `principal`(캐리의 그 식 `명목/(pv01×1e-4)`, 지금 커브 하나의 **근사**)이고
  선물은 null(원금 없음), 구 백엔드(`undefined`)에선 각주가 조용히 빈다.
  bp 레벨은 **2자리**(`fmtReconLevel`) — Δ 가 2자리라 1자리면 «청산−진입=Δ» 가
  표시 정밀도에서 깨진다(krw.ts 의 그 1만원 판례). 가드 `guards/mr-span.test.ts`.
- **실전 운용 규칙 다섯이 화면에서 내려갔다** [OWNER 2026-09-02 — "실전 규칙
  다섯은 일단 없애서 기억만 해두는 걸로"]. 타임스탑·레짐 필터·비용 모델·역신호
  청산·미청산 계상 — 긴 표본에서 **기각된 노브**다(§긴 표본 판정 ①: 다섯 전부
  켠 포트폴리오가 SR 1.60 → 1.01). **엔진·계약·기본값은 그대로**이고 화면의
  손잡이만 없앴다(`KnobBar` 그 자리 주석에 되살리는 법). 기각된 노브를 화면에
  두면 「켜 보면 좋아질까」를 화면이 계속 묻는데 답은 이미 측정돼 있다.
- **거래 표가 창 밖으로 넘치는 것은 정상이다** [OWNER 2026-09-02 — "거래표는
  넘치는게 정상이긴 해"]. 실측 표 1,140px 대 창 안폭 1,056px — 열 14~15개는
  대사에 필요한 것이고 가로 스크롤이 그 값이다. **고치지 마라**(감사가 「폭
  예산 초과」로 올렸을 때 이 줄을 근거로 닫는다).
- 게이트: 프런트 **1,615·101파일**(production-env 는 `next build` 뒤) · 백엔드 **987**.

## 5½. 디자인은 Main·Backtest 문법이다 (2026-09-02) [OWNER — "이제 디자인 구성도 Main이나 Backtest와 통일해주세요"]

감사(3에이전트, 읽기 전용)가 어긋남 **23건**을 실측했고 그중 **7건은 「의도된
이탈」**(머리 주석에 근거·실측이 적혀 있는 것 — BookDetail 손 격자, fmtReconLevel
2자리, InlineSigma, 이웃 칸 버튼, 실행 알약, PeriodSelector 자리, 손 대사표)이라
그대로 뒀다. **근거 없는 이탈만** 고쳤다.

### 옮겨 온 것 (Backtest 창의 문법)

- **창 몸통 리듬** `padding 2 · gap 2` — 두 MR 창이 Backtest 와 한 값(얼라인 5).
- **활자**: 창 머리 부제 `caption`(종전 legal) · 안내·stale `body` 뮤트 · 오류
  `body` + `.sr-up`(앱 공통 오류 문법 — 종전엔 맨 잉크 legal 이라 각주처럼 섰다).
- **차트 = LINKED PAIR 의 세로 결**: 패널이 풀폭이고(아래 정본화), 같은 `dates`
  + `useStackedScales` + **x 라벨은 맨 위 차트만**(`hideTimeAxis`) + 십자선
  `syncIndex` 동기 + 높이 두 급 **200/140**(Backtest 의 그 치수).
- **일별 대사 → 창 바닥 서랍**(`WindowDrawer` 탭). 거래 줄을 누르면 서랍이
  펴진다 — 그래서 `WindowDrawer`·`FloatingWindow` 에 **제어 prop**(open/
  onOpenChange)을 옵셔널로 더했다(기본은 종전대로 서랍이 자기 상태를 쥔다).
  종전에는 거래 패널의 «내용이 바뀌는» 판이라 목록과 대사를 같이 못 봤다.
- **캔버스 a11y**: 차트마다 `hoverLabel`(짚은 봉의 날짜+값 문장) — CLAUDE.md
  규칙 7 이 요구하는 그것이 다섯 차트에 아예 없었다.

### 정본이 바뀐 것 — 배치 [OWNER 2026-09-02 「세로 스택 정본화」]

`Panel` 이 `flexBasis: 50%` 라 **주석은 「2×2 격자」인데 화면은 세로 스택**이었다
(gap 을 더하면 한 줄에 둘이 안 들어간다). 오너가 화면 쪽을 정본으로 정했다 —
`Panel` 은 이제 풀폭이고 두 창의 주석도 현실을 적는다. 12년 시계열이 반폭에서
안 읽히는 것이 실질 근거다.

### 통합 장부 창에 **없는 것**을 명문화

표시 구간 탭·거래 표의 레벨/Δ 열·일별 대사 서랍이 낱개 창에만 있다. 셋 다
「만기가 섞인 장부에서는 정의가 안 선다」가 이유이고(구간을 자르면 걸쳐 들어온
다리를 어디에 셀지, 레벨 열에 6M 과 10Y 를 세로로 세우면 그 열이 무엇인지),
그 사유를 `BookWindow` 머리 주석의 «없는 것» 목록에 적었다 — 미완인지 의도인지
화면이 스스로 말하게.

### 공용 부품 승격 [OWNER 「한 벌로 승격」]

숫자 칸이 세 벌(`sim NumField`·`rv BpField`·`mr NumInput`), 표 머리 도우미가
두 벌(`Cond`·`ThHelp` 가 rv·MR)이었다 — Field 4벌→1벌 수리(2026-08-25)와 같은
길로 공용 한 벌씩. 배타 선택(`Choice`)도 캐논 `Segmented` 로 갈았다.

가드 `guards/mr-canon.test.ts`(21건) + 승격 가드가 이 통일을 잠근다.

### 간격 감사 2차 — 죽은 폭을 «구조»로 없앴다 [OWNER "칸 별 간격이라던가 이런거도 검증 끝났어?"]

**상자 «사이» 빈틈은 처음부터 캐논이었다** — 6px(묶음 안)·24px(묶음 사이)로
Backtest 설정 줄과 같다. 어긋난 것은 상자 **안**이었고 브라우저 실측이 잡았다:

- `Segmented` 에 **`fill`**(→ CDS `equalWidth`) 신설 → 컨트롤이 제 상자를 채운다.
  죽은 폭 5.1~27.8px → **0**. 얼라인 7 이 `Select` 에 `DROPDOWN_STYLES` 로 막아
  둔 그 실패의 세그먼트 판이다. Backtest 방향 칸은 자기 줄에 살아 안 켠다.
- **자유 입력 56 → 64**: 글자 자리가 22px 뿐이라 자기 화면의 프리셋 「0.05」
  (25.9px)·룩백 「252」(23.0)가 안 들어갔다(입력은 잘라서 스크롤 — 끝자리가
  조용히 사라진다). 감싸는 상자 220·208.
- **종목 lead 160 → 96**(최장 「KTB10 내재금리」 92.31px): 죽은 폭 107px 가 첫
  빈틈을 73.7px 로 벌려 **묶음 안이 묶음 사이보다 넓었다**.
- **`ThHelp` 가 자기 활자를 진다**: CDS `TableCell` 이 th children 을 `headline`
  (16/24)로 감싸 보드 머리 일곱 칸 중 셋만 16px 로 섰다 → 13/16/600 단일.
- `parts.headFont()` 신설(소문자 있으면 legal·없으면 caption — 리포 자신의 기준을
  함수로) · `.sr-stats` 일곱 자리 `flexWrap` · 노브 두 줄 seam 을 부품이 진다 ·
  통합 창 탭 → `Segmented` · `BandChart` 240 근거 + `hoverLabel`.

**오탐 하나**: 「창 표 넷의 sticky 머리 무동작」 — 실측하면 `thead` 가 sticky 이고
내부 스크롤러를 120px 굴려도 머리가 제자리다(headMovedBy 0). 안 고쳤다.

**σ 셋은 죽은 폭이 남는다**(0.6·11.7·9.2) — 「죽은 폭 0」과 「같은 성격 칸은 같은
폭」이 동시에 성립 못 하는 자리라 후자를 골랐고 그 판단이 `SIGMA_W` 주석에 있다.

### 가로 근접성 리듬은 **폐기됐다** [OWNER 2026-09-02 — "가로 근접성 리듬 폐기하고 그냥 동간격으로 배치하기"]

`.sr-fgroup`(묶음 첫 칸 +18px)이 **앱 전체에서 은퇴**했고, 컨트롤 칸 사이는
어디서나 **12px**(`gap={1.5}`) 한 값이다 — Backtest 설정 줄 · MR 노브 바 ·
MR/rv 조건 바 · 시뮬 설정 줄 셋. 무엇이 한 가족인지는 **라벨**이 말한다.

폐기 근거: 리듬이 두 벌이었고(설정 줄 6/24 · 조건 바 4/16) 어느 것이 정본인지
어느 파일도 말하지 않았다. 게다가 묶음이 «상자 안 죽은 폭»에 흔들려 선언은
6/24 인데 **보이는 빈틈은 여덟 가지**였다(73.7·25.0·25.5·6.0·17.2·32.8·6.0·24.0).

같이 든 것 — **칸 안 빈 부분 축약**: σ 공통 폭 상수 `SIGMA_W` 은퇴, 칸마다
제 내용 폭(128·117·120). 죽은 폭 11.7·9.2 → 0.7·1.2.

실측: MR 설정 줄 12×5 · 죽은 폭 0~1.2(종목 칸만 최장 계열명 96 고정 — 오너
선택) · Backtest 12×6(종전 6/24/24/6/24/6). 가드 `mr-canon` 21건이 잠근다.

## 6. 다음에 할 것 — 우선순위

**① 국고 다리를 민평 → 체결가로** ⭐ 가장 크다
지금 국고는 **민평(평가사 고시)** 이다. 이 리포의 연구 레인이 «마크는 SR 을
**1.5~2배 부풀린다**» 를 이미 측정해 뒀다(`bollinger-mr` 레인). 캐리가 SR 을
0.74→0.79 로 올렸지만 마크는 반대로 깎을 수 있어, **오늘 나온 모든 수가
진짜인지가 이 하나로 갈린다.** 장내/장외 체결가는 SQL 에 있다.

**통합이 생기면서 이 항목이 더 커졌다**: 화면이 이제 «아홉을 같이 치면 SR 1.07»
이라고 말한다. 그 수가 마크 위에 서 있으므로, 체결가로 옮기면 **통합 판정 전체가
같이 움직인다.** 옮길 때 `_mr_leg` 한 곳만 고치면 낱개·통합이 같이 따라온다.

**② 다리 수 상한이 없다** [오너 결정 대기]
동일가중 합이라 신호가 나면 **아홉이 다 들어간다** — 실측 최대 동시 9다리
(2016-11-17), 그날 걸린 명목이 900만원/bp 다. 평균은 1.33다리이고 46%의 날은
포지션이 없다. 「한 번에 몇 다리까지」가 정해지면 그건 노브가 아니라 **데스크의
제약**이라 서버가 진다(`allow_dirs`·`TRADABLE_DIRS` 와 같은 자리). 지금은 상한이
없다는 사실을 화면이 「최대 동시·그때 명목」으로 말하고만 있다.

**③ 만기별 명목을 다르게 걸 근거가 보인다** [미착수]
짧은 쪽이 세다는 것은 이미 두 번 나왔다 — 신호일 앞수익(6M +3.75bp ~ 10Y +0.74bp)
과 이번 만기별 성적(1Y +1억 3,768만·SR 1.01 대 9M +5,117만·SR 0.38). 지금은 아홉에
**같은 명목**을 건다. 만기별 가중을 붙이려면 그건 새 자유도라 **사전등록 없이는
안 된다**(격자가 아홉 배로 늘고 DSR 이 그만큼 나빠진다 — §긴 표본 판정 참조).

**④ 계열 축 이웃 칸** [전제가 바뀌었다 — 이웃 칸은 2026-09-02 에 **두 창 다**
내렸다(`StrategyWindow` 머리 주석에 사유). 계열 축을 세우려면 먼저 이웃 칸을
되살리는 결정이 필요하다.]
`neighbors` 는 노브 넷(룩백·진입σ·청산σ·손절σ)만 보여 주고 전부 같은 계열이라
90%대만 늘어선다. 계열 차원을 더하면 선물 칸이 BSS 칸 옆에 선다(응답 1.3초 →
2.5초쯤). **통합 창에는 이웃 칸이 아예 없다** — 한 칸마다 만기 아홉을 다시 돌아야
해서 열두 칸이면 백여덟 번이다. 계열 축을 세운다면 낱개 창 쪽이다.

⚠ **여기 적혀 있던 「선물 넷은 전부 음수 손익·음수 Sharpe」는 취소한다**
(2026-08-28). 그 수는 신호가 아니라 **단위 오류**였다 — 선물 내재금리는 `%`
계열인데 `명목 × Δ값` 을 그대로 곱해 평가가 100배 작았고, 비용만 제 단위였다.
고친 뒤 FUT-KTB3 은 승률 17% → **57%**, SR −1.25 → **−0.29** 다(여전히 음수지만
딴 그림이다). 게다가 **선물 두 계열은 창이 아예 안 열리고 있었다**(기본
`carry=true` 에서 500). 계열 축을 세우기 전에 이 둘이 고쳐져 있어야 했다.

**④½ 적대 대사가 확정한 결정 대기 넷** [2026-09-02 · 26에이전트 · 반박자 전원일치]
- **보드 z ≠ 엔진 z**: 보드(`mr._bands`)는 ddof=1, 엔진(PMS 규약)은 모집단 σ —
  같은 계열·창·날에 √(59/60) 배 갈리고 BSS-3Y 2,958봉 중 9봉이 진입 문턱
  반대편. 어느 쪽으로 통일하든 한 화면의 숫자가 통째로 바뀐다.
- **FSW 캐리의 CD 출처**: BSS 는 imx CD, FSW 는 `mkt_irs_close.cd_rate` —
  겹치는 날 70% 불일치·평균 4.15bp·최대 61bp(2022-10 mkt 쪽 동결). imx 로
  통일하면 FSW 캐리 숫자가 바뀐다. (죽은 코드 `mrcarry.assert_identity` 도
  두 출처를 섞고 있다 — 살리려면 같이 고칠 것.)
- **FUT/FSW 롤 점프 팬텀 손익**: MR 돈이 벤더 내재 «차분» 위라 분기 롤일의
  계약 갈아타기 점프(거래 불가능한 값)를 실손익으로 계상 — FUT 거래 35/109건
  이 >1bp 팬텀, 최대 25.6bp/거래. 리포 자체 규약(내재=수준·차분=조정가)과
  어긋난다. 수리는 롤일 Δ 마스크나 조정가 차분인데 FUT/FSW 성적이 통째로
  바뀐다. (BSS 는 무관 — 상수만기라 롤을 안 싣는 것이 실측돼 있다.)
- **원천 적재 정지(리포 밖)**: `imx_data.timeseries` 국고 6M·9M·1.5Y 세 항목만
  2026-08-24 에 동시 정지(다른 만기·IRS·CD 는 정상 적재 중). 인계문의 「만기
  마다 표본 날짜가 다르다」의 정체가 이것 — 적재기 점검 대상.

**⑤ pv01 근사 해소** [알려진 근사]
캐리의 원금 환산이 **지금 커브의 pv01 하나**를 6년 내내 쓴다. 크기만 좌우하고
부호·시점은 안 건드리지만, 정확히 하려면 봉마다 커브가 필요하고 이 엔진은
커브가 아니라 한 계열의 시계열만 받는 물건이라 그 자리가 없다 — 별도 레인.

**⑥ 밀었다**(2026-09-02 오전, 오너 지시 — §1). 순서도 지켜졌다: 백엔드
(`principal`·`/api/mr/book`)를 먼저 올리고 프런트가 뒤였다. 그 「프런트
먼저가 안 통하는」 사정 — 새 라우트는 구 백엔드에서 404 고, 보드의 `watch`
필드가 없으면 통합 줄이 조용히 사라진다 — 은 다음에 새 라우트·새 필드를
낼 때 그대로 다시 산다.

## 7. 통합 장부에서 조심할 자리 넷

1. **승률은 한 통에 모은 거래의 승률이다.** 아홉 승률의 평균이 아니다(78.41% 대
   78.29%). 새 집계를 붙일 때 이 규율을 깨지 마라 — 거래 수가 6건과 58건인 두
   만기를 평균 내면 그 수는 아무것도 아니다.
2. **날짜로 맞춘다.** 만기마다 민평×IRS 교집합이라 표본 날짜가 다르다(실측: 6M·
   9M·1.5Y 가 08-24, 나머지 08-31). 인덱스로 더하면 손익이 하루씩 밀린다.
3. **레벨을 만들지 마라.** 만기가 다른 아홉 스프레드의 평균은 거래할 수 있는 값이
   아니다. 통합 줄의 값·전일 칸이 비어 있는 것은 빠뜨린 게 아니라 규율이다.
4. **`_mr_leg` 를 고치면 두 화면이 같이 움직인다.** 그게 이 구조의 목적이다 —
   낱개만 고치고 싶어지면 그 순간이 통합의 수와 갈리는 자리다.

# (여기까지 복사)

---

# 오늘 알아낸 것 — 다시 파지 않게

## 문턱은 정확히 지켜지고 있다

「z 값대로 안 하고 마음대로 짠 것 아니냐」는 의심이 있었다. **아니다.**
`BSS-3Y` 거래 15건 전수를 보면 진입은 전부 `|z| ≥ 2.00`, `exit` 는 전부
`|z| ≤ 0.5`, 유일한 `stop` 은 `z = 3.93 ≥ 3.5` 다. 코드도 그대로다:

```python
should_stop = abs(zi) >= stop_z
should_exit = abs(zi) <= exit_z
elif abs(zi) >= entry_z:  want = -1 if zi > 0 else 1
```

## 승률 93% 는 성과가 아니다 — 그리고 「청산=승」도 법칙이 아니다

한 칸(`BSS-3Y·룩백60`)만 보면 «청산 → 승 14 / 손절 → 패 1» 로 대응이 100% 라
동어반복처럼 보인다. **그건 그 칸의 성질이다.** 전 계열 × 룩백 3종(39구성 ·
거래 1,296건):

```
청산 → 승 762   ·   청산 → 패 401      ← 청산의 35%가 손실
손절 → 패 123   ·   손절 → 승  10
```

z 는 **움직이는 평균**에 대한 거리라, 값이 그대로여도 평균이 따라 올라오면
손실인 채로 청산된다. 승률은 구성별로 **11%~100%** 로 흩어지고(중앙값 78%),
100% 다섯은 전부 **6~9건**짜리다 — 승률은 거래 수의 역함수다.

## 스왑만 치는 판은 **기각**됐다

「BSS 를 신호로만 쓰고 실제로는 스왑 페이/리시브만 친다」를 재 봤다. 신호는
BSS, 손익은 IRS 로 갈아 끼운 판이다(엔진과 같은 규칙임을 자기검사로 확인한 뒤
손익 계열만 바꿨다). BSS 아홉 만기 전부:

```
스프레드 매매   합계 +10억 5,045만   SR 중앙값  1.10   양수 9/9   평균 MDD  2,424만
스왑만 페이     합계  −5억 9,215만   SR 중앙값 −0.18   양수 1/9   평균 MDD 1억 6,154만
스왑만 리시브   합계  +5억 2,835만   SR 중앙값  0.15   양수 8/9   평균 MDD 1억 2,232만
```

승률이 50%대로 무너지고 낙폭이 **7배**가 된다. BSS 의 z 는 «국고와 IRS 의
관계» 신호이고 어느 쪽으로 좁혀질지는 말하지 않는다 — 한 다리만 치면 신호가
아무 견해도 갖지 않은 금리 방향에 통째로 노출된다(IRS 3Y 가 표본 기간에
128bp → 381bp 로 갔다). 문헌도 같은 방향이다(아래).

## 당일 종가 체결은 **부풀리고 있지 않다**

문헌이 same-bar 체결을 *"the most common concrete form of look-ahead bias"*
라 부르고 징후를 *"too clean — smooth equity, few losers"* 라 해서 의심했다.
신호를 한 봉 늦춰 다시 재니 아홉 만기 전부에서 **오히려 좋아졌다**:

```
당일 종가 체결   합계 +10억 5,045만   SR 중앙값 1.10
다음 봉 체결     합계 +12억 2,200만   SR 중앙값 1.18
```

신호가 60일 z 라 느려서 하루 늦어도 되돌림이 남는다. (볼린저 레인이
「즉시 0.64 → 지연 0.08」로 무너진 것과 다른 이유가 이것이다 — 저쪽은 밴드
재진입이라 엣지가 한 봉 크기였다.)

## 외부 문헌 대조 — 셋 부합, 둘 어긋남

**부합** ① 스왑스프레드는 두 다리 **DV01 중립**이 표준 표현이다(Clarus).
② 문턱 2σ / 0.5~1σ / 3σ 는 관례 그대로다(QuantInsti). ③ 높은 승률 + **음의
왜도**(작은 이익 다수·가끔 큰 손실)는 평균회귀의 서명이다(QuantifiedStrategies).

**어긋남** ④ 평균회귀의 통상 승률은 **50~65%** 인데 우리는 87~93% 다 — 표본과
한쪽 방향 탓이다. ⑤ **전제가 보편적이지 않다**: 미국 30년 스왑스프레드는
2008년 9월 이후 **줄곧 음수**이고, 그 지속성을 연금 듀레이션 수요·딜러
대차대조표 제약으로 설명하는 것이 Klingler·Sundaresan(J. Finance 2019)·
Jermann(2020)·NY Fed 다. 그 문헌의 한 줄이 우리 손절 설계를 겨눈다 —
미스프라이싱이 사라지기 전에 더 벌어져 손실로 밀어낼 수 있다.

우리 엔진은 60일 이동 평균이라 레벨 이동을 따라간다(부분적 방어). 대신 이
거래가 재는 것은 «경제적 균형으로의 회귀» 가 아니라 **«최근 60일 평균으로의
회귀»** 이고, 그건 훨씬 약한 주장이다.

## 캐리 — 중간 현금흐름은 안 상쇄된다

```
BSS −1 = 국고 매수 · IRS 페이 → (국고−조달) + (CD−스왑) = BSS + (CD−조달)
FSW −1 = 선물 매수 · IRS 페이 →     0       + (CD−스왑) = CD − 스왑
FUT −1 = 선물 매수            →     0                   = 0
```

선물에 조달을 붙이면 **이중계상**이다 — 증거금만 걸고 일일정산하며, 채권의
캐리는 선물 가격에 이미 박혀 있고 엔진이 재는 값은 그 가격에서 나온
내재수익률이다. 그래서 부호가 갈린다(조달 = 기준금리+10bp):

```
BSS-3Y 중앙 +0.090%/년   BSS-10Y 중앙 +0.379%/년   FSW-3Y 중앙 −0.092%/년
```

크기는 손익의 **4~11%** 이고, **승률을 93% → 86.7% 로 내린다**(캐리가 음수인
거래가 승패를 뒤집는다). 캐리는 결과를 좋게만 만드는 항이 아니다.

**★ 단위 함정.** 커브(국고·IRS·CD)는 **%**(3.817)인데 `funding.rate_on` 은
**소수**(0.0285)다. 그대로 빼면 100배 틀린 캐리가 나오는데 **예외가 아니라
그럴듯한 큰 수**(중앙 2.667%/년)가 나온다. 대수적으로 BSS 캐리는
`BSS + (CD − 조달)` 로 접혀 0.1% 안팎이어야 한다는 것이 그것을 잡은 자다 —
`mrcarry.assert_identity` 가 두 길로 같은 수가 나오는지 잰다.

**CD 91일은 `mkt_irs_close.cd_rate` 다.** `universe._fetch_irs` 의 매핑에는
6M~10Y 만 있어 3M 노드가 없다 — 따로 읽어야 한다(한 번 헛짚었다).

## 진입 규칙 — 레벨 대 복귀, 전 계열 실측 (2026-08-28)

오너의 「외부로 이탈했다가 다시 그 선을 터치할 때」는 **엔진이 하고 있던 규칙이
아니었다.** 원본 PMS 는 `|z| ≥ 진입σ` 인 모든 봉이 신호인 레벨 검사이고, 밴드를
**뚫는** 봉에 들어간다. 측정 보드(`mr._state`)는 이미 「재진입 n일째」를 판정하고
있었으므로, 두 화면이 같은 사건을 다른 규칙으로 말하고 있었던 셈이다.

둘을 나란히 놓고 전 계열을 쟀다(기본 노브 · 캐리 켬 · 명목 100만원/bp):

```
계열        모드   거래  승률   총손익(만원)     SR    MDD(만원)
BSS-3Y      이탈    15   87%        4,443    0.81        900
BSS-3Y      복귀    14   93%        2,777    0.76        900
BSS-2Y      이탈    21   90%       10,276    1.55      1,104
BSS-2Y      복귀    20   85%        7,422    1.40        867
BSS-9M      이탈    13   69%        2,617    0.29      2,485
BSS-9M      복귀    11   82%        4,218    0.65      2,406
FSW-3Y      이탈    52   83%       22,195    0.92      2,336
FSW-3Y      복귀    47   83%       13,268    0.65      2,025
FUT-KTB3    이탈    58   57%      −16,890   −0.29     20,590
FUT-KTB3    복귀    48   73%       −1,450   −0.03     17,265
합계 13계열 이탈 +65,426만원 · 복귀 +59,860만원
```

**복귀는 공짜가 아니다.** 승률은 13계열 중 10에서 오르고 낙폭은 전부 같거나
줄지만, 총손익은 11에서 줄고 SR 은 8에서 준다. 기다리는 만큼 되돌림의 앞머리를
내주고, 그만큼 「끝까지 벌어지기만 하는」 구간을 피한다 — 어느 쪽이 큰지는
계열마다 다르다. **짧은 만기(6M·9M)와 선물에서만 복귀가 이긴다.**

그래서 판정이 아니라 **노브**로 뒀고, 기본값은 원본 규칙(`level`)이다 — 적합성
벡터가 그 위에 서 있다.

## 실전 재설계 — 전 기간(민평 기준) 실측 [OWNER 2026-08-28 «일단 민평 기준으로»]

화면에서 노브를 하나씩 켠 BSS-3Y 전 기간(2020-01~2026-08) 수치다. 표본외
검증(2023-01~)은 `docs` 밖의 리포트 아티팩트와 `mr_live_report.py` 가 진다.

```
구성                총손익      MDD     SR   거래   승률   필터가 지운 진입
기본 재현          4,443만    900만   0.81    15    87%      —
+ 타임스탑 20      4,734만    839만   0.89    17    76%      —
+ 동적비용         3,951만    900만   0.72    15    87%      —
+ 레짐(변동성)     3,457만    900만   0.91    11    91%    5회 19일
+ 레짐(추세)       2,467만    594만   0.74     7    86%   10회 22일
+ 역신호청산       4,798만    900만   0.88    15    87%      —
+ 미청산 계상      4,443만    900만   0.81    16    81%      —   ← 승률만 바뀐다
다섯 전부          4,013만    501만   1.10    14    86%    5회 19일
```

**미청산 계상은 총손익·MDD 를 안 바꾼다.** 누적이 보유 봉마다 MTM 을 더하므로
열린 손실은 원래부터 곡선에 있었다 — 빠져 있던 것은 승률·거래 수·보유기간이고,
그래서 87% 가 81% 로 내려간다. 바뀐다면 그건 이중 계상이라 테스트가 잰다.

**전진분석은 파라미터 선택에 값을 못 더했다**(위 §Ⅱ 근거). 그 한 줄은 이웃 칸과 함께 2026-09-02 에 내려갔다(사유는 `StrategyWindow` 머리 주석).

**[미결] 국고 다리는 여전히 민평이다.** 오너가 «일단 민평 기준으로» 라고 정했고
(2026-08-28), 화면 각주가 그 사실과 「체결가로 재면 낮아질 수 있다」를 말한다.
체결가 재측정은 별도 레인이다.

## 긴 표본 판정 (2014-06 ~ 2026-08 · OOS 8.9년) [OWNER 2026-08-28 «더 길게 보지 뭐»]

`imx_data.timeseries` 에 국고채커브·IRS·CD 가 **2014-05 부터** 있다. 화면이 쓰는
`credit_matrix` 는 2020-01 부터라 6.7년인데, 그 길이로는 다중검정 문턱을 못 넘는다
(SR 1.05·시행 118 이면 최소 8.7년이 필요한데 3.5년이었다). 겹치는 1,633일에서
**상관 0.9996~1.0000 · 중앙 차이 0.00~0.10bp** 라 같은 계열임을 확인하고, 이음매를
안 만들려고 **전 기간을 새 출처 하나로** 썼다(`mr_live_wfo.load_long`).
화면은 안 건드렸다 — 보드의 z 와 순위가 통째로 바뀌는 일이라 별도 결정이다.

### ① 실전 재설계 다섯은 **기각**이다

```
              개선   악화   무변화        (9계열 · OOS 8.9년 · ΔSharpe)
타임스탑        2/9    7/9    0/9
동적비용        0/9    9/9    0/9
레짐(변동성)     1/9    8/9    0/9
역신호청산       1/9    0/9    8/9   ← 거의 안 걸린다
다섯 전부        1/9    8/9    0/9

포트폴리오   기존 SR 1.60 · +7억 2,332만 · MDD 5,113만
            다섯 전부 SR 1.01 · +2억 7,353만 · MDD 6,155만   ← 모든 축에서 나쁘다
```

짧은 표본(3.5년·BSS-3Y)에서 「타임스탑이 단독 최대 기여(SR 0.63→0.95)」로 보였던
것이 **12년·9계열에서 2/9 로 사라졌다.** 표본을 늘린 것이 한 일이 이것이다.

### ② 기본 규칙(60일/2σ/0.5σ)은 원시 통계로는 강하다

```
포트폴리오 SR 1.60 ± 0.34 · t 4.77 · 95%CI [0.94, 2.26] · 2,255봉
연도별 양수 10/10 · 9계열 전부 양수(SR 0.43~1.44)
큰 날 10개 빼도 손익 94% 남고 SR 1.92 (t 5.72) — 며칠이 만든 결과가 아니다
평균 쌍상관 0.216 → 유효 독립 계열 3.3/9
최소 백테스트 길이: N=1,062 에서도 5.5년 필요 < 보유 8.9년 → 통과
```

### ③ 그런데 **Deflated Sharpe 가 미결로 남긴다**

```
고른 칸(60/2σ/0.5σ)이 27칸 중 1위. 27칸 OOS SR: 중앙 0.78 · SD 0.61 · 최대 1.60
DSR   N=27   → 0.877
      N=118  → 0.523
      N=1062 → 0.098
왜도 +2.95 · 첨도 61.1 (큰 날이 양쪽으로 있어 상쇄된다 — 위 ② 의 절단 검정)
```

**판정을 가르는 사실 하나가 남았다:** 60일/2σ/0.5σ 가 이 자료(2014~2026 원화 BSS)를
보고 정해진 값인가. 아니라면 시행 수가 작아 그대로 서고, 맞다면 27칸 중 1위라는
사실이 곧 선택 편의라 DSR 이 옳다. **오너만 답할 수 있다(첫 PMS 를 만든 사람).**

## 승률·과거적합 의심에 대한 답 [OWNER 2026-08-28] — 그리고 화면이 그것을 말한다

오너 질문 둘. 「저렇게 단순한 전략이 승률이 이렇게 높을 수 있나」 · 「과거에
Overfitting 된 것 아닌가」. 둘 다 화면 밖에서만 답할 수 있었으므로 `app/mrdiag.py`
를 만들고 전략 실험 창에 **진단** 절을 세웠다(성과 카드 바로 다음, 이웃 칸 앞).

### ① 롤갭은 v2 계열에 **안 옮겨온다** — 먼저 확인했다

이웃 레인(`Desktopss-xgb`)이 온더런 지표종목에서 지표교체일 |Δ| 3.343bp 대
비롤일 0.825bp(**4.1배**), 최대변동 8일 중 5일이 롤이라고 보고했다. v2 는 같은
BSS 라도 **만기별 커브**(민평 매트릭스)라 구성이 다르므로 실측했다.

```
BSS-3Y   롤일 28건 평균|Δ| 1.730bp · 비롤일 0.820bp · 2.11배 · 최대변동 20일 중 2일
BSS-10Y  롤일 28건 평균|Δ| 1.193bp · 비롤일 0.854bp · 1.40배 · 최대변동 20일 중 1일
손익 기여 −1.3% · 롤일 제외 시 SR 1.60 → 1.63
```

**상수만기 구성이 롤을 안 싣는다.** 옮겨오지 않으므로 조정계열을 만들지 않는다.
(지표지정일은 `infomax.국재지표지정여부` 의 `변경내용 LIKE '%지표지정%'`.)

### ② 승률은 청산 구조의 산물이 **아니다** — 신호가 실제로 일한다

청산 규칙을 떼고 고정 10영업일 보유로만 재면(9계열·긴 표본):

```
신호일(실행 가능 방향)  n=1,364  10일 평균 +2.596bp  적중 76%
비신호일               n≈25,000 10일 평균 −0.130bp  적중 50%

만기별  6M +3.75 / 9M +4.47 / 1Y +4.61 / 1.5Y +3.67 / 2Y +3.37
        3Y +1.84 / 5Y +1.08 / 7Y +0.52 / 10Y +0.74   ← 짧을수록 세다
```

**★ 방향을 빼먹으면 답이 뒤집힌다.** `|z| ≥ 진입σ` 로 세면 BSS-3Y 가 −0.374bp·
적중 47% 로 나오고, 실행 가능한 쪽(`z ≥ +진입σ`)만 세면 **+1.842bp·적중 72%** 다.
못 하는 거래(z ≤ −2σ)의 수익이 섞이기 때문이다. `mrdiag.forward_edge` 는
엔진의 `_entry_signal` 을 그대로 불러 방향을 판정하고, 화면 라벨도 `|z|` 가
아니라 `z ≥ +2σ` 로 적는다(`signalCond`). 테스트가 이 성질을 잰다.

### ③ 승률 93% 의 정체 — 사유별로 쪼개면 보인다

```
BSS-3Y 기본 규칙   청산 14건 승률 93% 평균 +4.02bp · 11.4일
                  손절  1건 승률  0% 평균 −8.02bp ·  1.0일
                  손익비 1.08 · 프로핏팩터 7.00
```

승률만으로는 「작게 여러 번 이기고 크게 가끔 지는」 모양인지 알 수 없다. 손익비가
승률 옆에 없으면 그 승률은 읽는 사람을 속인다.

### ④ 과거적합의 증거는 못 찾았다 — 대신 **엣지 소멸**이 보인다

```
BSS-3Y  2020-01~2022-03  SR 1.15  +1,775만
        2022-03~2024-05  SR 0.88  +2,062만
        2024-06~2026-08  SR 0.42    +607만
```

부호가 유지된 채 크기만 단조로 준다. 과거적합이면 최근이 **무너지고 부호가
바뀐다** — 모양이 다르다. 같은 모양을 이웃 레인도 봤다(SR 1.01 → 0.42 → 0.20).

### 아직 안 한 것

- 화면은 여전히 **2020-01~** 계열을 쓴다(`credit_matrix`). 연구 경로만 12년이다.
  보드의 z 와 순위가 통째로 바뀌는 일이라 별도 결정으로 남긴다.
- 비용 프리셋(실측 편도 0.5bp)은 안 넣었다 — 손익분기 칸이 이미 그 질문에
  답한다(BSS-3Y 1.48bp > 0.5bp).

## 화면을 12년으로 옮기고, 비용 기본값을 실측으로 [OWNER 2026-08-28 «옮기고, 비용 프리셋 디폴트값을 0.05에서 0.5로»]

### 옮긴 것

`app/mrseries.py` 신설. BSS 계열이 `credit_matrix`(2020-01~) 대신
`imx_data.timeseries`(2014-05~)를 읽는다. **`universe_series` 는 안 건드렸다** —
Main·rv·시뮬이 같이 쓰는 함수라 MR 만의 사정으로 흔들 이유가 없다.

- **보드의 오늘 숫자는 안 바뀐다.** z·밴드·상태가 전부 트레일링 창이라 마지막
  점의 값은 앞이 얼마나 길든 같다. 실측 차이는 소수 첫째 자리 언저리
  (BSS-2Y −3.0 → −2.95, BSS-5Y 10.1 → 10.05).
- **캐리도 같은 출처로 옮겼다**(`mrcarry._bss_rates`). 안 옮기면 2014~2020 에서
  국고·IRS 를 못 찾아 캐리가 조용히 0 이 되고, 6년치 손익이 캐리 없이 계산된다.
  2020 이전 거래 21건 전부 캐리가 붙는 것으로 확인했다.
- 캐시는 **워터마크(`MAX(trade_date)`, 0.014초)를 열쇠로** 세 카테고리를 한 번에
  (90,594행·0.85초). 만기별로 읽으면 보드 한 번에 27번을 읽는다. 보드 2.1초.

### 비용 기본값 0.05 → 0.5

0.05 는 첫 PMS 의 값이지 이 데스크의 호가폭이 아니다. 오너 실측(국고3Y·IRS3Y
패키지 편도 ≤0.5bp)이 있으므로 **보수적인 쪽을 기본**으로 둔다. 프리셋 셋
(0.05 재현 / 0.2 / 0.5 실측)에 자유 입력을 남긴다. 규칙은 재현이고 **비용만
실측**이다 — 창 머리 주석이 그 예외를 적는다.

### 그래서 화면이 지금 말하는 것 (BSS-3Y · 12.0년 · 편도 0.5bp)

```
성과   +6,026만 · MDD −1,710만 · SR 0.62 · 38거래 · 승률 79%
조건   손익분기 편도 1.28bp (여유 0.78bp)
진단   신호일 134일(z ≥ +2σ) 10일 앞 +1.84bp 적중 72%  ·  비신호일 −0.08bp 적중 49%
       청산 32건 94% 평균 +2.89bp · 손절 6건 0% 평균 −4.67bp
       손익비 0.83 · 프로핏팩터 3.09          ← **손익비가 1 아래다**
구간별 14-06~18-07  SR −0.08  −202만
       18-07~22-07  SR  1.21  +3,939만
       22-07~26-08  SR  0.62  +2,288만
```

### ★ 앞서 적은 「단조 소멸」을 정정한다

6.7년 표본에서 3분할하면 SR 1.15 → 0.88 → 0.42 로 **단조 감소**로 보였고, 그것을
이웃 레인의 1.01 → 0.42 → 0.20 과 같은 모양이라고 적었다. 12년으로 늘리니
**−0.08 → 1.21 → 0.62** 다. 첫 4년이 아예 무성과였고, 짧은 표본은 그 구간을
통째로 못 보고 있었다. 소멸이 아니라 **국면 의존**에 가깝다.

손익비 0.83 도 새로 보이는 사실이다 — 이기는 거래가 지는 거래보다 **작다**.
승률 79% 와 프로핏팩터 3.09 가 그것을 덮고 있다.

## BSS 테너 통합 밴드 워치 (2026-09-01) [OWNER — "BSS 테너 통합 밴드 워치를 하나 만들어서 승률 및 세부사항들을 확인할 수 있게"]

측정면은 만기마다 한 줄을 세우고 전략 실험은 한 계열만 재현했다. 그래서 「이 규칙을
BSS 전체에 걸면 무엇이 되는가」가 화면에 없었고, 아홉 칸을 눈으로 더해야 했다 —
그렇게 더한 승률은 **틀린다**(거래 수가 다른 아홉 승률의 평균은 묶음의 승률이 아니다.
실측 78.41% 대 78.29%).

### 통합의 정의 — 연구 레인과 같은 것

`mr_live_report.py::all_tenors` 의 «9계열 동일가중 합» 을 그대로 쓴다: 만기마다 같은
명목을 걸고 일별 손익을 더하고, 거래는 한 통에 모은다. 날짜는 **날짜로** 맞춘다
(연구 스크립트는 인덱스로 맞추는데 그건 같은 OOS 창을 잘라 쓰기 때문이다 — 화면은
전 기간이라 만기마다 표본 날짜가 다르다. 실측: 6M·9M·1.5Y 가 08-24, 나머지가 08-31).

### 갈릴 자리를 안 만들었다

계열 하나의 준비·시뮬(단위 환산·방향·캐리·게이트·비용 경로·엔진 호출)을
`main._mr_leg` 로 갈라 내고 낱개 라우트와 통합 라우트가 **같은 함수**를 부른다.
대사 확인: `BSS-3Y` 낱개 `/api/mr/strategy` 와 통합 안의 그 다리가 총손익
62,264,702.22 · 38거래 · 승률 0.7895 · SR 0.637 로 **자릿수까지 같다**. 아홉 다리의
합과 통합 총손익의 차는 0.01원(반올림)이다.

### 기본 노브에서 화면이 말하는 것 (12.0년 · 편도 0.5bp · 캐리 켬)

```
성과   +6억 344만 · MDD −1억 498만 · SR 1.07 · 315거래 · 승률 78% (미청산 5다리 제외)
장부   최대 동시 9다리(2016-11-17) · 그때 명목 900만원/bp · 평균 1.33다리 · 무포지션 46%
조건   손익분기 편도 1.45bp (여유 0.95bp) · 막힌 진입 만기 9개 합계 479회
분산   개별 SR 중앙 0.41 (0.07~1.06) · 양수 9/9 · 평균 쌍상관 0.224 · 유효 독립 3.2/9
사유   청산 268건 92% · 손절 47건 0% · 손익비 0.82 · 프로핏팩터 2.99
구간   14-06~18-07 SR 0.71 · 18-07~22-07 SR 1.12 · 22-07~26-08 SR 1.31
```

**통합 SR 1.07 이 개별 SR 중앙값 0.41 의 2.6배**이고 최고 다리(1.06)보다도 높다 —
쌍상관 0.224(유효 독립 3.2/9)가 그 값어치의 출처다. 연구 레인이 낸 «평균 쌍상관
0.216 → 3.3/9» 과 같은 수다.

**손익비 0.82 는 여전히 1 아래다.** 승률 78%와 프로핏팩터 2.99 가 그것을 덮고 있다.

### 화면이 안 하는 것 — 적어 둔다

- **이웃 칸(노브 민감도)이 없다.** 한 칸마다 만기 아홉을 다시 돌아야 해서 열두 칸이면
  백여덟 번이다. 견고성은 노브를 옮겨 실행해서 본다(이웃 칸은 두 창 다 내렸다).
- **레벨을 안 만든다.** 보드의 통합 줄에 값·전일 칸이 없다 — 만기가 다른 아홉
  스프레드의 평균은 거래할 수 있는 값이 아니다. 단위 없는 둘(|z|·%B)만 평균이고
  나머지는 개수다.

### 고치다 드러난 것 셋

1. **머리가 안 붙고 있었다.** CDS `Table` 이 표를 `overflow: auto` 상자로 감싸므로
   sticky 의 기준 스크롤러가 바깥(`.sr-rv-rank-scroll`)이 아니라 그 상자가 된다 —
   굴릴 것이 없는 상자라 아무 일도 안 일어난다. 실측에서 thead 가 −107px 로 밀려
   올라갔고, 그동안 주석은 「머리는 스크롤을 따라온다」였다. `.sr-rv-rank-scroll > div
   { overflow: visible }` 한 줄이 머리와 통합 줄을 같이 고친다(가드가 잡는다).
2. **보드가 11초였다.** 통합은 계열 아홉을 도는데 `funding.rate_on` 이 호출마다
   조달 계열의 날짜 목록을 다시 만들고 있었다(기준금리는 **일별**이라 수천 점 ×
   27,129회 = 5.0초), `mrseries._watermark()` 는 호출마다 SSL 왕복이었다(18회 1.6초).
   둘 다 값을 하나도 안 바꾸는 메모이제이션으로 **11초 → 0.87초**.
3. **캐시 스키마를 안 올리면 조용히 옛 보드가 나간다.** `watch` 를 붙이고 백엔드를
   재기동했는데도 응답에 없었다 — `cache.py` 머리가 적어 둔 그 함정 그대로다(v13).

### 다음에 볼 만한 것

- 만기별 성적표에서 **짧은 쪽이 세다**는 앞선 실측(신호일 앞수익 6M +3.75bp ~ 10Y
  +0.74bp)이 손익으로도 보이는지: 1Y +1억 3,768만(SR 1.01) 대 9M +5,117만(SR 0.38).
  거래 수·보유기간까지 같이 보면 만기별 명목을 다르게 걸 근거가 될 수 있다.
- 동시 다리 수가 최대 아홉이라 **명목이 하루에 아홉 배로 뛴다.** 다리 수 상한을
  노브로 둘지는 오너 결정이다(지금은 상한이 없다 — 신호가 나면 다 들어간다).

## 표시 구간·대사 재료 (2026-09-02) — 다시 파지 않게

### 기간 탭의 뜻은 오너에게 물어 정했다

「지난 한달·한분기·1년칸 신설」은 세 가지로 읽힌다 — ①표시만 자르고 0 재기준
②표시만 자르되 누적 원값 ③백테스트 재실행. 오너 답은 **①이고 낱개 창만**,
건수는 **거래 일련번호·총 건수**, 노셔널은 **원/bp 에 액면(억원) 병기**다.
③이었다면 z 밴드가 구간 앞머리 룩백만큼 잘리고 「전체 재현」이라는 창의 계약이
깨졌다 — 같은 문장이 산술 세 벌로 갈리는 자리는 짓지 말고 물어라.

### 구간 산술은 세 가지가 전부다

`w0`(구간 첫 인덱스, **날짜로** 자른다 — 달력 개월·`monthsBefore` UTC 산술) ·
`baseCum`(구간 **직전** 봉의 누적 — 첫 봉을 빼면 첫날 손익이 사라진다) ·
사건 인덱스 이동(`i - w0` — 안 옮기면 마커가 딴 날에 선다). 곡선은
`p.cum - baseCum` 하나로 전체·구간이 같은 식이다. 「걸친 거래」는 청산이 구간
안인 것 — 진입만 세면 걸쳐 들어온 손익이 곡선엔 있는데 표엔 없다.

### 액면 환산과 그 한계

`principal = 명목 / (pv01 × 1e-4)` (`mrcarry.carry_krw` 의 그 식·지금 커브
하나). 실측: 6M 202.4억 ~ 10Y 12.2억, 3Y 35.4억(pv01 2.8244). 선물은 원금이
없어 null 이고, **null(선물)과 undefined(구 백엔드)를 갈라야** BSS 창이
「선물은…」이라고 거짓말하지 않는다. §6 ⑤(pv01 근사)가 이 표기에도 그대로 든다.

### fmtReconLevel — 항등을 주장하는 표의 자리수

캐논 `fmtLevel`(bp 1자리)로 레벨을 적으면 «3.5→1.8 인데 Δ −1.75» 가 선다 —
Δ(2자리)와 각자 반올림한 항이 표시된 합과 모순되는 krw.ts 의 그 사고다.
**항등을 주장하는 표는 양변을 같은 자리수로** — bp 레벨만 2자리로 갈라 냈다.

### SauronV2Backend 재기동의 함정 (실측)

`Start-ScheduledTask` 는 이미 돌고 있으면 **무시**되고, `Stop-ScheduledTask` 는
아침 리프레시가 띄운 옛 프로세스(07:49 발)를 **못 죽인다** — :8200 리스너 PID 를
`Get-NetTCPConnection` 으로 찾아 직접 내리고 Start 해야 새 코드가 선다. 태스크
상태 «Ready·결과 0» 은 래퍼가 끝났다는 뜻이지 서버가 죽었다는 뜻이 아니다.
새 페이로드 필드가 안 보이면 `'principal' in d` 처럼 **키 존재**로 판별하라 —
`d.get()` 은 구 코드와 새 코드의 None 을 구분하지 못한다.

## 적대 대사 2웨이브 (2026-09-02) [OWNER "대사를 훨씬 더 빡세게"]

**방법**: 워크플로 26에이전트(검증 13축 + 반박자 12 + 크리틱 1) · 체크 177건 ·
전부 엔진 함수를 안 부르고 재계산(데이터 창구만 허용) · 발견마다 반박자 2인이
따로 재현. 스크립트는 세션 스크래치에.

**닫힌 것(전수)**: 13계열 543거래 돈 삼분해 0.00원 · 신호 규칙 전수(진입/청산/
손절/우선순위 비공허 판 포함·누락 신호 0) · z 는 모집단 σ(ddof=0)·판정은
반올림 전 · 역신호 경로 32건 전수(FUT 는 «지연 1봉 플립» 이 실측 규약) ·
z=null(σ=0) 봉 규약 일관(진입·청산 전부 건너뛰고 캐리는 계속·타임스탑만 관통) ·
book=Σ아홉이 비기본 노브 4판(동적비용 합집합 통계까지)에서 성립 · 동적비용/
레짐 재구현 일치+**프리픽스 불변(무룩어헤드)** 3계열 · FUT 원천 사슬 완결
(페이로드=벤더 2,614/2,614·FSW 삼각 원천까지) · 이웃 격자 전 칸 독립 재시뮬
일치·베이스 규칙 상속 확인 · 보드 내부값(ddof=1 규약으로) 재계산 일치.

**수리 3(완료, `35d7a3a1`)**: ① 타임스탑×σ=0 봉 → exitZ=None 무가드 round 로
낱개·통합 500(유효 노브에서 창 전사) — 가드+계약 null+화면 '—' ② pv01
무반올림(8자리도 6M 에서 되곱 84원 어긋남) ③ 보드 fut defn 「(5% 합성)」
오라벨(KTB3 역사 구간 중앙 58bp 어긋남 실측) → 「벤더 직독」·캐시 v14.

**관찰 노트(무해·기록)**: 세 엔드포인트 전부 미지 쿼리 파라미터 조용 무시
(오철자 노브 = 기본값 결과) · IRS 원천 주말 행 13건(조인이 걸러 페이로드 누출
0) · `daily_lktb_price` 2026-07-30 완전 중복 1쌍 + 로더 무정렬 dict 덮어쓰기
(값 갈리는 중복이 오면 비결정) · 2019-02-15 가 벤더 두 표에 부재 → FUT/FSW
4계열 1점 무언 탈락(백필 대상) · lookback=2 는 |z|≡1 퇴화 칸(1 ulp 가 신호를
가름 — 엔진은 자기 일관) · 파이썬 3.12+ sum() 은 Neumaier 보정합이라 엔진의
소박한 누적과 1 ulp 갈린다(재시뮬 짤 때 손 루프로).

## 실측 프로브 — 화면을 «재는» 방법

얼라인 감사에서 쓴 방법이고 이 레인에도 쓸 수 있다. 브라우저 콘솔에서 행마다
컨트롤 높이·라벨 top·이웃 간격을 재고, 접힌 행은 줄마다 따로 잰다. 오늘 그것이
실제 위반 넷을 잡았고 **오탐 셋도 걸러냈다**(고칠 뻔했다). 다만 그 프로브는
브라우저에만 있고 CI 에는 없다 — 재현 가능한 스크립트로 남기는 것은 아직
안 했다.
