# 레인 — v1 패리티 (2026-08-20 착수 · **같은 날 마감**)

v1(braveworld)이 지키던 것 중 v2가 놓친 것을 되찾는 레인.

## 이 목록이 어떻게 나왔나

v1 프런트 모듈 140개와 가드 51개에서 명제를 전부 뽑고(가드의 `it(...)` 제목
단위), 각 명제가 v2에 서 있는지 소스에서 하나씩 대조했다. 파일 이름이 사라졌어도
기능이 다른 곳에 흡수됐으면 누락으로 세지 않았다.

**이 방법이 못 잡는 것**: 가드가 없는 v1 코드 안의 조용한 규칙(특정 셀의 반올림,
특정 조건의 문구). 그건 140파일 정독이라 이 레인보다 크다. P0 의 `row-vm-source`
를 심으면 그 계열이 **구조적으로** 막히므로, 정독보다 그것이 값이 싸다.

---

## 0단계 — 시작 전에 정해야 하는 것

### 0.1 조달 기본값 [OWNER, 2026-08-20 — "한은 기준금리"]

    v1  DEFAULT_BASIS = "base"   한국은행 기준금리
    v2  DEFAULT_BASIS = "call"   콜금리

오너 결정은 **base**다. 그런데 v2 에서 그대로 뒤집으면 화면이 선다 — 아래가
막고 있다.

**측정 (2026-08-20, 라이브 SQL):**

    infomax.기준금리        2012-12-31 ~ 2026-03-21, 4,829행, 마지막 값 2.50%
                            → 2026-07-16 인상(2.50 → 2.75%)이 **없다**
    data/bokbaserate.xlsx   2016-01-01 ~ 2026-07-16, 3,850행, 마지막 값 2.75%
                            → 그 인상이 **있다**

`app/funding.py` 의 `_require_fresh` 가 base 를 실패 상태로 세우는 이유가 이것이고,
게이트가 없었다면 7월 이후 조달이 **25bp 낮게** 잡혔을 것이다 — 화면 어디에도 안
보이는 방식으로. 기본값이 항상 422 를 돌려주는 화면은 제품이 아니라서 call 이
기본이 됐다.

**따라서 오너 지시 둘이 부딪힌다:**

    2026-08-18  "v2 데이터 소스는 SQL만"
    2026-08-20  "조달 기본값은 한은 기준금리"

SQL 만 쓰면서 base 를 기본으로 둘 방법은 지금 없다. 결정이 필요하다 — 아래 셋 중
하나이고, **정해지기 전에는 이 레인의 어떤 가드도 심지 않는다**(틀린 기준 위에
세우면 그 가드가 틀린 것을 고정한다).

| 안 | 내용 | 대가 |
|---|---|---|
| A | base 의 출처를 `bokbaserate.xlsx` 로 (v1 과 같은 경로) | "SQL만" 규칙의 예외가 하나 생긴다. 대신 오늘 기준으로 값이 옳다 |
| B | SQL 테이블이 다시 적재될 때까지 call 유지 | 오너 결정이 보류된다. 적재 시점은 우리 소관이 아니다 |
| C | base 기본 + 실패는 실패대로 노출 | 현금채권 화면이 지금 당장 선다. 사실상 불가 |

워크북도 2026-07-16 에서 멈춘다는 점은 같다. 다만 그 날이 **마지막 금통위 결정
그 자체**라 "그 뒤로 결정이 없었다" 는 평탄 연장이 오늘은 참이다. 다음 금통위가
지나가면 같은 병이 재발하므로, 어느 안을 고르든 게이트는 "최근 행이 있는가" 가
아니라 **"마지막 금통위 결정을 덮는가"** 로 바뀌어야 한다.

### 0.2 함께 결정할 것

- **regret** — v1 은 2026-08-14 에 은퇴시켰다. v2 는 소비자 없이 계속 굽는다.
- **네비게이션** — v1 주석: "두 층의 탐색, **둘 다 사이드바에** [OWNER, 2026-08-07 · 2차]".
  v2 는 TopNav 다. v2 가 그 지시에서 벗어나 있다.
- **`useIsWide` / `AnimatedNumber`** — 반응형 두 pane, 값 변화 크로스페이드. 필요 여부만.

### 0.3 안 가져오기로 확정 [OWNER, 2026-08-20]

    PayReceive 다이어그램        안 가져옴
    IntroCurtain 인트로 커튼     안 가져옴
    CommandBar + tileRegistry    이미 은퇴 [OWNER]. 점프는 scrollToIndex 가 승계

---

## P0 — 숫자가 조용히 거짓말할 수 있는 것

넷 다 "화면은 멀쩡한데 값이 틀린" 계열이다. 가드부터 심고, 위반이 나오면 수리한다.

### P0-1 `row-vm-source` — 프런트가 몰래 계산하지 않는다

v2 에 대응 가드가 **전무**하다.

v1 의 명제:
- 행 뷰모델이 담는 **모든** 필드가 `ROW_FIELD_SOURCE` 에 선언돼 있다
- 출처는 `dto`(서버) 아니면 `format`(표시 변환) **둘뿐** — 클라이언트 계산 레인이 없다
- dto 출처 필드는 손대지 않고 통과한다(숨은 산술 없음)
- 52주 통계는 읽어 넘길 뿐, 여기서 파생하지 않는다

v2 는 `table/rows.ts` · `cashbondRows.ts` · `universeRows.ts` 세 갈래로 행을
만드는데 이 경계가 없다. 산술이 하나 끼어들어도 아무도 모른다.

### P0-2 `krw-additivity` — 돈이 더해진다

v2 는 `splitKrw` 가 여러 가드에 등장하지만 **가산성 자체를 단언하는 곳이 없다.**

v1 의 명제:
- 실측 3중항 핀: `9,133 + 82 = 9,215`, 그리고 82 가 raw carry 다
- 항등이 참인 4중항의 결정적 스윕에서도 등식이 유지된다
- **반올림이지 버림이 아니다**
- 페이어와 그 미러 리시버가 미러 숫자를 찍는다
- `fmtKrw` 와 단위 표기가 만원 이상에서 **한 문법**이다
- 행과 합계 행이 독립 `fmtKrw` 호출을 하지 않는다

### P0-3 `range-slider` — 52주 위치 마커가 옆 숫자와 어긋날 수 없다

v2 에 위치 트랙은 **있는데** 일치 가드가 없다. 그림과 숫자가 갈리면 눈으로는 못
잡는다.

v1 의 명제:
- 52주 고점이면 트랙 끝, 저점이면 시작, 중간이면 중간
- 극값 **밖**이면 끝으로 클램프 — 트랙을 벗어나지 않는다
- 마커는 **옆에 인쇄된 숫자를 읽어서** 그린다(따로 계산하지 않는다)
- 임계 미만이면 그래픽이 사라지되 52주 숫자는 남고, 헤더가 그 사실을 말한다
- 퇴화 행은 빈 칸을 그린다

### P0-4 `policy-dual-axis` — bp 행과 % 행이 축을 안 섞는다

v2 는 `reference-lines` 가 일부만 덮는다.

v1 의 명제:
- 모델이 모든 단위를 실제로 지나간다(공허하지 않다)
- bp 행은 보조축에 앉고, % 행은 공유축을 쓰고, **한 그룹이 두 축을 걸치지 않는다**
- 기준선 둘을 그린다
- **기준선을 담으려고 종목을 재스케일하지 않는다**

---

## P1 — 기능이 아예 없는 것

### P1-1 보이는 구간의 극값 (`visible-extremes` + `window-extremes`)

v2 에는 서버가 구운 52주(고정창)만 있다 — **다른 질문에 답한다.**

핵심 설계는 **한 번 스캔, 두 소비자**다: 차트의 y-도메인과 마크가 같은 계산에서
나와 어긋날 수 없다. v1 이 못 박은 규칙:
- 창이 움직이면 마크도 움직인다 — 남아 있는 마크가 곧 실패다
- null range 는 "전부"를 뜻한다(라이브러리가 첫 레이아웃 전에 주는 상태)
- 소수 경계는 `ceil(from)..floor(to)`
- 데이터 밖·역전 범위는 아무것도 안 찍는다
- 동점 규칙이 **둘 다 명시**돼 있다: 창 극값은 FIRST, 보이는 극값은 MOST RECENT
- 평평한 창은 마지막 보이는 점에 마크 하나만
- 가장자리 극값은 플롯 가장자리에 앉지, 잘려 나가지 않는다

### P1-2 시뮬레이션 차트 hover 리드아웃

v1 `HoverPanel` 84줄. 경로를 설계하고 결과를 보는 화면인데 v2 는 "D+37 에 얼마" 를
못 읽는다. 백테스트(`LinkedCharts`)에는 있으니 문법은 이미 리포 안에 있다.

### P1-3 `domain-guard` — 히스토리 차트가 요청 도메인을 조용히 자르면 던진다

v2 는 `surface3d` 에만 비슷한 것이 있고 히스토리 차트에는 없다.

- 좁은 컨테이너가 앞쪽 연도를 먹으면 throw
- 메시지에 **요청 범위와 실제 렌더 범위를 둘 다** 싣는다
- 한 바 정도 가장자리 여유는 허용
- 머리뿐 아니라 꼬리가 잘려도 throw
- 첫 레이아웃 전(null range)에는 no-op

### P1-4 `policy-line` — 기준선이 축을 빈틈없이 덮는다

v2 에 `chart/references.ts` 는 있지만 규칙이 고정돼 있지 않다.

- 결정은 그 날짜 이후 **첫 점**에 착지한다
- 시리즈 시작 **전**의 결정은 시작 시점에 유효하다
- 그런 결정이 여럿이면 **마지막 것**으로 접힌다
- 연속 결정이 겹침도 구멍도 없이 축을 타일링한다
- bound 에서 멈춘다(차트 끝이 아니라)

### P1-5 `calendar` — 금통위 데이터 무결성

v2 는 `src/data/calendar.json` 을 **검증 없이** 쓴다. RV 와 시뮬이 그것을 읽는다.

- 잘린 파일을 잡는다(가드가 공허하지 않다)
- 생성된 LPR 이 가드를 침묵시키지 못한다
- 각 항목이 date · kind · label · **source · verified** 를 들고 있다
- 날짜마다 어느 은행에서 읽었는지 이름이 있다
- 네 은행을 다 담는다
- 모든 날짜가 검증 목록과 일치한다
- 2026 년만 있다 — 조작된 과거는 수리가 아니라 **삭제**됐다

---

## P2 — 동작은 있는데 못 박히지 않은 것

대부분 "맞게 돌고 있는데 고정이 없다" 라 빠르게 끝난다.

| 명제 | v2 상태 | 못 박을 것 |
|---|---|---|
| `candle-mode` | 가드 없음 | 선·주봉·월봉만(**일봉 없음** — 원천이 종가뿐), 모르는 모드는 추측 말고 **거부**, 캔들 창은 wick 으로 측정, 동점은 FIRST |
| `freshness` | 부분 | 영업일 사다리(주말 아님), 임계값을 여기서 복제하지 않음, **전일종가 규칙**(다음 영업일에도 current), 종가 하나 빠지면 behind·둘이면 stale |
| `sort-key` | 부분 | 모든 행이 유한 숫자 정렬키, **ON-start forward 금지**(그건 스팟 커브), 3M 이 두 번째 |
| `reorder` | 가드 없음 | 정렬·스크리너는 애니메이션, 탭·시작필터는 스냅, 임계 위는 정렬도 스냅, 창 밖 행은 안 움직임, 행 높이 0 에 나눗셈 금지 |
| `readout-parity` | 부분 | 팝업이 미리보기의 **모든** 리드아웃을 렌더, **캔들 모드에서도**, 캔들 카드는 창 통계가 아니라 그 **바**를 보임, 커브는 날짜가 아니라 테너를 명명, 둘 다 반올림 안 함 |
| `range-column` | 부분 | 52주 칸에 색·틴트·강조 토큰 없음, 클릭·호버 불가, 헤더/본문이 **같은 폰트 크기**로 서브그리드 해석, 헤더는 고→저→평균 순 |
| `scroll-affordance` | 부분 | 가로 스크롤 컨테이너, 자기 클립 금지, 고정 열 sticky 로 정체성 유지, 세로 괘선 없음 |
| `backtest-back` / `backtest-context` | 부분 | 인스턴스 키로 북과 결과 복원, open·close 둘 다 replace(히스토리에 창이 안 쌓임), 휴일이면 **다음** 영업일(이전 아님), 데이터 이후면 레벨 없음, 결과 전에도 진입 레벨 리드아웃 |
| `failure-visible` | 부분 | 로딩과 에러가 **다른 컴포넌트**, aria 로 알림, 토스트 아닌 버튼, 2단계 상세도 자기 재시도, 버튼이 작동 중임을 말함 |
| `label-quantity` | 부분 | 라벨이 아래 수량을 명명, 색은 한 뜻, 날짜를 **payload 에서** 가져오지 시계에서 안 가져옴 |
| `date-labels` | CDS 가 처리 | 사다리(10년→연, 1년 내→월초, 몇 주→일)를 우리가 정할지 CDS 기본에 맡길지 **결정 필요** |
| `pane-still` | 미확인 | 핀은 평범한 state set — pane 이 애니메이션하지 않음 |
| `guard-hygiene` | 없음 | 주석 스트리퍼 자체 테스트. **`production-env` 도 `stripComments` 를 쓴다** — 싸고 즉시 유효 |

---

## 안 가져오는 것 — 아키텍처가 달라서

| v1 가드 | 이유 |
|---|---|
| `cache-policy` · `static-paths` | v2 는 구운 산출물이 0개. **굽기를 되살리는 날 같이 와야 한다** |
| `live-routes-proxied` | v2 는 rewrite 를 안 쓴다(cross-origin 직접). `production-env` 가 그 자리를 지킨다 |
| `glass-contrast` · `ramp-sync` · `palette` · `no-raw-hex` | 사이드바 없음 / 스택 다름 / `color-source`·`contrast` 가 승계 |
| `lazy-chart` | v2 가 더 낫다(동적 import + CDS 대체) |
| `table-grid` · `sticky-opaque` · `tint-contrast` · `motion-tokens` · `canvas-var` | `colgroup`·`table-contract`·`ch-context`·`cds-tablerow-dom`·`surface-tokens` 가 덮는다 |

---

## 순서

    0단계  조달 기본값 결정 (0.1) — 이것이 먼저다
    1단계  P0 넷 — 가드부터, 위반이 나오면 수리
    2단계  P1 다섯 — P1-1 과 P1-3 은 한 덩어리(극값과 도메인은 같은 스캔),
                    P1-2 는 독립, P1-4·P1-5 는 기준선 한 덩어리
    3단계  P2 열셋

P0·P1 이 실질이고 P2 는 굳히기다. 1·2단계만으로 "숫자가 조용히 틀릴 수 있는
통로" 는 대부분 막힌다.

---

## 참고

- v1 리포: `Projects_AS\braveworld` (`frontend/guards` 51개, `backend` 143파일)
- 백엔드는 v2 ⊇ v1 이다(v1 에만 있는 파일 0개). 프런트가 갈린 곳이다
- v2 에만 있는 백엔드 수정: `NonBusinessDayError(date, reason)` 시그니처 3곳
  — **v1 은 아직 그 버그를 들고 있다**(별개 리포라 이 레인에서 손대지 않는다)

---

# 마감 (2026-08-20)

세 단계 다 돌았다. **위반은 딱 하나 나왔다** — P0-3.

| | 결과 |
|---|---|
| P0-1 `row-vm-source` | 가드가 **없었다**(`rows.ts:9` 는 있다고 적어 뒀다). 심었고 위반 0 |
| P0-2 `krw-additivity` | 가드 없었다. 실측 3중항으로 고정. 위반 0 |
| P0-3 위치 트랙 | **위반** — 마커가 순위 백분위. 수리 + 가드 (`8395035`) |
| P0-4 `policy-dual-axis` | 기존 `reference-lines` 가 이미 덮고 있었다 |
| P1-1 보이는 구간 극값 | 레인 문서의 "기능 없음" 은 **과장**이었다 — 값은 있었고 자리·표식·규칙이 없었다 |
| P1-2 시뮬 hover | 세 차트 다 신설(카드 + 스크러버 라벨) |
| P1-3 `domain-guard` | v1 의 병(라이브러리 클립)은 v2 에 없다. 대신 조용한 길 셋을 못 박았다 |
| P1-4 `policy-line` | 구현은 옳았고 명제 셋이 안 박혀 있었다. 위반 0 |
| P1-5 `calendar` | 검증 없이 쓰이고 있었다. 17개 명제. 위반 0 |
| P2 | `freshness` · `row-order` · `guard-hygiene`(+`_source`) · `p2-parity` 넷으로 정리 |

## 대조에서 **정정**한 것

- `candle-mode` 는 **오너가 제거 지시**한 기능이다 [2026-08-18]. 누락이 아니다.
- `failure-visible` 의 "재시도 버튼" 은 v2 가 **안 달기로** 한 결정이다 [OWNER].
  v1 명제와 정반대이고 `error-boundary` 가 그 결정을 이미 핀한다.
- P1-1 은 "기능 자체가 없다" 가 아니라 "자리·표식·규칙이 없다" 였다.
- v1 의 `production-env`·`static-paths`·`live-routes-proxied`·rewrite 는 **v1 에
  실제로 있다**. 앞서 "존재한 적이 없다" 로 적은 것은 v2 기준의 과장이었다.

## 이번에 드러난 구조적 문제 하나

`stripComments` 가 여섯 가드에 복사돼 있었고 **이미 갈려 있었다** — 다섯은
뒤따르는 주석까지 걷고 하나는 줄 전체일 때만 걷었다. 갈렸다는 것보다 나쁜 것은
그 차이를 아무도 모른 채 각 가드가 조금씩 다른 텍스트를 보고 있었다는 사실이다.
`guards/_source.ts` 로 모으고 `guard-hygiene` 이 그 한 벌을 시험한다.

## 열려 있던 넷, 전부 결정됨 [OWNER, 2026-08-20]

| 항목 | 결정 | 처리 |
|---|---|---|
| `date-labels` | **CDS 기본 유지** | v1 사다리를 이식하지 않는다. 규칙이 두 벌이 되면 CDS 가 눈금 폭을 바꾸는 날 갈린다 |
| `regret` | **v2 도 은퇴** | 백엔드·프런트에서 걷어냈다. `events.replay_leading_events` 도 유일한 소비자가 사라져 함께 내려갔다 |
| 네비게이션 | **TopNav 유지** | v1 의 사이드바 지시는 그 리포의 키트 맥락에서 나온 것이고, v2 는 CDS 로 다시 세운 별개 앱이다 |
| `useIsWide` · `AnimatedNumber` | **둘 다 안 가져온다** | 창이 고정폭 1020 이라 반응형이 지금 뜻이 없고, 숫자 크로스페이드는 허버로 훑는 표에서 오히려 방해다 |

이로써 이 레인에 열린 항목이 없다.

게이트(마감): build 0 / vitest 0 (**786**, 착수 시점 655) / lint 0(경고 4).

