# 인계 — sauron-v2, 2026-08-18 (레인 6 대부분 + Lab)

앞 인계는 `HANDOFF.md`(레인 1~5)와 `HANDOFF-2026-08-14b.md`(레인 6 내비·리드아웃).
이 문서는 그 뒤 세션이다. **아래는 전부 실측이고 재검증하지 말 것.**

---

## 0. 오너 상시 규칙 (그대로)

- **코드 고칠 때 항상 v1 을 읽으며 계약을 대사한다.** v1 = `Projects_AS\braveworld`,
  **읽기 전용**. 이 세션은 그 규칙대로 갔다 — 커브 표면은 v1 백엔드 모듈을
  **바이트 동일**로 가져왔고(`md5 c50291c0…`), 기하 모듈과 두 가드도 v1 것을 옮겼다.
- **커밋·푸시는 오너 몫.** 이 세션도 커밋하지 않았다.
- 이 리포는 **pnpm**. 포트 주인: `:3000/:8000` 동결 · `:3100/:8100` v1 · `:3200/:8200` v2.

---

## 1. 지금 상태

| | |
|---|---|
| HEAD | `127b878` (변함없음) |
| 미커밋 | 워킹트리 전체 — v2 작업은 아직 한 줄도 커밋 안 됨 |
| 프런트 가드 | **474/474** (`pnpm vitest run`, 33파일) |
| 백엔드 | **371 passed · 24 skipped · 1 xfailed** (`pytest`) |
| 빌드 | `pnpm build` 통과 |
| 서버 | `:3200` = **`next dev`** · `:8200` = uvicorn (둘 다 이 세션이 띄워 뒀다) |

---

## 2. 이번 세션이 한 일

### 2.1 모션 시스템 + 감속 (`theme/motion.css` · `theme/motion.ts`)

시간이 **다섯 군데**에 흩어져 있었다(`type.css` 의 `0.12s` ×3, `useFlipReorder` 의
`220ms` + 곡선). 세 시간(fast 120 / base 220 / exit 160)과 곡선 하나로 모으고,
`prefers-reduced-motion` 을 **두 겹**으로 걸었다 — 토큰 자체를 0 으로 만들고,
그 위에 `!important` 블랭킷으로 저자가 잊은 자리까지 덮는다. CSS·TS 두 벌의 숫자는
`guards/motion-tokens.test.ts` 가 파일을 읽어 대조하고, **다른 파일에 시간이 다시
적히는 것**도 같은 가드가 막는다.

### 2.2 영역별 에러 바운더리 (`ui/ErrorBoundary.tsx`)

여섯 영역(오버뷰·표·미리보기·시뮬레이션·백테스트 창·확대 창·연구실·기준점 띠)이
각자 경계를 진다. `DataState.ErrorState` 와 다른 물건이다 — 저쪽은 **가져오기**
실패(다시 시도가 답), 이쪽은 **렌더** 실패(다시 그려도 같은 자리에서 또 죽는다).
그래서 여기엔 「다시 시도」 버튼이 없고, 가드가 그 부재를 지킨다.

### 2.3 하단 기준점 띠 (`ui/BottomStrip.tsx`)

10Y · 3s10s · 1Yx1Y — 수준·기울기·포워드 하나씩. **`fixed` 가 아니라 페이지 기둥의
마지막 flex 아이템**이다. v1 이 이 컴포넌트에서 가장 길게 설명하는 문제(띠가 접힐 때
앱 루트 패딩이 같은 프레임에 뛰어야 하는 것)가 v2 의 `height:100vh` flex 기둥에서는
아예 생기지 않는다 — 띠가 줄면 내용이 그만큼 커지고, 맞출 두 값이 없다.

> ⚠ `.sr-strip` 의 `transition: height` 는 **의도된 예외**다(합성 전용 규칙 위반).
> `transform` 으로 밀면 띠만 움직이고 되찾아 주려던 22px 이 빈 칸으로 남는다.
> 디자인 훅이 이 줄을 계속 지적하는데, 고치지 말 것.

### 2.4 차트 종류 — 선 · 주봉 · 월봉 (`chart/chartType.ts`)

**전역 환경설정**이다(localStorage). 상단 내비 오른쪽 토글 하나가 화면의 모든
시계열 차트를 바꾼다. `?type=w` 로 **읽기만** 한다 — 토글은 URL 에 안 쓴다.

- 봉은 **서버가** 묶는다(`derive.py::ohlc_buckets`). 브라우저는 시계열을 안 만진다.
- **V2-LOCAL 백엔드 수정**: `/api/universe/series/{id}` 가 `interval` 을 받는다.
  안 받으면 국고·크레딧·선물 행만 선으로 그려지면서 컨트롤은 「주봉」이라고 말한다.
- **가장 값나가는 함정**: 구간(`SPANS.days`)은 **영업일**이라 그대로 봉 개수로 쓰면
  「1Y 주봉」이 260봉 = 5년치를 그린다. `windowLen()` 이 환산한다(주 5일·월 21일).
- CDS 는 도메인을 `series` 에서 뽑는데 봉은 **우리 레이어**가 그리므로, 종가만 실으면
  꼬리가 잘린다 → 안 그려지는 `WICK_HI`/`WICK_LO` 시리즈 둘로 도메인을 넓힌다.
- `Scrubber` 의 `seriesIds` 를 **명시**한다. 기본값이 전체라 꼬리 시리즈에도 구슬이 찍힌다.
- CDS 축 확인(소스 실독): `xAxis.data` 가 문자열이면 도메인이 `[0, n-1]` 이고 스케일은
  선형이다 → 캔들 레이어는 **인덱스**를 x 로 준다(`CandleLayerA` 의 `xValue`).

### 2.5 표로 보기 — 포워드 매트릭스 + 틴트 범례

포워드 탭의 「표로 보기」 → 떠 있는 창(`windowKey="matrix"`, `?fm=`). 격자 안에 선이
없고(칸끼리 변을 공유해 하나의 면), 고정 두 열은 **불투명 배경**이어야 한다.
틴트는 변화 열과 **다른 눈금**이다 — bp 가 아니라 자기 과거 백분위(`movePct`),
문턱 70 아래는 아예 안 칠하고 97 에서 0.45 로 최대. 범례 견본의 알파가 그 눈금의
양 끝이라 범례와 칸이 어긋날 수 없다(가드가 그 대조를 한다).

CDS `Table` 을 안 쓴 이유: `TableCell` 이 셀마다 32px 을 먹어서 열 열 개 격자에
320px 의 순수 여백이 생긴다(이 리포 실측). `ReconStack` 과 같은 선례로 손으로 짠 표.

### 2.6 커맨드 바 (`/`, Cmd+K)

이름을 치면 **탭과 행이 동시에** 정해진다. `cmdk` 없이 60줄. 접두사 우선 정렬이
핵심이다(안 그러면 `1Y` 가 `11Yx1Y` 들에 밀려 여덟 줄 밖으로 나간다). `escapeStack`
에 올라타서 Esc 가 **맨 위 한 겹**만 닫는다. 하단 띠와 **같은 `jumpTo`** 를 쓴다.

### 2.7 변화 기록 (`ui/ChangeLog.tsx`)

`WallSummary.events` 는 **계산돼서 오고 있었고 아무 데도 안 그려졌다**(v1 이 겪은
orphan 을 v2 가 그대로 물려받은 상태). 내비의 방아쇠 + 팝오버로 붙였다. 변화가 없는
날에도 방아쇠는 **남는다** — 비어 있음 자체가 정보다(하루의 31%).

### 2.8 제자리 확대 (`chart/zoom.ts`)

휠 = 확대(커서 아래 날짜가 그 자리에 남는다), **Shift+휠** = 좌우 이동, 더블클릭 =
전체. 끌기가 아닌 이유는 CDS 스크러버가 포인터 이동을 이미 쓰기 때문이다.

> ⚠ **React 의 `onWheel` 은 passive 다**(React 17+ 가 루트에 그렇게 단다).
> `preventDefault()` 가 아무 일도 안 해서, 확대하려던 휠이 창 본문까지 스크롤한다.
> 그래서 `addEventListener('wheel', …, { passive: false })` 로 **네이티브로** 단다.

확대 중에는 히어로의 구간 라벨이 「1Y」가 아니라 **보이는 날짜 범위**로 바뀐다 —
숫자와 라벨이 다른 기간을 말하면 안 된다.

### 2.9 Lab = 커브 표면 (v1 포팅)

- 백엔드 `app/surface.py` **바이트 동일 복사** + `/api/surface` + 굽기 등록
  (`SURFACE_PATH`, `build_static.py`). v1 의 `tests/test_surface.py` 도 그대로
  가져와 **16/16 통과**.
- 프런트 `chart/surfaceGeometry.ts`(v1 복사, 주석의 파일 참조만 v2 것으로),
  `ui/YieldSurface.tsx`(Tailwind → v2 토큰), `ui/useMeasure.ts`.
- 가드는 v1 의 `lab-tab` + `yield-surface-render` 를 합쳐 20건.
- **채움이 `--sr-card` 여야 한다** — floating horizon 의 가림이 곧 입체감이라,
  채움 색이 바탕과 다르면 지형 대신 얼룩이 된다. 가드가 그 문자열을 지킨다.

### 2.10 정리

`NOT_BUILT` 에서 `simulation`(레인 5에 이미 화면이 있음 — 도달 불가능한 죽은 문자열)
과 `lab` 을 지웠다. 남은 것은 `strategy` 하나다.

---

## 3. 남은 것

### ① Strategy — **오너 결정 대기** (착수 금지)

v1 에 **원본이 없다.** 이 섹션은 오너가 v2 에서 새로 더한 것이고(`ui/nav.ts` 의
`[OWNER 2026-08-13]`), 무엇을 보여줄 화면인지가 정해져 있지 않다. 지어내면 v1 대사
규칙이 적용될 수 없는 첫 화면이 된다. **무엇을 만들지 물을 것.**

### ② Cash Bond + Setting — v1 에 있고 v2 에 없다 (큰 레인)

v1 `0b1cb837`·`38b6a1ab`. 백엔드 라우트 넷(`/api/settings/funding`,
`/api/cashbond/instruments|series|backtest`)과 프런트 ~1,060줄(`CashBondTable`
`CashBondView` `CashBondWindow` `SettingView` + `state/funding.ts`).
**Setting 을 혼자 옮기면 안 된다** — 그 화면이 정하는 값을 읽는 것이 Cash Bond
백테스트뿐이라, 단독으로는 아무것도 안 하는 설정 화면이 된다. [오너 규칙: Setting 은
Lab 앞]

### ③ 인트로 커튼 — v1 쪽이 **미커밋·재설계 중**

v1 워킹트리에 `IntroCurtain.tsx`·`introCurves.ts`·`introPrewarm.ts`·`introVideo.ts`
가 수정/미추적 상태로 떠 있다(커브 面 + 착지로 교체하는 중). 움직이는 설계를 지금
옮기면 두 리포가 서로 다른 중간 상태로 갈라진다. **v1 이 커밋한 뒤에.**

### ④ 그 밖에

- 표의 숫자 셀 굵기 500 vs 600 — 여전히 오너 결정 대기.
- `WIDEST.label` 낡음(px 쿠션이 가려 주고 있다).

---

## 4. 브라우저 검증에 대한 정직한 경계

이 세션은 **화면을 눈으로 다 못 봤다.** 확장이 붙은 Chrome 탭이 다른 창 뒤에 있어
`document.hidden === true` 였고, 그 상태에서는 CDS 차트의 `<svg>` 자식이 **0개**라
캔들이 그려졌는지 잴 수가 없다(§HANDOFF-08-14b 2.2 의 그 인공산물). 헤드리스 CDP
프로브도 시도했으나 `ws` 모듈이 없고 `--dump-dom` 이 멈춰서 접었다.

그래서 이렇게 갈랐다:

| 확인함 | 방법 |
|---|---|
| 페이로드가 실제로 봉을 낸다 | `GET /api/universe/series/GOVT-3Y?interval=w` 실호출 |
| CDS 가 인덱스 도메인을 만든다 | `CartesianChart.js` · `utils/axis.js` 소스 실독 |
| 띠·토글·표·창·확대의 규칙 | 가드 474건 |
| 표면이 실제로 그려진다 | `renderToStaticMarkup` (가드 안에서) |
| **캔들이 화면에 그려지는 모양** | **못 봄 — 앞에 뜬 창에서 한 번 봐야 한다** |

`:3200` 은 `next dev` 라 새로고침이면 최신 소스가 뜬다(굽기·재시작 세트 아님).

---

## 5. 검증 명령

```powershell
Set-Location C:\Users\infomax\Projects\apps\sauron-v2
pnpm vitest run                        # 474/474
npx tsc --noEmit -p tsconfig.json
pnpm build
Set-Location backend; $env:PYTHONUTF8="1"
& C:\Users\infomax\Miniconda3\python.exe -m pytest -q   # 371 passed
```
