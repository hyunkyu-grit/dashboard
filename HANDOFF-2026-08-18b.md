# 인계 — sauron-v2, 2026-08-18 오후 (v1 이식 레인: 현금채권·자산스왑·Setting + 백테스트·시뮬 깊이)

앞 인계는 `HANDOFF-2026-08-18.md`(레인 6 + Lab). 이 문서는 그 뒤 세션이다.
**아래는 전부 실측이고 재검증하지 말 것.**

---

## 0. 지금 상태

| | |
|---|---|
| 프런트 가드 | **533/533** (`pnpm vitest run`, 39파일) |
| 타입 | `npx tsc --noEmit` 통과 |
| 빌드 | `pnpm build` 통과 |
| 백엔드 | **546 passed · 24 skipped · 1 xfailed** (`pytest`, sauron-v2/backend) |
| 서버 | `:3200` = `next dev` · `:8200` = uvicorn (둘 다 이 세션이 새로 띄움, **새 라우트 반영됨**) |
| 커밋 | 0건 — 전부 워킹트리 (커밋·푸시는 오너 몫) |

세션 시작 시점 기준선: 프런트 476(부모 세션 포함) → 533, 백엔드 527(픽스처
보충 전 기준) → 546.

---

## 1. 레인 A — 현금채권 + 자산스왑 + Setting (완료)

### 1.1 백엔드 — 바이트 동일 복사 (md5 대조 전건)

v1 `0b1cb837`·`38b6a1ab` 기준:

    app/creditmatrix.py    0e339bb8…   SQL 전용이라 그대로
    app/cashbond.py        8467d3eb…   가격·백테스트·자산스왑·세타·일별 대사
    app/calendar_cache.py  cc2cb651…   MEMO-1C
    app/df_cache.py        3cf17616…   MEMO-2
    app/schedule_cache.py  0f4e1a52…   스케줄 메모
    app/backtest.py        31f6ae4a…   ← 레인 B-1, 개시 포함 (아래)
    app/theta.py           ab97729d…   ← 세타 하루치 (아래)
    app/instruments.py     aeb76739…   ← 레인 C, 채권 전개 (아래)

테스트도 함께: `test_cashbond`(적응, 아래) · `test_calendar_cache` ·
`test_df_cache` · `test_schedule_cache` · `test_backtest` 6종 ·
`test_theta`(한 줄 적응) · `test_backtest_characterization` +
`characterization.py`/`regen_characterization.py`/`data/backtest_characterization.json`
(v2 에 없던 픽스처 — import 에러의 원인이었다).

라우트 4개(`/api/settings/funding`, `/api/cashbond/instruments|series|backtest`)와
lifespan 의 캐시 3종 설치를 `app/main.py` 에 v1 그대로 배선. `cashbond/series`
는 v2 `derive.series_history` 를 그대로 쓴다(같은 몸통, PreviewPane 이 그대로
먹는 모양).

### 1.2 기준금리 SQL 검증 — **값 일치 · 최신성 실패** [데이터 소스 규칙]

`infomax.기준금리`(일자·한국·미국, 퍼센트) 실측 2026-08-18:

    값 대사   v1 bokbaserate.xlsx 와 겹치는 3,733일 전건 일치 (오차 0)
    기간      2012-12-31 ~ — 민평(2020-01-02)을 완전히 덮음
    최신성    **실패** — 2026-03-21 에 멈춤 (영업일 107일 경과).
              2026-07-16 인상(2.50→2.75%)이 테이블에 없다. 평탄 연장하면
              그 뒤 조달이 25bp 낮게, 화면 어디에도 안 보이게 틀린다.
    대안 후보  infomax.단기금리 도 2026-03-20 정지 — 신선한 SQL 기준금리 없음
    콜금리    mkt_irs_close.call_rate 는 2026-08-17 까지 신선 — 통과

처리 (`app/funding.py`, v1 을 베끼되 이 부분만 다르다):

- base 는 SQL 에서 읽되 **신선도 게이트**(`_require_fresh`, 영업일 10일)가
  실패 상태를 세운다 — 멈춘 날짜·놓친 결정·대안을 문장으로 말하는 422.
  테이블이 다시 적재되면 게이트는 저절로 열린다. 폴백은 없다.
- **`DEFAULT_BASIS = "call"`** (v1 은 base). 기본값이 늘 422 인 화면은 제품이
  아니라서다. 프런트 `FUNDING_DEFAULT` 도 call — 두 기본이 같아야 첫 요청부터
  안 어긋난다(가드가 둘을 대조한다).
- `test_cashbond.py` 의 base 픽스처: v1 은 로컬 xlsx 를 그대로 읽었지만 v2 는
  **금통위 계단(발효일, %) 23건을 테스트가 시드**한다 — 시장데이터가 아니라
  오너 검증 가능한 공개 사실(calendar.json 과 같은 성질)이고, `_ladder` 는
  변화점만 있으면 일별 시계열과 수학적으로 등가다. 게이트 자체는
  `TestBaseFreshnessGate` 가 monkeypatch 로 따로 찌른다.

### 1.3 프런트 — v2 관례로 계약 대사 포팅

- **행 = 표준 `Row`** (`table/cashbondRows.ts`, universeRows 와 같은 성질의
  어댑터). `Group` 에 `cashbond`/`asw` 가 들어갔다 — v1 tabs.ts 의 "Group 에
  넣지 말 것" 은 "Group = IRS 행 빌더 어휘" 라는 전제 위의 규칙인데, v2 는
  그 전제가 이미 사실이 아니다(국고·크레딧·선물이 같은 열거). 규칙이 지키던
  것(두 항목 = 별개 탭, 현금채권 행이 IRS 빌더에 안 들어감)은 그대로다 —
  `rows.ts` 주석에 근거를 적었다.
- **메가 패널 다섯 번째 카테고리** 현금채권(항목: 현금채권·자산스왑) — nav.ts
  만 고치면 TopNav 는 저절로 따라온다. **Setting 섹션은 Lab 바로 앞**
  [OWNER 규칙, 가드가 자리를 핀].
- **종목군 필터 = 제목 줄 Select** (`ui/BondTypeFilter.tsx`, `?ct=`) — v1 의
  칩 줄이 아니라 포워드 시작점(StartFilter)의 선례다. v2 의 칩 줄은 오너가
  걷어냈고, 이 축은 스크리너가 아니라 다른 유니버스의 단면이라 살아남는다.
- **조달 상태** `state/funding.ts` — zustand 금지라 `useSyncExternalStore` +
  localStorage(`sr-funding`), `BottomStrip.useStripCollapsed` 와 같은 관례.
- **SettingView** — SegmentedTabs + blur/Enter 커밋 스프레드 칸 + 출처 블록.
  base 를 고르면 서버의 422 문장을 **그대로** 보여준다(실측 스크린샷 확인).
- **CashBondWindow** (`src/cashbond/`) — IRS `BacktestWindow` 와 같은 양식,
  다른 것은 상품 차이뿐: 방향 칸 없음(매수만), 종목·테너 분리 Select, 조달
  칸, 진입일 기본 1년 전. 북은 `?cb=` 로 URL 에 산다(링크가 곧 북 — 실측:
  붙여넣으면 같은 북이 뜬다). 세션 기억은 backtest/book 의 memory 를 키
  `cashbond` 로 같이 쓴다. `WindowKey` 에 `"cashbond"` 추가.
- **ReconStack 조달 열** — `funding` 필드가 있으면 꼬리 여섯(합계·평가·캐리·
  롤다운·조달·그날 손익), 없으면 IRS 모양 그대로 다섯. 열 존재를 데이터가
  정한다(상수 0 열 금지). sticky 오프셋·colgroup 이 전부 동적이 됐고 기존
  recon-stack 가드는 손대지 않고 통과.
- **PreviewPane** — cashbond/asw 그룹이면 `/api/cashbond/series` 로 간다
  (셋째 경로, 같은 모양).
- 신선도 키: cashbond·asw 둘 다 `govt`(= credit_matrix). asw 를 bss 로
  가리켰더니 헤더 08-17 vs 표 값 08-14 로 어긋났다(실측) — 자산스왑 시계열은
  **민평 달력** 위에 선다.

### 1.4 브라우저 실측 (next dev :3200 + 라이브 :8200)

- 표 171행(CB 94·ASW 77), 세타 열(3M 은 — , 매수 기준 양수), 호버 → 민평
  시계열 + CD91/기준금리 기준선 + 52주 통계.
- 백테스트 창: `CB:KTB:3Y 100억 2025-08-14` → **−2억 6,262만원** = 평가
  −3억 2,820만 + 캐리 +2억 3,921만 + 롤다운 +9,041만 + 조달 −2억 6,404만
  (화면에서 가산적으로 닫힘, API 값과 일치). 일별 대사에 조달 열 실측.
- **v1 대사**: 같은 북(CB 3Y + ASW KDB 5Y, call+10bp)을 :8100/:8200 에 던져
  총손익·포지션 전 필드·points·funding **전건 일치**. recon 의 `dbp` 열만
  달랐는데 — 이건 v1 원본의 잠재 비결정성이다(아래 §4).

---

## 2. 레인 B — 백테스트 깊이 (완료)

### 2.1 개시(4번째 칸) — 백엔드는 복사, 화면은 접기

`app/backtest.py`·`app/theta.py` 를 v1 바이트 동일로 올렸다(v2 사본은 개시
이전의 구판이었고 v2 고유 수정이 없음을 diff 로 확인). 세타는 **하루치**가
됐다(`HORIZON_DAYS`, thetaBasis `horizonMonths`→`horizonDays`) — 프런트
`ThetaBasis` 타입과 `THETA_TITLE`("하루 손익") 을 같이 옮겼다.

화면은 v1 의 표시 결정 그대로 **개시를 평가에 접는다** [OWNER, 2026-08-14 —
"개시손익 적으면 걍 무시해도 될 거 같은데"]: `splitKrw` 4번째 인자,
`backtestDays` 의 `valuation + startup`, `splitCashBondKrw` 도 같은 자리.
서버는 계속 따로 보낸다 — 접는 것은 표시 결정이다. 라이브 실측: IRS 10Y
개시 −217,194원, 네 칸 항등식 성립. `guards/backtest-startup.test.ts` 가
**접는 자리 둘 다** 접는지를 핀한다(한쪽만 접으면 3분해와 대사표가 같은 날을
두고 다른 평가를 말한다 — 합계 검증으로는 안 잡히는 종류).

### 2.2 차트 한 쌍 — `src/backtest/LinkedCharts.tsx` (CDS 재구현)

v1 `BacktestPnlCharts`(손 SVG)의 CDS 판. 정렬은 조정이 아니라 **구성**이다:
두 CartesianChart 가 같은 날짜 배열(x 도메인)과 같은 inset 을 받는다. 십자선은
`ReferenceLine dataX=인덱스` 로 **반대쪽 차트에** 선다(짚는 쪽은 CDS 스크러버
자신의 세로선). 0선 = `ReferenceLine dataY=0`. 진입·청산 마크도 ReferenceLine.
돈 카드(날짜·레벨·누적·당일)는 공용 ReadoutCard 가 아니라 **자기 카드**다
(v1 의 같은 결정 — 그 카드는 fmtLevel 문법을 소유한다. 다른 수량, 다른 카드).
누적 손익 값은 서버 발행점의 전진 워크(**찾기**지 계산이 아니다, `pnlAtDates`
로 뽑아 가드가 찌른다). IRS·현금채권 두 창이 같은 컴포넌트를 쓴다.
브라우저 실측: 위(민평 상승)·아래(손익 하락)가 픽셀 정렬, 교차 십자선·카드 확인.

`BacktestDailyPnl` 은 **안 옮겼다** — v1 스스로 은퇴시켰다(일별 대사로 합쳐짐,
v1 BacktestWindow:1376 주석). `backtestMemory` 는 v2 에 이미 있었다(레인 4).

---

## 3. 레인 C — 시뮬레이션 깊이 (실질 완료)

### 3.1 골든 픽스처 먼저 [세션 규칙 그대로]

v2 에 없던 `tests/data/simulate_golden_source_response.json` ·
`simulate_request_representative.json` 을 md5 대조 복사 → simulate 계열
**52 테스트 전부 통과** (수치 동등성 핀).

### 3.2 시뮬 포지션에 현금채권·자산스왑 [v1 642c5c46 — 레인 A 완료가 선행조건]

- 백엔드: `app/instruments.py` v1 바이트 동일(순수 +200줄) — 채권 카탈로그
  (cashbond 94·assetswap 77, 라이브 실측), `_expand_bond`(CB 한 줄, ASW 두 줄
  par-par), pvbp/krdMap/mtmYield 채움, 섹터 사상 명시. `test_instruments` 29 통과.
- 프런트 `sim/scenario.ts`: `InstrumentKind` +cashbond/assetswap, `kindOf`
  접두사 우선, `isBondKind`/`isBondLeg`, `EngineLeg.bondType`.
- `SimulationPage`: 종류 여섯, **채권은 방향 칸을 안 그리고** 옮길 때
  direction 을 1 로 되돌린다(안 보이는 −1 로 줄이 죽는 v1 실측 함정), 채권
  다리 금리 칸은 읽기 전용(엔진은 mtmYield 를 읽으므로 입력칸이면 조용한
  거짓말), 빈 목록이면 이유를 적는다.
- **ResultsWindow — 채권 성분이 자기 행으로 선다**: 분해가 성분 여섯
  (스왑평가·스왑캐리(잔차)·스왑롤다운·채권평가·채권캐리·조달비용, 각각
  manUnits 한 번)이 됐고, 케이스 비교·워터폴·성분 누적 경로가 채권이 있을
  때만 세 행/세 선을 더 세운다. 이전 판은 채권 몫이 "스왑캐리" 잔차에 조용히
  접혔다(그 자리 주석이 예고했던 바로 그 날이 왔다).
- 브라우저 실측: 종류 드롭다운 여섯, `국고채 3M 매수 100억원 2.8610 % 만기
  2026-11-14`(방향 칸 없음), 실행 → 결과 창. 결과 창의 채권 행은 렌더 가드
  (`sim-bond-parts`)가 jsdom 으로 핀 — 채권 북이면 세 행이 서고 스왑만이면
  아예 없다(상수 0 행 금지).

### 3.3 안 옮긴 것과 이유

프롬프트의 "3단 플로우·TermStructureChart·PositionsPanel·ComponentCurves·
HoverPanel·수동 포지션" 은 **v1 파일 트리**에서 나온 목록이다. v1 의 살아있는
표면을 열어 확인한 결과: 결과 창(ResultsStage)이 실제로 그리는 것은
ComponentCurves+Waterfall+일별대사+FundingNote 뿐이고(전부 v2 에 있음),
포지션은 [OWNER, 2026-08-07 — "북은 더 이상 포지션의 주인이 아니다"] **수동
입력이 전부**다(= v2 SimulationPage 의 모델 그 자체). TermStructureChart·
HoverPanel 은 v1 자체 킷의 차트 프리미티브라 CDS 가 그 자리를 진다.
RunningStage 는 실행 버튼의 "계산 중…" 상태가 대신한다.

---

## 4. 이 세션이 발견한 것들 (코드 밖)

- **v1 `book_recon` 의 recon `dbp` 는 혼합 종목군 북에서 비결정적이다.**
  `types = {p.bond_type for p in positions}` 가 **set** 이라 어느 종목군의
  민평 Δbp 를 찍을지가 프로세스 해시 시드에 달렸다(실측: 같은 요청을 v1/v2
  에 던져 dbp 만 갈렸고 손익·KRD·합계는 전건 일치). 정보 열이라 손익엔
  무영향. 바이트 동일 복사를 지키느라 v2 도 같은 성질 — 고치려면 v1 부터.
- **정적 트리 굽기의 워터마크**: `mkt_irs_close` 는 낮에 적재된다(이 세션
  중에 08-14 → 08-17 로 전진). 떠 있는 서버는 import 시점 데이터로 살므로,
  static-agreement 를 돌리기 전에 **서버를 새로 띄워야** 트리와 같은 데이터를
  본다.

## 5. 사고와 복구 (기록)

`cd` 가 겹친 셸에서 **braveworld(v1) 의 pytest 와 build_static.py 를 잘못
돌렸다** — v1 정적 트리 985파일이 08-17 데이터로 다시 구워졌다. 전건이 추적
파일의 수정(M)이었고 굽기 직전 v1 자체 테스트가 "커밋된 트리 = 08-14" 를
확인했으므로 `git restore -- frontend/public/api` 로 **정확히 원상 복구**했다
(복구 후 public/api 변경 0건, 남은 워킹트리 변경은 오너의 인트로 커튼 작업과
아침 자동화의 irsdata.xlsx 뿐 — 원래 있던 것). v1 커밋 이력은 안 건드렸다.
교훈: **v1 을 읽는 셸과 v2 를 실행하는 셸에서 `cd` 를 섞지 말 것 — 실행
명령은 절대 경로로 시작한다.**

## 6. dataset.py xlsx 폴백 — 제거하지 않음, 계획만 [규칙이 허용한 경로]

제거 연쇄가 이 레인 규모를 넘는다: ① `merge_expected_close` 는 오너 인용이
붙은 설계다("1D는 엑셀에서 가져와서 채우는 걸로") ② 전용 테스트
`test_dataset_merge.py`(167줄) 재작성 ③ `build_static` 이 같은 로더를 탄다
④ `policy.py` 도 bokbaserate.xlsx 를 읽는데 SQL 대체 후보가 방금 최신성
검증에 실패했다(§1.2) — 기준금리의 신선한 SQL 원천이 생기기 전에는 policy
줄이 갈 곳이 없다.

**제거 계획**: (1) `infomax.기준금리` 적재가 재개되면 → funding 게이트가
저절로 열리고, policy.load_base_rate 를 SQL 로 옮기고 xlsx 를 지운다.
(2) irsdata.xlsx 보충 — `merge_expected_close` 에서 xlsx 갈래 셋을 걷어내면
"SQL 에 기대일이 없다 = 지연 칩" 하나만 남는다(그 갈래는 이미 있다).
test_dataset_merge 는 그 한 갈래의 테스트로 줄인다. (3) `DEFAULT_XLSX` ·
`load_dataset` (엑셀 파서) 삭제, build_static 은 자동으로 따라온다.
순서는 (2)→(3) 이 (1) 과 독립이라 먼저 가능 — 단 1D 가 SQL 에 없는 날
아침 화면의 1D 가 빈칸이 되는 것을 오너가 받아들일 때.

## 7. 남은 것

- Strategy 탭 — 착수 금지 그대로 (다음 = rv2 프롬프트).
- CashBondWindow 의 실행 **전** 종목 차트(리드인) — v1 은 있었다. v2 는 실행
  후에만 쌍이 선다. 다음 항목으로.
- LinkedCharts 의 IRS 창 폴백(히스토리 도착 전 한 프레임은 손익 선 하나) —
  의도된 물러섬, 다음 렌더에 쌍이 선다.
- 브라우저 자동화의 클릭 좌표가 이 세션 후반 계속 표류했다(창 DPR 이 바뀌는
  듯). CDS Select 는 **열고 → 키보드(Down/Return)** 가 안전했다.

## 8. 검증 명령 (그대로)

```powershell
Set-Location C:\Users\infomax\Projects\apps\sauron-v2
pnpm vitest run                        # 533/533
npx tsc --noEmit -p tsconfig.json
pnpm build                             # 이후 :3200 dev 재기동 잊지 말 것
Set-Location backend; $env:PYTHONUTF8="1"
& C:\Users\infomax\Miniconda3\python.exe -m pytest -q   # 546 passed · 24 skipped · 1 xfailed
```
