# 선물 계열 실가격 대사 — 이어받기 프롬프트 (2026-09-03)

> 아래 전체를 새 세션에 그대로 붙여넣으면 이 일감을 이어서 할 수 있습니다.
>
> 오늘(09-03) MR 대사 레인을 통째로 갈아엎었고, **BSS 아홉 계열은 끝났습니다.**
> 남은 것은 **선물 계열 넷**(FUT-KTB3·FUT-KTB10·FSW-3Y·FSW-10Y)뿐입니다.

---

## 0. 너의 임무

`Projects\apps\sauron-v2` 의 MR 창에서 **선물 계열도 실가격 대사로** 돌린다.
지금 선물 넷만 옛 근사(`평가 = 명목 × Δ스프레드`)로 돌고, BSS 아홉은 실제
자산스왑을 가격해 `평가 + 캐리 + 롤다운 + 조달 + 비용` 으로 회계한다.

시작 전에 반드시:

1. `CLAUDE.md` 를 읽는다 — 캐논·얼라인·말줄임 금지·낱말 중간 줄바꿈 금지.
2. UI 를 건드리면 `cds-code` 스킬을 먼저 부른다(CLAUDE.md 규칙 1).
3. 이 문서 **§5 함정**을 읽는다. 오늘 내가 실제로 밟은 것들이다.
4. `docs/MR_LANE_STATE.md` 와 `backend/app/futures.py` 머리를 읽는다.

---

## 1. 절대 규칙

- **커밋은 오너가 지시할 때만.** 완료 보고는 커밋 해시 + `git show --stat`.
- **`git stash` 금지**(이 리포의 기존 규칙 — 동시 세션과 pop 이 조용히 실패).
  커밋은 반드시 `git commit --only -- <경로>` 로 경로를 못 박는다.
- **원격이 둘이다.** 둘 다 밀어야 한다:
  ```
  origin     github.com/wwoo1116-cell/rateslab.git    ← Vercel 배포
  dashboard  github.com/hyunkyu-grit/dashboard.git
  ```
  `git push origin master:main && git push dashboard master:main`
  ⚠ **에이전트는 push 가 하네스에 막힌다.** 오너에게 명령을 드려야 한다.
- **백엔드는 push 로 안 바뀐다.** `:8200` 은 예약 작업 `SauronV2Backend` 가
  `backend/serve.ps1` 로 띄운 로컬 프로세스다. 백엔드를 고쳤으면:
  ```powershell
  Stop-ScheduledTask -TaskName SauronV2Backend
  # 포트가 안 비면 Get-NetTCPConnection -LocalPort 8200 로 PID 확인 후 Stop-Process
  Start-ScheduledTask  -TaskName SauronV2Backend
  ```
  `serve.ps1` 은 「이미 듣고 있으면 아무것도 안 함」이라 **포트를 먼저 비워야
  한다.** :8200 은 Funnel 로 공개돼 있어 재기동 중 수 초 끊긴다.
- **지어낸 데이터 금지.** 없는 것은 화면에도 시험에도 없어야 한다.
- `.premove.bak` 294장은 **그대로 둔다** [오너 2026-09-01].

---

## 2. 오늘 무엇이 바뀌었나 (BSS 는 끝났다)

커밋 열하나. `git log --oneline a39465de..HEAD` 로 전부 볼 수 있다.

### 회계

`simulate()` 는 **언제**(진입·청산 시점)만 정하고, **얼마**는 실제 자산스왑을
가격해서 낸다. 엔진 함수는 한 줄도 안 건드렸다 — PMS 원본 이식이라 합성
픽스처로 잠겨 있고(`test_kpi_conformance_vector_matches_pms`), 회계는
**라우트에서** 얹는다(`main._mr_real_accounting`).

    그날 손익 = 평가 + 캐리 + 롤다운 + 조달 + 비용

백테스트·시뮬 대사의 네 성분에 전략의 비용 하나가 붙은 모양이다. 비용이 표에
없는 이유는 **상품의 성질이 아니라 노브**이기 때문이다.

바꾼 이유: 종전 산술은 `평가 = 명목 × Δ스프레드` 하나라 롤다운도 조달도
없었는데, 실측에서 **엔진이 안 세는 롤다운(789만원)이 엔진 총손익(688만원)보다
컸다.**

### 표본

BSS 는 민평 행렬의 첫 날(2020-01-02)부터만 쓴다(`main._mr_reconcilable`).
값 계열은 `imx_data.timeseries`(2014-05-28~)인데 대사는 `credit_matrix`
(2020-01-02~)로 가격하기 때문이다. **화면이 보여 주는 것은 전부 대사할 수
있어야 한다** [오너]. 바닥은 하드코딩하지 않고 행렬의 첫 날을 읽는다.
**선물은 안 자른다** — 민평 제약이 안 걸리는 자리다.

### 액면

거래마다 **진입일 커브**로 환산한다(`_mr_pv01_at`·`_mr_principal_at`).
종전엔 「지금 커브」 pv01 하나로 6년 내내 했는데, 회계가 실가격이 되면서 그
오차가 손익 전체를 스케일했다(옛 거래가 노브보다 최대 16% 큰 포지션).

### 화면

- 「일별 대사」 = `ReconStack`(백테스트·시뮬과 같은 컴포넌트) — 테너별 KRD·Δbp·
  추정에 합계·평가·잔차·캐리·롤다운·조달·그날 손익.
- 「일별 레벨」 = 형제 탭. 레벨·z·다리 레벨·CD.
- 대사표의 **오른쪽 고정을 풀었다**(2026-09-03) — 한 줄이 통째로 움직인다.
  v1 결정(「좌우의 범례 … 열과 행 고정」)을 오너가 뒤집은 것이다.

### 지금 수치 (참고)

    BSS-7Y  표본 2020-01-02~ · 거래 15 · 총손익 2,506만원 · SR 0.51
    게이트  pytest 1,234 통과 · vitest 1,648 통과 · 0 실패

---

## 3. 선물 쪽 사실 — **이미 다 있다**

### CTD 는 없다

한국 국채선물은 **현금결제**다. 인도가 없으므로 CTD 라는 개념 자체가 없고,
최종결제가격은 바스켓 지표종목 수익률을 **표면 5% 가상채권**에 대입한 이론가다.
(내가 09-03 에 「CTD 를 풀어야 한다」고 두 번 말했는데 틀렸다. 오너가 잡았다.)

### `futures.book_recon` 이 이미 있고 돈다

`backend/app/futures.py:671`. 스왑·채권 대사와 **같은 응답 모양**이라
`backtestDays` → `ReconStack` 으로 그대로 들어간다. 실측(09-03):

```python
from app.main import _dataset
from app import futures as ft
fut = ft.load()
pos = ft.as_position("FSW:3Y", -1, 1_000_000_000.0, date(2021,1,14), date(2021,3,16))
rec = ft.book_recon(fut, _dataset, [pos])
# tenors ['3Y'] · rows 42
# 2021-02-16  krd {'3Y': 314355}  dbp {'3Y': -2}
#             추정합 691,581 · 평가 700,000 · 잔차 8,419
#             캐리 None · 롤다운 None · 조달 None
```

`FUT:3Y` 는 같은 크기에 부호만 반대(`-314,355`)다.

### 캐리·롤다운·조달이 `None` 인 것은 결함이 아니다

모듈 머리가 근거까지 적어 뒀다 — 「현금결제(CTD 없음)·연결 계열이라 만기
롤오프가 없고, **캐리·조달·롤다운은 존재하지 않는 성분**이다(공란 정책;
합성채는 늙지 않는다)」. 0 으로 채우면 「그날 롤다운이 0 이었다」는 **다른 말**이
된다. `ReconStack` 은 `funding` 이 `undefined` 면 열을 안 세운다.

### FSW 는 이미 진입일 DV01 중립이다

「두 다리는 진입일 DV01 중립으로 가중한다 — 엔진이 스프레드·플라이에 이미 쓰는
규칙(`_build_legs`)과 같다. IRS 다리는 스왑 엔진(`_run_one`) 그대로다.」
즉 내가 오늘 BSS 에 공들여 세운 규약을 선물 쪽은 이미 지키고 있다.

⚠ **방향 규약이 BSS 와 다르다.** `FSW:3Y +1` = 호가값(스프레드) 롱 =
**선물 매도 + IRS 리시브**. MR 엔진 부호(`hold`/`dir`)와 사상을 반드시
실측으로 확인할 것.

---

## 4. 해야 할 배선 — 셋

1. **명목 → 액면.** MR 노브는 `₩/bp`(DV01)인데 `as_position` 은 액면을 받는다.
   BSS 는 `_mr_principal_at`(진입일 커브 pv01)로 환산했다. 선물은 **pv01 이
   아니라 선물 DV01**(액면/100 × 듀레이션 …)이므로 그 자리에 맞는 환산을
   `futures.py` 에서 찾아 쓸 것. 화면 머리는 이미 「선물은 액면 환산이 없어요」
   라고 적고 있다 — 그 문장도 같이 고쳐야 한다.

2. **방향 사상.** 위 ⚠ 참조. BSS 는 `direction = -dir` 이었다. 선물은 다를
   가능성이 높다. **거래 하나를 골라 손익 부호가 엔진과 맞는지 실측**할 것.

3. **IRS 다리가 들어 있나.** 위 실측에서 `FUT:3Y` 와 `FSW:3Y` 의 KRD 크기가
   같고 부호만 반대다 — **선물 다리만** 든 것으로 보인다. FSW 의 IRS 다리
   (캐리 = CD − 스왑고정)가 `book_recon` 안에 있는지, `fsw_swap_leg`
   (`futures.py:450`)가 어디에 쓰이는지 확인할 것. 없으면 그 다리를 어떻게
   합칠지가 **오너 결정**이다.

그 뒤에 `main._mr_real_accounting` 의 `kind != "bss"` 가드를 풀고
(`_mr_recon_rows` 대신 선물 경로를 태운다), `/api/mr/recon` 의
`MR_RECON_WHY["kind"]` 분기를 지우고, 화면의 「선물은 자산스왑이 아니에요」
문장을 고친다.

**성과 수치가 바뀐다** — 지금 FUT·FSW 넷은 옛 근사다. 바뀌면 오너에게 먼저
말할 것.

---

## 5. 이미 밟은 함정 — **다시 밟지 마라**

### 검산을 계열 하나로 하면 결함이 숨는다

BSS-7Y 하나만 보고 「대사 맞다」고 보고했다가, 오너가 전수를 시켜서 **미청산
다리가 통째로 0 이던 결함**을 잡았다(BSS-9M 51봉). 7Y 의 어긋남은 −150만이라
작았고, 6M·1Y·1.5Y·5Y 는 마침 표본 끝이 평평해 정상으로 보였다.
**아홉 계열을 다 재라.**

### 단위 — `yield_at` 은 소수, 점의 다리 레벨은 %

`creditmatrix.yield_at` → `0.04304`. `points[].legs[0].lvl` → `4.304`.
같은 값이다. 이걸 안 보고 재서 「두 커브가 300bp 갈린다」는 **가짜 경보**를 한
번 냈다. 검산 자신도 틀린다.

### 반올림 — 세 곳에서 물렸다

`lib/krw.ts::splitKrw` 의 규칙(**한쪽이 잔차를 진다**)을 안 쓰면 표가 화면에서
안 더해진다. 오늘 물린 자리 셋:

- 서버가 성분마다 따로 반올림 → `actual ≠ 평가+캐리+롤다운+조달`(거래당 23원).
  **「그날 손익」이 정본이고 평가가 잔차를 진다.**
- 기준 액면의 값을 배수 → `Σest ≠ estTotal`(8원). **총계는 성분의 합이다.**
- 스케일 뒤 `−KRD × Δbp ≠ est`(2원). **추정을 KRD·Δbp 에서 유도한다.**

### 브라우저 실측

- **`requestAnimationFrame` 으로 기다리지 마라** — 백그라운드 탭에서 rAF 가 0
  이라 CDP 가 45초 타임아웃으로 죽는다. `setTimeout` 을 쓴다.
- **스크린샷은 0.817배로 축소된다**(페이지 1920 → 이미지 1568). `zoom` 의
  region 은 **이미지 좌표**다. 페이지 좌표를 그대로 넣으면 엉뚱한 데를 찍는다.
- 창을 닫았다 열면 위치가 바뀐다. 매번 rect 를 다시 재라.

### 가드가 옛 결정을 붙들고 있다

오늘 가드 여섯 개를 새 명제로 옮겼다. 가드가 빨개지면 **먼저 「이 가드가 지키던
명제가 아직 참인가」를 묻는다.** 예: `recon-stack.test.tsx` 는 v1 의 「범례는
사방 고정」을 지키고 있었고, 오너가 그 결정을 뒤집었으므로 가드를 다시 썼다.

⚠ 본문의 `td[rowspan='3']` 은 **왼쪽 날짜 칸도** 걸린다. 돈 칸은
`.sr-recon-right` 로 가른다.

### 죽은 상수·거짓 주석

- `LEVEL_COLS` 를 만들었는데 화면은 안 읽고 가드만 읽었다(게다가 값도 틀렸다).
  「관찰 σ」와 같은 병이라 지웠다. **상수를 두기 전에 화면이 그걸 읽는지 봐라.**
- `mrcarry` 의 「이 환산은 캐리의 크기만 정하고 부호·시점은 안 건드린다」가
  회계를 바꾸는 순간 **거짓이 됐다.** 근사를 정당화하던 문장은 전제가 바뀌면
  같이 죽는다.
- 화면 문장 「감도 × Δ = 평가」도 거짓이 됐는데 게이트 1,600건이 안 잡았다.

### 커맨드

- `git remote -v | head -2` 로 **원격을 다 못 봤다** — 둘인데 하나만 보고
  명령을 드려 엉뚱한 리포에 밀었다. `head` 로 자르지 마라.
- 시험이 추적 파일을 더럽힌다: `tests/engine` 다섯 파일이 ECOS 캐시
  (`backend/data/raw/bigfoot_*.csv` 14장)를 다시 쓴다. 값은 같고
  `retrieved_at` 스탬프만 바뀐다. 커밋 전에 `git checkout -- backend/data/raw`.

---

## 6. 검증

```powershell
cd C:\Users\infomax\Projects\apps\sauron-v2

# 프런트 (20초)
npx tsc --noEmit ; npx eslint src guards ; npx vitest run

# 백엔드 (7~10분) — test_static_agreement 는 자기가 안 띄운 :8200 을 거부한다
cd backend ; python -m pytest -q --ignore=tests/test_static_agreement.py
```

기준선(2026-09-03): **pytest 1,234 · vitest 1,648 · 0 실패.**

`production-env` 둘이 빨개지면 `.next` 가 dev 판이라는 뜻이다 — 배포 전에
`NEXT_PUBLIC_API_BASE=https://e110430.tailc7b701.ts.net/v2 npx next build` 한
뒤 다시 돌리면 초록이다.

### 대사 검산 (이 레인의 핵심)

`backend/tests/test_mr_legrecon.py` 가 라우트를 타고 재는 항등들:

    명목 = 액면 × pv01 × 1e-4          거래마다 0.00원
    표 세로합 + 비용 = 거래 손익        거래마다 0.00원
    평가+캐리+롤다운+조달+비용 = 손익   봉마다 0.00원
    Σ거래 + 미청산 = 총손익            계열마다 0
    −KRD × Δbp = 추정                  칸마다
    대사 행이 봉에 다 얹힌다            어긋난 날 0
    신호 국고 = 민평 국고               3Y+ 는 ≤0.6bp

**선물을 붙이면 이 시험들을 선물에도 걸어라.** 지금은 `CASES` 가 선물,
`LEVEL_ONLY` 가 BSS 로 갈려 있다.

### 화면 실측

`:8200` 은 건드리지 말고 **별도 포트**로:

```powershell
# 백엔드 (backend.log 를 공개 프로세스가 물고 있으므로 serve.ps1 대신 직접)
cd backend
$env:PYTHONUTF8=1; python -m uvicorn app.main:app --port 8299
# 프런트 — .env.development.local 을 :8299 로 바꾸고 (끝나면 :8200 으로 복구)
npx next dev -p 3201
```

`http://127.0.0.1:3201/?g=strategy&s=mean-reversion` → 계열 행 클릭 →
「전략 실험」 → 「실행」 → 거래 줄 클릭.

---

## 7. 오너 결정 대기

1. **엔진에 롤다운을 넣을지.** 지금 롤다운은 **회계에만** 있고 신호는 여전히
   z 만 본다. 즉 「롤다운이 큰 구간을 골라 들어간다」는 규칙이 아니다. 넣으면
   그건 새 전략이고 사전등록이 필요하다.
2. **FSW 의 IRS 다리를 대사에 어떻게 합칠지** (§4-3).
3. **민평 이력** — 2020-01-02 이전이 조달되면 BSS 표본이 자동으로 늘어난다
   (바닥을 하드코딩하지 않았다).
4. **CSV 스탬프 구조** — `retrieved_at` 이 데이터 파일 안에 행마다 있어서 엔진
   시험을 돌릴 때마다 14장이 통째로 diff 로 뜬다. 사이드카로 빼면 「새로 는
   행」만 남는다.
5. **`/terminal` 내비 등록** — 지금은 URL 로만 닿는다. 등록은 「이 화면이
   제품이 됐다」는 뜻이라 오너 판정이다.

---

## 8. 내일 시니어 트레이더가 물을 것 (09-04 검사 예정)

- **왜 숫자가 달라졌나** — 엔진이 롤다운·조달을 안 세고 있었고, 안 세던
  롤다운이 세던 전부보다 컸다. BSS-7Y 총손익 456만 → 2,506만.
- **왜 2020년부터인가** — 민평 이력이 그때부터라, 대사 못 하는 구간은 안 보이게
  했다.
- **KRD 부호** — 앱 전체 규약(`손익 = −KRD × Δbp`, 음수 KRD = 페이·숏)으로
  통일했다. 종전 MR 「감도」(`hold × 명목`)와 부호가 반대다.
- **선물은 왜 다른가** — 현금결제라 캐리·조달·롤다운이 존재하지 않는 성분이고,
  실가격 대사는 아직 안 붙였다(이 문서의 임무).
